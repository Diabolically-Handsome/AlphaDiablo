diff --git a/Source/diablo.cpp b/Source/diablo.cpp
index 859eff0..6e86033 100644
--- a/Source/diablo.cpp
+++ b/Source/diablo.cpp
@@ -1374,13 +1374,15 @@ tl::expected<void, std::string> LoadLvlGFX()
 	switch (leveltype) {
 	case DTYPE_TOWN: {
 		auto cel = LoadFileInMemWithStatus("nlevels\\towndata\\town.cel");
-		if (!cel.has_value()) {
+		// In HeadlessMode a failed open yields success with a null buffer; treat it as missing
+		// so that we correctly fall back to the Diablo town assets.
+		if (!cel.has_value() || *cel == nullptr) {
 			ASSIGN_OR_RETURN(pDungeonCels, LoadFileInMemWithStatus("levels\\towndata\\town.cel"));
 		} else {
 			pDungeonCels = std::move(*cel);
 		}
 		auto til = LoadFileInMemWithStatus<MegaTile>("nlevels\\towndata\\town.til");
-		if (!til.has_value()) {
+		if (!til.has_value() || *til == nullptr) {
 			ASSIGN_OR_RETURN(pMegaTiles, LoadFileInMemWithStatus<MegaTile>("levels\\towndata\\town.til"));
 		} else {
 			pMegaTiles = std::move(*til);
diff --git a/Source/levels/gendung.cpp b/Source/levels/gendung.cpp
index cf66a04..1e048e6 100644
--- a/Source/levels/gendung.cpp
+++ b/Source/levels/gendung.cpp
@@ -78,7 +78,7 @@ std::unique_ptr<uint16_t[]> LoadMinData(size_t &tileCount)
 	switch (leveltype) {
 	case DTYPE_TOWN: {
 		auto min = LoadFileInMemWithStatus<uint16_t>("nlevels\\towndata\\town.min", &tileCount);
-		if (!min.has_value()) {
+		if (!min.has_value() || *min == nullptr) {
 			return LoadFileInMem<uint16_t>("levels\\towndata\\town.min", &tileCount);
 		} else {
 			return std::move(*min);
@@ -442,11 +442,17 @@ void CreateDungeon(uint32_t rseed, lvl_entry entry)
 tl::expected<void, std::string> LoadLevelSOLData()
 {
 	switch (leveltype) {
-	case DTYPE_TOWN:
-		if (!LoadIntegralFileInMemWithStatus("nlevels\\towndata\\town.sol", SOLData).has_value()) {
+	case DTYPE_TOWN: {
+		// In HeadlessMode the loader reports success even when the asset is missing
+		// (leaving SOLData untouched), so probe the asset to pick the right source.
+		size_t hfTownSolSize;
+		if (!OpenAsset("nlevels\\towndata\\town.sol", hfTownSolSize).ok()) {
 			RETURN_IF_ERROR(LoadIntegralFileInMemWithStatus("levels\\towndata\\town.sol", SOLData));
+		} else {
+			RETURN_IF_ERROR(LoadIntegralFileInMemWithStatus("nlevels\\towndata\\town.sol", SOLData));
 		}
 		break;
+	}
 	case DTYPE_CATHEDRAL:
 		RETURN_IF_ERROR(LoadIntegralFileInMemWithStatus("levels\\l1data\\l1.sol", SOLData));
 		// Fix incorrectly marked arched tiles
