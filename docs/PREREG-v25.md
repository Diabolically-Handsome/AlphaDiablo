# v25-ALT「换届选举」预注册文档(v2,批评者 27 项确认全落地后冻结)

**考题**:总设计师架构完全体首考——在冻结的 v24-leg7 皮筋工人之上重新训练经理。
主张:**换届有增益**(v22-H 的价值观是对着脚本工人学的,班底已换)。

铁律:① 环境侧(python/diablogym)零改动;② 单处方 = 重训经理;fresh/warm 为
**同一处方的预注册对照臂**(先例:v22 恶魔臂 F,裁臂规则预写,金牌仍单臂一次);
③ 金种子 9000-9031 全案至多一次,探针不达发射线不烧;④ 对照值 97.2/92.0 引存档
(同种子 argmax 确定性重放,免重烧——此为"至多一次"纪律下版本间可比的机制依据)。

## D1 班底与仪器(批评者 blocker 修正:改动全部入册)

工人 = v24-leg7,npz numpy 前向(policy.npz 已导出,parity 0/1000)。

| 文件 | 改动 |
|---|---|
| train/train_ppo.py | ① --worker-npz(默认 None);② make_env 的 options 分支:worker_npz 非空时**在函数体内**(spawn 子进程)构造 NumpyManager 并 OptionsEnv(workers={0: net.choose}),外挂 _SeedDiscipline 薄包装(reset seed=None → sample_train_seed 拒采 7000/9000 段,逐局种子入 info);③ --options 守护断言块(mppo/γ1.0/3000微步/n_steps 64/--seed 必传+邻域拒撞)——终结"--options 靠人肉记忆"时代 |
| train/eval_assembled.py | --manager-npz(默认 v22-h,旧档回归可证)+ load_worker 加 .npz 分支 |
| train/export_manager_npz.py | 参数化 zip/npz 路径(argv 同 export_worker_npz) |
| train/export_manager_sd.py(新) | v22-H zip → 全量 policy state_dict(含价值头).pt,M-warm 注入用 |
| train/run_v25_election.py(新) | 条款唯一执行者(互锁:步数达标才准考;ledger 留痕) |

**G-A0(发车前,三合一)**:(a) 新评测路径回归——`--worker npz(leg7) --manager 默认`
满 32 逐种子 ret/died/depth/mode_seq ≡ v24-G3-leg7.json 存档(G0'' 32/32 先例;
兼证仪器未坏 + npz 工人位级保真);失配 → 回退条款:双侧统一表示并重锚参考行。
(b) 子进程轻量条款(修正:torch 模块随 train_ppo 顶层 import 必然进入子进程,v23 先例同;可执行要求 = 不 pickle 网络对象、不在子进程构造 SB3 模型、工人逐拍走 numpy 前向——由 make_env 体内构造 NumpyManager 保证)。(c) 每臂训后:export_manager_npz 导出 + 1000 obs parity
(G-KL-C 判据),不过不考试。

## D2 选举配方(v22-H status.json 存档逐字,批评者 major 修正)

共用:`--options --algo mppo --gamma 1.0 --max-steps 3000 --n-steps 64 --num-envs 4
--total-steps 40000 --worker-npz train/models/v24-worker-leg7/policy.npz`
(SB3 量子化 ≈40.2k 决策,如实注册;两臂串行,决策率锚 v22-H 实测 29/s ≈ 25 分钟/臂)。

- **M-fresh**:从零,lr 3e-4,ent 0.02,--seed 22(v22-H 原种子,最近复刻)。
- **M-warm**:fresh 模型 + `--bc-init v22-H 全量 policy sd(含价值头,热价值头
  不触 v22 雷,freeze=0)`,lr 1e-4,ent 0.005,--seed 23。禁用 --resume-from
  (封-5 断言会炸且语义驮旧,批评者判死)。
- R25.2 判词口径 = "热启动配方 vs 从零配方"整体对比;"旧价值观是包袱/资产"
  只许立案不许归因(lr/ent 混杂已入册)。

## D3 裁决与发射(批评者 blocker/major 修正:两臂皆满 32,配对判据)

1. **提前放弃闸**:两臂各 16 种子(7000-7015)初筛,**双双 <75** 才判训练失败
   免考满 32;否则两臂皆考满 32(7000-7031)——R25.1/R25.2 口径自动闭合,
   16 种子海市蜃楼(v23:105.7→76.0)与选臂偏置一并消除。
2. **胜者** = 满 32 均值高者;±0.05 平分 → 死数少者 → 仍平取 M-fresh。
3. **金牌发射线(配对判据,对 v24-G3-leg7.json 逐种子)**:配对均差 ≥ **+4**
   (≈1 个池外推 SE,锚:v24 探针→金 +5.2,v23 −29.7)且配对赢 ≥18/32 且
   死 ≤6 且哨兵(下条)。中间档 [+2,+4) → "探针级改进,不烧牌,留工作站复赛"。
   胜者均值 ≥85 且不达线 → "连任,本轮无增益(功效限定)";<85 → **"重训未
   复现参考水平,换届命题未考"**(本版不追加重训)。
4. **哨兵(R4 拆包,批评者 major:τ̄ 是经理战略的函数)**:换层率 ≤2.04%(工人
   套利仪表,工人冻结仍有效)、cap <5%、override <3%(≥8% 数据作废)为闸;
   **farm τ̄ 带降格为只记不裁 + 判词强制注记**。条件条款:胜者满 32 DIVE >1/局
   时,τ̄/override 触线走双归因(配比漂移 vs 真退化,v24 P-拐杖-否 先例),
   且新增 DIVE 口径作废线:**DIVE >1/局 且死 >6 → run 作废**(对齐 v23 R4 精神)。
5. **金牌(若发射)**:单臂一次;败臂、未发射臂、本版一切其他 ckpt 永不接触
   9000-9031。

## P 线(全空间划分,落档唯一;死数维度补全)

按序判定,金牌 = G(若发射):
1. 死 >6 → **P25-回退**(死数击穿单独入判词,不混均分叙事)。
2. G ≥ 101.2(=97.2+1SE)且死 ≤4 → **P25-增益(显著)**:v25-golden 登基,
   判词"换届增益成立,双脑完全体首胜"。
3. G ∈(97.2, 101.2) 且死 ≤4 → **P25-点估增益**:"点估增益,功效内不判显著",
   **v24 王座不动**,入册留工作站复赛。
4. G >97.2 且死 ∈{5,6} → **P25-持平(安全性限定)**:判词附"安全性未达 v24"。
5. G ∈[93.9, 97.2] → **P25-持平**:"连任,无显著增益"。
6. G <93.9 → **P25-回退**:"探针虚示,换届失败",烧牌入册不粉饰。

王座条款:仅 P25-增益(显著)变更现役组装体;其余各档(含不发射)v24-golden
连任、leaderboard 行不动,v25-golden 仅追加对照行。榜行格式:notes 强制携带
经理出处(mgr=v25-fresh/warm)+ 对 v22-H 经理的决策分歧率注记(防空洞胜利,
v24 先例)。

## R 线

| # | 预测 | 数字 |
|---|---|---|
| R25.1 | M-fresh 满 32 | ∈[85,100],点 93 |
| R25.2 | 配对(warm−fresh,满 32 同种子) | ∈[0,+4],点 +2 |
| R25.3 | 胜者 DIVE 份额(满 32 与金池各测,双双入册,不一致本身入册) | <0.5/局 如预测;[0.5,1] 边缘入册不叙事;>1 立案(叙事须机制尸检,否则只许写"份额异常机制未明") |
| R25.4 | 金牌(若发射) | ∈[93,103],点 97.5;**带内含 [93,93.9) 回退区——预测带覆盖发射后失败之可能,预先挂账,与 P 线不冲突** |

判词纪律:任何 P25 判词携带 DIVE 份额 + mode_seq 摘要 + farm τ̄ 数字。

## 残余不确定性

1. 经理训练无中途 ckpt(save_freq > 40k)——finally 半成品由驱动步数互锁拦截;
   40k 单抽签无重试条款,训崩落"命题未考"档,如实判。
2. M-warm 驮入的旧价值头对新工人分布的偏差,方向未知。
3. 金池 9000-9031 第三次开牌,跨版本王座比较带固定池选择偏置,判词携带限定。
4. 种子拒采覆盖训练局;评测/金池口径与历版同构。

*v2 冻结:2026-07-10,批评者(3 镜头 27 项)全落地。commit 时间戳即公证。*
