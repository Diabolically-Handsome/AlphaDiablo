# R19-A 训练臂预注册草案(2026-09-09 午,主刀;**未冻结,待 M2 结果与主席签字**)— 一层毕业臂

上位文件:`r19-PREREG-DRAFT-20260908.md`(战役草案);本文件只管阶段 A 的训练臂。

## 〇、主席裁定(2026-09-08 晚 — 2026-09-09 午)

1. 收入端:金币边打边捡(gold-grab-v1)、清扫 v2(箱/桶/石棺,一二层,新触发事实,目标预算按层 24+24,不占进城名额)、购物 v2(full-v2,按「预计收益/金价」排序)——合并探针 R19-M1/M2。
2. 生存端:一层窗内急停(stop-v1)、教练 coach-v05(战备比 < 1 不无条件开 DIVE;连续两个停滞 DIVE 窗后本层锁 DIVE)。
3. 部署形态终点是一张网;earned-dive-suffix 的「一层退化」不能再出现。
4. 阈值变更须新预注册;处女池零接触;冻结件 sha 入台账后才发射。

## 一、假设与可辨结局

**H-A**:在「收入进得来、钱花得对、被围能脱身、教练不乱开窗」的世界里,从父代 7e31dc54 热启动、**所有窗都由学习者实况打**(含一层)的工人,会把一层打稳(一层死亡 ≤ 5%、一层清空 ≥ 50%),并在首次下楼时带着更好的装备(腰带 8、护甲 ≥ 15),二层风险率不升。
**反假设**:(甲)一层稳了但从不下楼(首降率降、深度不动);(乙)收入法条挤占战斗(二层击杀、clvl≥4 降);(丙)急停被学成「一挨打就跑」(击杀/局降、一层清空率降)。四闸与配对复测分辨。

## 二、世界(训练 = 测量;以 M2 合并树的认证字节为准)

| 项 | 值 | 来源 |
|---|---|---|
| 起点权重 | 7e31dc54 | 现役工人 |
| 热启动 | **critic 迁移式**(`sb3-orthogonal-value-only-v1` 或 structured-layout-v1,分离裁剪 v2),**不是** weights-only——理由:带预热回执,正式考卷可直接受理,不再依赖修正案三;代价:critic 预热期(R16 同款) | R16 先例;R18-B 考卷拒绝事件 |
| 学习窗范围 | **farm-dive-v1(全部窗实况,含一层)**——一层保持由构造保证;备选 earned-dive-suffix-v2(父代前缀 + 一定份额一层窗)留作对照臂 | 主席裁定 3 |
| 协议 / 购买 / 经济 | l2-town-v1 / **full-v2** / sustain-loot-v1 | M1/M2 |
| 收入法 | gold-grab-v1(12+12)、sweep-v2(24+24,不占进城名额,触发半径 8,怪物半径 2)、cain-v1、smith-v1 | M1/M2 |
| 生存法 | retreat-v1、**stop-v1**、hunt_scope=l1-only、portal 关、boss_room 关(阶段 B 再开) | M2 |
| 教练 | **coach-v05** | M2 |
| 时钟 / 托管 | completion-l2-r18c;托管 0.5/幂 1.6/战备门/尺 v2;**兑现条件不变**(阶段 A 不改托管) | R18-B |
| 工资项 | 与 R18-B 固定字典相同 | R16 |
| PPO | mppo、CPU、lr 1e-4、ent 0.005、target-kl 0.01、n-steps 512、4 环境(父代合同钉死) | R16 |

## 三、闸门

- **A–D 四闸**:六卷 `r17-anchor-*` 重铸对照(考卷协议 3000 拍旧形态、协议关),critic 迁移热启动的产物可被正式考卷受理;闸 D 身份探针照旧。
- **E 配对复测(主判据)**:训练后工人 vs 7e31dc54,同 M2-FULL 世界、同池 2_133(48 种子),对照行 = **M2-FULL 臂的父代行**(rows sha 待 M2 出);
  - 主-1 一层死亡 ≤ 5%(对照 7/48 = 15%);
  - 主-2 一层清空率(剩 ≤ 3 只)≥ 50%(对照 10%);
  - 主-3 成对存活 saved − lost ≥ +6/48 且单侧 UCB95 < 0;
  - 守门(不得退化):二层风险率不升(≤ 对照 × 1.1)、二层击杀 ≥ 0.9 × 对照、首降率(到达二层的局)≥ 对照 − 3;
  - 反假设(丙)检查:急停触发后 30 拍内存活率、每局击杀。
- **F 机制子指标**:急停触发次数与结局;coach-v05 锁窗次数;DIVE 窗停滞数(对照数百);捡金/清扫/购物按层账本;腰带与护甲首降分布;托管兑现/罚没。

## 四、身份(冻结时填)

协议 bundle sha、桥 sha(现役 build-res)、引擎 sha、补丁栈 0001–0014、工人 zip sha、探针版本、对照行 rows sha、M2 合并补丁 sha、本文件 sha → 台账。

## 五、预算与时间

- 步数 1 048 576(=512×2048);4 环境;存档/哨兵 63 488;10 小时保险丝;发射前泡机 ≥ 30 min;磁盘 ≥ 20 GB(现 894 GB 空闲);Windows 更新暂停(昨夜教训)。
- 评估池 2_133;处女池 2_116–119、2_126–128 零接触。

## 六、发射前顺序(固定)

1. M2 合并树通过审核 → 安装到主树(新版本串,旧默认路径逐位不变)→ 全套件 + 探针回归(16 种子 33023de1…)+ 六卷考场重铸身份 + 双向重烤;
2. 对照行:7e31dc54 × M2-FULL 世界 × 2_133(48 种子)在主树上重跑并登记 sha;
3. 热启动候选铸造(critic 迁移);4096 步冒烟 + 30 分钟泡机;
4. 冻结本文件(sha 入台账)→ 发射 → 台账。

## 七、待主席裁定

1. 热启动方式:critic 迁移(推荐,考卷可受理)还是 weights-only(需修正案三);2. 学习窗:farm-dive-v1(推荐)还是 earned-dive-suffix-v2;3. 闸门数字;4. 发射时间。
