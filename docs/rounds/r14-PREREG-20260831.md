# R14 pre-registration: wage amendment trial, option B, a bounded return of the descent bonus
Base: the clauses of r13-PREREG-FROZEN-20260830 (adaab461) + r13.2-PREREG (de6c2a71) are inherited
(digests of the pre-translation text).
Decision of 2026-08-31: run option B first, to see whether the speed runner still appears under B; if it
does, that route can be sealed for good.

## 1. Rationale and experimental frame
R13.2 scenario ruling: refusing to dive is the true optimum under the current worker wage system (option A
falsified at the level of a theorem). The problem belongs to the wage constitution. This case is **not
simply an amendment but a direct test of the "speed-runner hypothesis"**:
- B holds ⇒ the cheapest amendment route is open;
- B fails in the speed-runner direction ⇒ that route is sealed permanently on hard evidence, and option D
  (escrow payment) becomes the only solution.
Both outcomes are final knowledge. Advantage of this control: this round's starting worker has
internalised the survival skill of a 10% death rate (the same lineage starting point as R13.2 arm α2 = the
certified worker); if even this worker degrades into a speed runner, that proves the incentive structure
itself is toxic.

## 2. Rules (minimal amendment)
- `worker_descend_bonus_fraction = 0.25`: only inside live DIVE windows, the worker keeps 25% of the
  descent bonus (v2 progressive: L(d)→L(d+1) full amount 48×d, kept 12×d);
- the meaning of `w["bonus"]` = **the amount actually stripped**, and the identity R ≡ W + bonus keeps
  holding bit for bit (the hard check in the shared window kernel is enforced as before); the manager's
  ledger R is not touched at all;
- fraction=0 (default) or outside the live-DIVE domain: the path is bit-identical to the old rules
  (fail-closed double check: worker_env validates the classroom, OptionsEnv validates authority);
- shaping (option A) is set to zero and shelved; the a11 cold-start lever of 2.0 is kept (the same as
  α/α2).

## 3. Recipe
Arm r14-arm-b-wage: the R13.2 arm α2 recipe with --worker-depth-shaping-unit removed and
--worker-descend-bonus-fraction 0.25 added; T = round2048(266240/(1−s_G0)), frozen after G0-4
calibration; every other flag unchanged. The four gate criteria / exam sheets / pools / gate D probes /
opening sha guard: inherited unchanged from the frozen R13 version.

## 4. Speed-runner decision rules (pre-registered, to prevent interpretation on the spot)
Based on the devil sheets (farm-dive-v1 double registration, both pools):
- **Signature of the speed runner's return**: died ≥ 90/128 and l3+ ≥ 30 (death rate and depth both
  surge, in the direction of the R10 candidate sheet's 124/56) ⇒ **option B sealed permanently**, and
  option D is opened;
- **Signature of B holding**: gate A holds (died ≤ 110 and McNemar UCB<0) and gate B recovers significantly
  (paired depth LCB > −0.10) ⇒ route B is open, and whether to tune it further goes to a decision;
- **Middle ground** (survival held but depth recovery insufficient, l3+ < 30 and LCB ≤ −0.10) ⇒ record the
  evidence of insufficient dose and go to a decision: raise the fraction and retry one round, or move
  straight to option D;
- Additional forensics: the micro_steps distribution (the R10 speed-runner signature = episode length
  collapsing to ~700), per-depth death rates, the total descend_bonus_kept, and the timeline of the
  training descent rate (against R13.2's collapse curve 2.04→0.20: if B works, the curve should hold or
  rise).

## 5. Gates (launch only when all are green)
Final suite all green (new accounting tests for option B, test_r13_live_dive.py 6/6); four anchors
re-baked as certification; G0-4 classroom acceptance ≥1.0 descends per 10 windows + a non-zero
descend_bonus_kept booked; frozen sha + launch order; launch pre-authorised (the decision above, with the
previous round's practice of launching without waiting continuing).

## Freeze statement
This file is frozen once the G0-4 calibration numbers are filed; its sha256 is in the ledger (not
published).
Driver: `train/runs/r10-staging/run_r14_arm_b.py` (not published).

---
## Final chapter (2026-08-31, G0-4 calibration and freeze)
- G0-4 acceptance: 2.31 descends per 10 windows (the highest in three rounds: prior 1.72 → potential 1.96 →
  real money 2.31); descend_bonus_kept=156.0 exactly (12 descents, Σ12×d); shaping ledger 0/0 (option A
  shelved, confirmed);
- Calibration: s=0.337 → T=401,408;
- Gates: suite 870/0; four anchors re-baked and reproduced bit for bit; test file 6/6;
- This file is frozen from here on; its sha256 is in the ledger. Driver: r10-staging/run_r14_arm_b.py (not
  published).
