# R13.2 final verdict: potential-based depth shaping (option A)
2026-08-31, all exams finished. Pre-registration sha16 de6c2a71 (digest of the pre-translation
text), arm α2 (r13-arm-a2-shaping, sha16 77577ffc), T=411,648 steps in 58 minutes. Case ledger
`r10-staging/r13_ledger.jsonl` (not published).

## 1. Exam overview (anchor = the re-baked certified sheets)
| Sheet | died | l3+ | ret | kills |
|---|---|---|---|---|
| Devil a (anchor 124/56/148.2/14.5) | **13** | 0 | 66.96 | 29.8 |
| Devil b (anchor 124/67/174.5/15.1) | **21** | 0 | 66.18 | 29.8 |
| M29 a farm-only (anchor 146.5/43.0) | 113 | 26 | **150.93 (1.030×)** | 37.5 (0.872×) |
| M29 b farm-only (anchor 146.1/41.0) | 111 | 31 | **148.17 (1.014×)** | 37.0 (0.902×) |
| M29 full registration a/b (information run) | 14/23 | 0/0 | 71.8/66.3 | 33.2/30.6 |

Four gates: A ✅ unprecedented (deaths cut by another 70%); B ✗ completely (l3+ at zero); C half passed (ret
beats the anchor in both pools for the first time, kills 0.87/0.90 still short of the line, but the series
R12 0.65 → R13 0.75 → R13.2 0.87 converges monotonically); D ✅ (D1 0.730, D2 0.168). **No candidate is
crowned.**

## 2. Scenario ruling (the case's core scientific output)
The decision device planted by the theory review triggered exactly: the classroom descent rate during
training (per 63,488-step interval) was **2.04 → 2.01 → 1.28 → 0.92 → 0.51 → 0.29 → 0.20 per 10
windows**. During the shaping pulse the worker dived actively (65 times in the first interval); once the
critic absorbed −φ, the pulse vanished as the theorem says, and the policy swung back monotonically to an
even more thorough refusal to dive. The shaping conservation ledger was exact throughout (credited 2392 /
refunded −1976).

**Ruling: scenario one holds. Refusing to dive is the true optimum under the current worker wage system.**
It is not a failure of exploration, not a shortage of lesson time, and not a credit-assignment obstacle
(all three were briefly repaired by the shaping pulse and then pushed back by the theorem). Option A
falsifies itself in the cleanest possible way: potential-based shaping does not change the optimal
policy, and the optimal policy itself is to refuse to dive.

The complete chain of evidence over three rounds: R12 = the classroom does not exist (structure); R13 = the
classroom exists, and the worker learned the optimum of the wage function (refusing to dive); R13.2 =
mathematical proof that this optimum really is refusing to dive. **Where the problem finally belongs: the
wage constitution.** The worker's wage function must contain an income term truly bound to depth
progress; otherwise every constitutional form of shaping is futile.

## 3. Side results
- Anti-forgetting almost healed: on the M29 sheets ret beats the anchor in both pools (1.030×/1.014×),
  kills converge to 0.87/0.90; l3+ 26/31 vs anchor 14/17, still nearly doubled;
- Survival skill at its peak: 13/128 deaths on the devil sheets, a 10% death rate (anchor 97%), and 14/23 in
  the deployed form: this worker is the best "one who comes back alive" the project has produced, and it
  lacks only a reason to descend.

## 4. Constitutional options for R14 (for decision)
- **Option B, bounded return**: inside live-DIVE windows the worker keeps part of the descent bonus (e.g.
  25%, flat or capped). The original concern: the speed runner comes back. Mitigating argument: this
  round's worker has internalised extreme survival (a 10% death rate), a starting point entirely unlike
  R10's reckless speed runner; a death-forfeit clause can be added as a backstop.
- **Option D, escrow payment (a combined option, leaning recommended)**: the descent fee goes into escrow,
  vests only if the worker survives the following windows, and is forfeited on death, reusing the kill-lock
  escrow machinery certified in R11 (certified then as "an anti-suicide valve with zero side effects").
  Unlike R11's manager-side collapse ("any forfeit lock → constant FARM"): the manager then could not
  influence the frozen worker's survival, so forfeiting was a pure tax; now the worker fully controls its
  own survival (13/128 as proof), so escrow is a learnable contract for it: "go down, stay alive, get
  paid".
- Pausing is also possible: although the candidate was not crowned, its survival skill and FARM strength
  are valuable assets that can be archived and considered in R14 together with manager retraining.

## 5. Closing formalities
The arm α2 product is archived as an experimental artefact; zero fresh seeds used in the whole case; 2_116
untouched; the final dive_share of 0.522 exceeded the band and was recorded as an anomaly (it accompanied
the convergence to refusing to dive; the mechanism is clear); this document is the master copy, NEW FILES
ONLY.
