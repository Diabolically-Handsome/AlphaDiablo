# PREREG-捆绑评测基建(B1):双经理捆绑转正 · 死门重校仪表 · P8 换种子复制腿

状态:**D0 已获亲批,施工中(冻结 commit 待 E 项与 W-G0 烟测同卷)**。

**D0 批文转录(值守 2026-07-17 15:36 入册)**:总设计师应答原文「**三个都准了 嘿嘿 开工吧**」——D0-① 金评划界"不立法"准;D0-② 8000 池首曝 2 对/4 发 + 按发计 + 每案至多一对自本案后生效,准;D0-③ 2026-07-17 批文兼作 P8 腿发车亲批,确认。D0-④ 值守核验:批文时间戳已于转录时复核;BC_SD_WAIVER 随本状态行入册(豁免依据见 W7)。

**编号注记**:本案系**测量与仪表化案**(R2 同类),不占 v 系列战役号,案号 B1(基建一号);P8 腿虽含训练点火,其性质系复制测量之载具,不烧金种子、不参与任何名分与发射判词。**④×② 内容案(经理换届/联合训练/观测补旗等)另立预注册,本案零涉。**

**零漂移之诚实表述(勘正,承合规 M-1/工程 M-2)**:本案 E 项施工必改 train_ppo.py,故**全束 impl bundle sha 对 v32 必变**,以案级首取值为对账基准照 W5/W10;本案所断言之"零漂移"系**环境与评测协议束零漂移**,以现成 runtime_five(eval_contract 之 bridge/engine/game_data/assets/protocol 五 sha;protocol 束覆盖全部 python/diablogym 环境文件与 eval 协议文件)对 v32 台账 CASE_RUNTIME 落定值逐字相等为机器可执行断言(W-E0)。"环境侧子束"一语废除,不复出现。

## 案由与批文链(如实转录)

- **批文**:总设计师 2026-07-17 对捆绑评测基建案范围呈报之应答(起草稿误记 07-18,D0-④ 复核勘正):「**按您说的,批了**」——范围 = 经理审判庭改革菜单**处方①**(双经理捆绑评测转正 + 经理敏感度指标)+ **处方②**(死门重校 + 训练中期金丝雀评测 + 经理观测漂移监控)+ **P8**(换训练种子重跑一条 ctrl 配方腿 + 8000 系留出种子池之灾难判据预登记)。批文日期系如实转录项,**冻结前由值守对应答原始时间戳复核一次**(承 v32 E4 日期勘误教训)。
- **发车亲批注记(承合规 m-1)**:v32 路径裁定对捆绑案明文"发车须总设计师亲批";本案理解**上引批文(范围明含 P8)构成 P8 腿发车亲批**,此理解于冻结呈报单列确认(D0-③),防日后"批了立案还是批了发车"之争。
- **批文链上游**:① v32 判决附录路径裁定①——档3 触发解耦条款,「④×② 捆绑评估案取得启动资格,另立预注册、批评者面板先行,发车须总设计师亲批」;② FORENSICS-经理审判庭 V5 立案 + 改革菜单排序(①②系菜单前两位,「P8 系一切终审级归因的先决」);③ FORENSICS-F2 设计输入节 + P5/P7 附录先决登记项(「P8 与 horizon 敏感性声明一并列入捆绑案预注册」)。
- **性质裁定(在此入册)**:本案系**测量与仪表化案,环境侧零改动**——`OptionsEnv`/`WorkerWindowEnv`/掩码/工资/契约字段一字不动(W-E0 断言,见编号注记之诚实表述)。一切施工限:训练驱动层旋钮/评测通道/训练侧只读仪表/离线分析件。
- **必读证据基**:FORENSICS-F2(正文 + P1-P9 附录)、FORENSICS-经理审判庭(V1-V5)、PREREG-v32 判决附录(F1/F2/F3,本案直接受理 F2)。

## D0 单列请示(冻结前置;总设计师亲批三项 + 值守核验一项)

1. **金评划界(修宪面,承合规 B-3)**:处方①转正义务**明文限定于探针池(7000 系)与留出池(8000 系)工人考发**;金种子池(9000-9031)考发是否捆绑 M29 读数,涉金池暴露 ×2 与"每案一烧"名分法,系修宪级,**本案不立此法**——请亲批确认"不立法"本身,金评维持现行单 H 纪律,捆绑与否俟菜单④换届案并案议。
2. **8000 池首曝与暴露纪律立法(承合规 M-3/统计 m-8)**:本案首曝共 **2 对 / 4 发**(K1/K2 参照对系判据验证之构造性必需 + L4/L5 腿考对);HOLDOUT_EXPOSURE 记账口径 = **按发计**,4 发全数入账;"后续每案至多一对捆绑发"之纪律**自本案案结后生效**。请亲批首曝额度与立法条文。
3. **发车亲批确认**:确认 2026-07-17 批文构成 P8 腿发车亲批(见案由)。
4. **值守核验(非亲批)**:批文日期时间戳复核;BC_SD 不重生成之裁量豁免(D1-W7)随冻结呈报单列。

## 一战一处方

处方 = "**给评测环装上第二双眼睛和心电图,再问一次:重训连带劣化(F2)是不是训练种子 303000 的独案**"。具体三肢:

1. **双经理捆绑评测转正**:自本案起探针池/留出池一切工人考发标配 H+M29 双发,新增经理敏感度指标 MS;**H 轨保留裁决默认(R1-W7 契约与 DEFAULT_MANAGER 字段零触碰),M29 轨系附加科学读数,永不作裁决线**。
2. **死门重校仪表 + 金丝雀 + 漂移监控**:calib/sentinel/dry-anchor 全部加密采样并挂预注册阈值之 would-trip 双读数,**本案内一律只记不裁**;训练中期金丝雀评测走检查点离线通道,种子集预登记防窥考;经理观测漂移随金丝雀落档。
3. **P8 复制腿**:v30/v32 之 ctrl 配方**逐字**,仅换训练种子;考于 7000 探针池(双经理)与 8000 留出池(双经理);灾难判据先预登记、后于 8000 池验证。

零金种子(9000-9031 全程不触碰)、零名分变动、零发射、零环境侵入。**有效复制数命题**:F2 全部结论现系 1 检查点×1 配方×1 训练种子之 N≈1;本案把训练种子维度打破为 N=2,并为后案沉淀常备仪表。两个方向的结果都算赢(判词规格见 D3 转化条款,措辞已按 N=2 限定收口)。

## E 施工清单(仪表与评测通道;环境侧零侵入,随冻结 commit 入库)

- **E0 零侵入总则**:任何 E 项不得触碰训练 RNG 流、梯度、env 流、掩码语义、契约字段。凡训练侧新增旋钮,须过 W-G0-零侵入实弹(见 D1)方可冻结。**本案训练侧旋钮封闭枚举(共三枚,承工程 M-1/M-5)**:`--ckpt-every-steps`(暴露现成 AtomicRolloutCheckpointCallback 之 every_steps,调用点现写死 250,000)、`--sentinel-every`、`--dry-anchor-every`(两回调现走默认 500,000;均系纯读+IO,dry-anchor 用自有 rng(26) 不碰训练 RNG)。`--calib-probes` 系现成 CLI(逗号分隔整数表),零施工。**不新写任何检查点导出路径**——原判"新增 `--canary-export-steps`"废除:现成回调自带 rollout 边界原子保存、量子对齐与案中 impl-sha 漂移断言,重复发明反引"步数已记、梯度未吃"边界病。
- **E1 双经理捆绑评测通道(处方①)**:驱动器级——每次工人考发自动成对(同 worker 同种子池,`--manager-npz` 分别 H 与 M29,承 v30 面板"逐次显式、禁默认回落"纪律);exam_ok 事件强制加携对侧 tag;新增 MS_REPORT 事件(逐种子 MS 向量 **+ 带符号量 Δ_H−Δ_M29 并列**(承统计 m-7,防"M29 侧更差"型翻转被绝对值吞没;P3 分区表 B 类系反向先例)+ max/中位/超线计数,常量见 D3)。**转正条款**:本案判决附录落账后,双经理捆绑升格为 PREREG 范本义务(后续探针池/留出池工人考发预注册缺捆绑条款者,面板应以 blocker 拦截);**义务范围明文排除金评(D0-①)**。**豁免封闭枚举(承合规 M-4/工程 m-4)**:①运维快筛/健全读数发(L1 型,半池单 H);②补导出/parity 复验类零分数发;③金丝雀离线读数发(自身即双经理)——添类须面板过审。契约字段不升版——纪律靠范本与面板,非运行时强制,弱点在残余⑪登记。eval 协议文件零手改。
- **E2 金丝雀通道(处方②,按工程 B-1/M-1 重设计)**:`--ckpt-every-steps 98304`(=48×2048)令现成回调在腿内 +98,304k 步原子落 ckpt zip;npz 由**案外独立进程**以现成 `export_worker_npz.py`(自带 parity 自检,v32 补导出通道先例)逐 ckpt 离线导出;金丝雀评测走 eval_assembled(numpy 前向,与终考同前向路径;**禁直接喂 ckpt zip 走 torch 前向,口径不同**)。**金丝雀评测种子面 = 全池 7000-7031**(eval_assembled 之 `--seeds` 仅受连续区间,eval 协议零手改;C 集系离线提取之**记分种子集**,仅约束 RB.5/RB.8,不约束考发面;成本实测外推 ~70-80 秒/发)。**金丝雀点表写死**:中期四点 +98,304k(k=1..4,即 3,596,288 / 3,694,592 / 3,792,896 / 3,891,200)+ 终点 npz(nt 3,997,696);机械使然另落之 +491,520(3,989,504)ckpt **照常归档但不入金丝雀序列**(点表预登记即封闭,防多一点之后见择优)。CANARY_EVAL 事件落档。**防窥考三条(写死)**:①记分种子集 C 于腿点火前以 CANARY_SET 事件冻结;②金丝雀一切读数只记不裁,禁作任何腿内干预依据(无早停、无调参、无检查点重选——腿之终点检查点唯一 = nt 3,997,696);③8000 留出池与金丝雀通道构造性不相交(C ⊂ 7000 池)。**金丝雀终局(承合规 M-5)**:同一 ckpt×manager 组合之 CANARY_EVAL 落档**至多一条即终局**;重试仅限"无档案之运维失败",重试计数入 OPERATIONAL-canary 事件;**补评截止 = 首个 L1 之 FIRING_START 事件时间戳之前**(承统计 m-3),此后禁补,防后见。
- **E3 死门仪表(处方②)**:①calib 探针 **10 点**,经现成 `--calib-probes` 传逐字步表(腿内每 +49,152 步一点,k=1..10,全局步表见 D2,量子对齐),全程 `--calib-record-only`(leashed_ppo record_only 下 tripped 旗永不武装,已核验);②新门 would-trip 双读数随每点落 GATE_WOULD_TRIP 事件:**绝对门** distill_ce > 0.2(F2 血统基线目标;**饱和预期注记**:v32-ctrl 两点实测 0.974/1.010,10 点大概率满超——该门系重校目标线非区分器,判词禁作异常叙事,承工程 m-2)、**梯度比门** g_pg/g_ce > 30,**口径钉死**:比值 := calib.jsonl 之 g_pg/g_ce,其中 g_ce = ‖∇(β·distill_ce)‖(**β=0.015625 已折入**;v32-ctrl 实测 44×/52×,与 F2"20-90×"同口径;字面裸 CE 梯度读法差 64×,禁用,承工程 m-1);③sentinel 经 `--sentinel-every 49152` 加密至 ≥10 点,口径维持 v28-v30 旧掩码同尺(v32 残余承继);④dry-anchor 经 `--dry-anchor-every 49152` 加密,**双参考钉死(承统计 M-4/工程 m-3 合并)**:王座级绝对参考 **0.7515**(王座入场值,v32-ctrl 腿首发点实测同值在册)/ 谱系级血统参考 **0.6305**(v28-leg1 起点,F2-P9 在册——漂移早于 v32 之 +12pp 血统事实随定标数据入册,防误导后案挂闸);**增量门零点 = 0.7515**(would-trip 线:增量 > +2pp;回调跨界后首发,首点约 3,538,944 步,非入场步,如实登记)。**以上一切门在本案内只记不裁**——阈值系定标输入,正式挂闸另案。
- **E4 经理观测漂移监控(处方②)**:载具指名 **probe_f2_replay.py 范式**(评测档案不存观测,重放探针系唯一通道;零 RNG 消耗、以档案行逐字段对账自证保真;**保真锚顺序写死:先落金丝雀评测档案、后重放对账**,承工程 m-8)。OBS_DRIFT 事件 **schema 钉死(承统计 M-5)**:8 追加维逐维 {mean, std, min, max, P5/P50/P95} + 窗末 walkable 体态 {walkΣ/121 窗末均值, 西南带均值}(局部图下标 44-164);**漂移读数 := 腿侧对王座侧逐维 P50 差**,统计量封闭枚举,禁判读日另择表述。依 P1 改判与 P5 勘正,F-lock 机制在基础 295 维体态而非 8 仪表维,监控面双覆盖。离线分析件(具名新文件)随判决附录。
- **E5 判别器工具箱 v2**:manager_invariant_registry 增设**近失判据**(P4:7017 型"分数不变、轨迹可变"漏检勘正);复合签名判别脚本(具名新文件,常量见 D3)随冻结入库;**判别器语义收窄条款(承 P7 裁定三)**:"经理不变式"只证"换考官无效",不证"损失不可回收",一切引用判词强制携此限定。
- **E6 launcher 加固成文(承 v32 OPS 事故;具名交付,承工程 m-6)**:交付物 = `train/launch_case.sh` + launchd plist 模板(孤儿化直挂,PPID=1)+ caffeinate 断言 + progress.jsonl 心跳 mtime 值守检查项操作细则(阈值成文)。仓内现无任何 committed launcher(v32 系临场手作),本案将其由事故补丁升格为驱动器标准条款,随冻结 commit 入库。
- **E7 测试同步**:三旋钮单测(ckpt-every/sentinel-every/dry-anchor-every 步集行为)+ MS 计算 + 签名判别器 + calib 步表解析;**契约 contract_revision 零触碰**(无环境语义变更,4 不升 5)。
- **E8 附属脚本入库(承合规 m-8)**:W-C 机器提取脚本(depth≥2 提取照 depth2_count 先例)随冻结 commit 入库——提取结果有 CANARY_SET 事件,提取器不入库则"以档案行为准"不可复验。

## D1 世界身份与 W 门

W1 冻结公证 / W2 版本 / W3 引擎 REF / W4 diabdat / W5 impl 阶段取样 / W6 runtime / W8 空闲 + 驱动器互斥 / W9 首燃先决 / W10 续跑对账 / W11 每发身份重申 / CASE_RUNTIME 案级五 sha 对账——全部照 v31/v32 驱动器逐字承继。本案特设:

- **W-E0 环境零漂移断言(按工程 M-2 重设)**:预检断言本案 runtime_five(bridge/engine/game_data/assets/protocol 五 sha)与 **v32 台账 CASE_RUNTIME 落定值(钉为驱动器冻结常量)逐字相等**——语义恰为"环境 + 评测协议零漂移",零新机械;不等 → 不冻结不发车,CASE_HALT_ENV_DRIFT 呈报。全束 impl sha 因 E 项必变,以案级首取值为对账基准照 W5/W10。
- **W7 工件钉死**(全文 sha 以驱动器冻结常量为准,失配即 P4 不发车):king zip/npz(v28-leg1,`976b6c05…f27f8a` 谱系)、H npz(== DEFAULT_MANAGER_SHA256)、M29 npz(`89441388…7b66d6`)、KING_SD(沿用 v32 件 + 案内 check_teacher_parity 0/1000 复验)。**BC_SD 沿用 v32 重生成件之显式裁量条款(承合规 M-2)**:v32 先例系"impl 全束变 → BC 回执失效 → 重生成";本案全束 sha 亦必变,豁免重生成之**依据明文** = BC 生成链不经训练驱动层旋钮、且 runtime_five 零漂移(W-E0);此豁免非默认承继而系案级裁量,BC_SD_WAIVER 事件入台账,随冻结呈报单列(D0-④)。sha 以 v32 台账 BC_REGEN 落定值冻结为本案常量。**PRIORS 前科档案钉死**:v32-ref-launch(113.0)/ v32-ref-science(140.9)/ v32 ctrl-full32(92.3)/ ctrl-m29(125.2)四档全文 sha 常量断言,PRIOR_REFERENCE 事件入台账(指纹对照基准,先封存后引用)。
- **W-G0-零侵入(E 项旋钮之冻结先决,实弹;按合规 B-2/工程 M-4/统计 m-4 重设)**:**烟测种子 305000**(非案腿种子,预检断言未见于历案台账任一 leg_start 且 ∉ 评测各池——弃 304000,防"点火前偷看腿前段遥测"暗门);同配方短程对照 **2 × 102,400 步**(50 rollout,整除 2048):带全套旋钮 vs 裸 ctrl 配方;**烟测专用步表写死于案内,保证窗内实际发生 ≥1 个 calib 点、≥1 次 ckpt 保存、≥1 次 sentinel 与 dry-anchor 发射**(解析逻辑同一份代码;"仪表被证零侵入"不得实为"仪表根本没跑")。**相等判据改张量级**:policy state_dict + optimizer state 逐张量 torch.equal;遥测比对限 RNG 相关字段(step/loss/ret 等)逐字段——文件字节级比对废除(SB3 zip 成员时间戳与 pickle 序列化不可复现,按文件比对必然假阳性)。**烟测遥测除上述位级判据外,禁作任何 go/no-go 或回炉输入**。不等 → 不冻结不发车,仪表回炉重设计(G0_NULLINTRUSION 事件,含双侧张量摘要 sha)。限制:短程外推全程之 IO/时钟理论残余,残余⑨登记。烟测成本 ~2×9 分钟(按 v32 腿 45.3 min/499,712 步折算)。
- **W-H8 留出池处女断言**:预检扫描 eval-assembled 目录(meta.seeds)、全部榜文件与历案台账,断言 8000-8031 零出现;命中 → 停机呈报,不得静默降级。**证据等级如实注记(承统计 m-5)**:此系**登记面处女**断言,历史未登记之手工暴露不可探测,入残余⑭。**8000 池纪律在此立法**:该池专用于灾难判据类验证,本案系首曝(HOLDOUT_FIRSTBURN 事件,**本案 2 对 / 4 发**,D0-② 单列请示);HOLDOUT_EXPOSURE **按发计**累计;"后续每案至多一对捆绑发"自本案案结后生效;8000 池非金池,无名分义,但暴露预算同样不可再生。**驱动器预检并断言训练种子 ∉ 8000-8031**(train_ppo 内置撞段守卫仅含 7000/9000 段,承工程 m-5,不改 train_ppo 语义,驱动器侧补)。
- **W-C 金丝雀记分集冻结**:C := v32-ref-launch 档案中 depth≥2 之全部种子(E8 脚本机器提取,预期 7 席,以档案行为准,实数记 n_D)∪ 健康对照 {7003, 7011}(P5 无尖峰种子,阴性对照)。预检时提取并以 CANARY_SET 事件写死(携 n_D 与逐种子名单),腿点火前无此事件 → 不发车。**RB.5/RB.8 之点与带以 n_D 为准之比例式条款见 D5(承统计 m-1)。**

## D2 配方与考发序列

**预检序列(严格串行;承工程 M-3 重构——原"REF_BITEQ 独立承接步"废除,launch 侧位级承接由 V1 方差发一发两用)**:

仪表施工 commit(E1-E8 全件)→ W 线全过(W1/W7/PRIOR_REFERENCE/W-E0/W-H8/304000 与 305000 台账扫描)→ W-G0 零侵入实弹 → FREEZE_SHA/CASE_RUNTIME → **V1 方差发**(兼 RB.1 位级承接:对钉死之 v32-ref-launch 逐种子逐字段 biteq;失配处置循 RB.2 带外条款之漂移分诊,禁以"新读数也合理"降级)→ **K1/K2 留出参照两发** → CANARY_SET → **CRITERION_REGISTER**(嵌 K1 提取之 8000 池 depth≥2 种子名单实例化,承统计 m-2——"先登记后开箱"对名单本身成立;该事件系 D3 判据之重申性落账,非案中另立判据之口子,承合规 m-7)→ P8 腿点火 → 金丝雀离线序列(全部先于 L1)→ 腿考五发 L1-L5 → CRITERION_VALIDATE → R 线记分卡 → 判决附录。

**science 侧(140.9)承接**:降级为 PRIORS 全文 sha 钉死(W7 已含),不设新发;**M29 侧评测确定性系推定**,推定注记随一切 M29 判词,实测另案补测(残余⑮;与 RB.2 转化条款对齐,承合规 m-4/统计 M-2)。

**评测发清单(每发额度 = 台账制 2 次,-b 封闭枚举照 R2 D4-P2 逐字;命令形 = eval_assembled 逐字,--manager-npz 逐次显式)**:

| 发 | tag | worker | manager | 种子池 | 用途 |
|---|---|---|---|---|---|
| V1 方差发 | `b1-varprobe-launch` | king npz | H | 7000-7031 | 王座参照种子内方差估计,兼 RB.1 位级承接(**独立重评一次之授权在此落章**:全新进程重烧 king×H,与 v32-ref-launch 逐种子逐字段比对;只记不裁,不改锚、不改参照名分;**V1 永不替代配对基准,v32-ref-launch 原档终局地位与 RB.4 基准身份不变**,承合规 m-2) |
| K1 留出参照 | `b1-ref8k-launch` | king npz | H | 8000-8031 | 8000 池王座 H 参照(首曝) |
| K2 留出参照 | `b1-ref8k-science` | king npz | M29 | 8000-8031 | 8000 池王座 M29 参照 |
| L1 快筛 | `p8-s16` | P8 npz | H | 7000-7015 | 运维健全读数(**E1 豁免枚举①适用,单 H 合规**) |
| L2 主考 | `p8-full32` | P8 npz | H | 7000-7031 | F2 复现主读数 |
| L3 捆绑 | `p8-full32-m29` | P8 npz | M29 | 7000-7031 | MS 指标与经理格点 |
| L4 留出 | `p8-hold8k` | P8 npz | H | 8000-8031 | 判据验证·H 侧 |
| L5 留出 | `p8-hold8k-m29` | P8 npz | M29 | 8000-8031 | 判据验证·M29 侧(经理敏感真值) |

**P8 腿(唯一训练点火)**:v30/v32 ctrl 配方**逐字**——`--worker --algo mppo --gamma 1.0 --max-steps 3000 --n-steps 512 --num-envs 4 --lr 3e-4 --ent-coef 0.005 --total-steps 499712 --distill-beta 0.015625 --teacher-sd <BC_SD> --teacher-override <KING_SD> --skip-dry --manager-npz <M29 npz> --resume-from <king zip> --allow-legacy-resume --no-drink-sovereignty --calib-record-only`,偏离**封闭枚举五处**并逐字登记:

① `--seed 304000`(**种子选择规则写死**:原谱系种子 303000 + 1000,常量,禁止任何形式之种子搜索、重选或"跑坏了换一颗";预检断言 304000 未在历案台账任一 leg_start 出现,命中 → 停机呈报而非顺延取值);
② `--calib-probes 3547136,3596288,3645440,3694592,3743744,3792896,3842048,3891200,3940352,3989504`(k=1..10,腿内 +49,152k,量子对齐);
③ `--ckpt-every-steps 98304`;④ `--sentinel-every 49152`;⑤ `--dry-anchor-every 49152`。
(原 `--canary-export-steps` 废除,见 E2。)五枚仪表旋钮全数过 W-G0 方可冻结。

nt 闸:nt_zip == **3,997,696**;timeout 6h;导出 export_worker_npz(自带 parity)。**launcher 按 E6 加固条款点火。**

**金丝雀评测(检查点离线,只记不裁)**:中期四 ckpt(+98,304k,k=1..4)各经 export_worker_npz 导出 → eval_assembled **全池 7000-7031** × {H, M29} 各一发(共 8 发)+ 终点 npz 即腿考本体;CANARY_EVAL 事件携 {ckpt 全局步, manager, 逐种子 ret/died/depth/D 窗数/τ 分布, **τ 中位距 25 地板之近失距离脚注**(承统计 m-9,只记不裁;P5 型重放取证明示出界不做), OBS_DRIFT 摘要}。金丝雀发不占评测发额度;终局与补评截止条款见 E2。

**考发终局条款(v32 逐字)**:一切 eval 档案落档并记 exam_ok(携档案 sha16)即案级冻结;续跑经 expected identity + 台账 sha 复验采信(exam_or_adopt),禁 .void 重烧;残档无账或有账无档均停机呈报。

**本案无放弃闸(测量案性质,在此登记)**:P8 系复制测量,一切分数皆读数——v32 之教训恰是"腿≈复现"默认被证伪,故不得以低分作废腿。运维闸仅三式:rc=0 ∧ nt=3,997,696 ∧ parity;未过按 P 线额度重点火。

**算力预算登记(承工程 m-7,实测外推)**:腿 ~45 min、烟测 2×~9 min、评测 8 发 ~12 min、金丝雀 8 发 + 4 次导出 ~15 min;全案单机单日 ≈ 1.5-2.5 小时,6h timeout 极宽裕,无预算风险。

**驱动器**:`train/run_b1_infra.py`,克隆 run_v32_sovereign.py 骨架(常量区承继 v32 全部 W7 常量 + PRIORS 四档 sha + v32 CASE_RUNTIME 五 sha + SEED=304000 + 步表常量;工具区 log/sha/require/runtime_five/exam_or_adopt/biteq/by_seed/depth2_count 等逐字承继;阶段 S0 预检 → S1 烟测 → S2 V1 → S3 K1/K2 → S4 CANARY_SET+CRITERION_REGISTER → S5 腿 → S6 金丝雀 → S7 腿考 → S8 判据验证 → S9 记分卡;防后见条款由阶段顺序强制)。

## D3 判据、常量与判词条款(全部预登记,案中禁调)

- **区间约定与临线条款(全案统一,承统计 M-1)**:一切带均为**闭区间** [lo, hi],带内 := lo ≤ x ≤ hi;RB.4 之"带外上/下"即 x > hi / x < lo(端点归属自相矛盾之旧文废除)。**临线** := 连续量 |x − 边界| ≤ 0.05×带宽,计数量恰在边界或距边界 1——临线判决**强制携"临线 + 线未重标"注记**(承 v32 D3),适用于一切硬边界(RB.4 之 −2/−45、RB.5 计数、RB.7 之 2、REC 之 ±20、MS 之 20)。
- **族级解读条款(承统计 M-6)**:本案带之目标覆盖意图 ≈ 85%(主观校准);R 线含约 17 个带判读,**预期 2-3 条散发带外系统计常态**;散发带外(≤3 条且机制互异)禁作系统性异常叙事,聚簇带外(≥4 条同向或同机制)才升级 NEEDS_ATTENTION;判决附录须列全表带内/带外计数,禁摘樱桃。MS 逐种子挂旗系 32 次比较之族,族账注记随 MS_REPORT(若 RB.2 判方差 = 0,则旗无测量噪声假阳,注记随行)。
- **MS 经理敏感度指标(处方①核心交付)**:对同池同工人,MS(s) := |Δ_H(s) − Δ_M29(s)|,其中 Δ_m(s) = ret_leg,m(s) − ret_ref,m(s)(各对**同经理**参照配对);MS_REPORT 并列带符号量 Δ_H−Δ_M29(E1);聚合读数 = max / 中位 / 超线计数;**MS_FLAG_LINE := 20**。F2 灾难在此指标 ~80 分爆表而训练遥测无感,系本指标之存在性证明。**MS 限定(强制随判词)**:M29 与 H 共享 exhausted 盲区与 7017 型选窗盲点(审判庭 V2/P7),MS≈0 不证工人无损——MS 系"经理敏感损伤"探测器,非全损伤探测器。
- **F-lock 复合签名(监控判据,弃用 F×100 指纹)**:种子 s 判"F-lock 候选" iff(i)s ∈ 同池 king×H 参照 depth≥2 种子集;(ii)腿×H 档案该种子 D 窗数 = 0;(iii)该种子 F 窗 τ 中位 ∈ [25, 40] 地板区。**(ii)∧(iii) 合取强制**——健康王座 τ==25 亦达 71.4%(P5 ②),τ 地板单用禁作判据。分诊后件:∧ 判别器 v2 判"轨迹可变" → F-lock 型;判"不变(含近失)" → 工人损伤候选,且携 E5 语义收窄限定。**签名特异性未标定条款(承统计 M-3,强制随一切签名判词)**:条件(i)构造性排除健康对照,案内无已知健康腿可估假阳性率——签名命中之判读携"特异性未标定"限定;**转化条款**:若 RB.4 落"不复现",P8 腿自身即首个健康腿,其签名命中率升格为**假阳性率首实测**,入定标交付。残余⑬登记。
- **8000 池灾难判据验证规格(先登记后开箱;承统计 B-3 重设)**:经理敏感真值 REC(s) := [Δ_H(s) ≤ −20] ∧ [Δ_M29(s) ≥ Δ_H(s) + 20]。**池级注记(承统计 m-6)**:REC 首肢在池级整体下移情形近于恒真(RB.7 点估 −18),判别负担主压 M29 回收肢,判词如实携此注记。CRITERION_VALIDATE 事件 **schema 钉死**:①**完整 2×2 混淆表**(签名命中/未命中 × REC/非REC;分母 = CRITERION_REGISTER 所嵌之 8000 池 king×H depth≥2 名单);②**precision 与 recall 并列**(recall 系首曝定标读数,无先验带,如实登记);③precision 判词**强制携 Clopper-Pearson 95% 精确二项区间**,禁裸点数;④**最小可判命中数 := 3**——命中 < 3 时 precision 判"不可判(样本不足)",不入带判读,循 RB.7 同型降级呈报;命中 = 0 → 判"指纹不复现于留出池",与 RB.5 联判,不构成重烧或加烧理由。
- **统计纪律(硬约束,一切判词强制)**:凡引用均值,**必并列中位 + 符号检验(赢负计数与精确二项 p)+ 去杠杆均值(留一去最大杠杆种子后重算并列名该种子)**——承 F2 头条勘正②(+13.9 由 7014 单种子 +190.7 驱动之教训);组均值胜者诅咒警示随行;事后圈定名单禁作总体效应量;**损伤面按经理格点独立分区**(P3 教训),P8 腿最差名单须 H/M29 双视角各列。**主判复合化见 RB.4(承统计 B-2),禁均值单独裁决。**
- **horizon 敏感性声明(强制,承 P5 ④)**:一切签名/判据/灾难名单条件于评测协议 horizon(max-steps 3000 与 eval_assembled 局长口径);F-lock 本相系亚阈值压制、过零速度对时间预算敏感(sov_7029 距过零仅 8 窗先例),"D 窗 = 0"系 horizon 内陈述;本案内**禁调 horizon**,灾难类判词强制携"horizon 限定"注记;CANARY_EVAL 之 τ 近失距离脚注(D2)为此提供量化脚注;horizon 扫描另案。
- **H 轨默认条款**:R1-W7 契约与 DEFAULT_MANAGER 零触碰;M29 一切读数(含 MS、留出池、金丝雀)**永不进入任何裁决线**;经理换届(菜单④)须另案走锚桥,本案数据仅作其先决材料。
- **不溯及既往条款(写死)**:双经理捆绑、MS 指标、新死门 would-trip 读数、复合签名,一律不重开 v22-v32 历届判决——历届判词、名分、锚值(r2-throne 97.7 / r2-script 94.2 / r2-launch 133.9 前任注记等)全部维持原状;MS 对历史档案之回填计算只作 registry 附录,标注"回填、非判词依据"。
- **交付验收(案结判词三肢,无档位竞赛)**:①处方①转正生效(E1 通道 + 范本义务条款落账,金评划界依 D0-① 亲批结果);②处方②定标数据交付(10 点 calib/双门 would-trip/dry-anchor 双参考/OBS_DRIFT/金丝雀全序列落档,供死门重校正式挂闸案引用);③P8 复现判词(RB.4/RB.5 联判,各向皆合法结论)。**转化条款(措辞按 N=2 限定收口,承统计 M-7)**:RB.4 落"不复现" → F2 全案结论限定升级为"训练种子特异候选",死门重校阈值定标改以 P8 腿实测分布为准——**该定标只能定特异性(不误报健康),不能定灵敏度(能否报灾难),此限定随转化条款写死**;RB.4 落"复现" → 判词为「**F-lock 于第二训练种子上复现(2/2,同配方、同教师、同治下),可复制性证据 1→2**」,强制携残余①⑨⑩,禁作总体级"可复制命题"陈述;审判庭菜单④换届案先决之一满足,呈报总设计师排产。**链式承继(承合规 m-5)**:全案中途 P5 停机时,处方①转正与处方②已落档定标数据随"科学读数终局优先于冻结形式"条款在续案承继。

## D4 P 线(穷尽;v31/v32 承继 + 本案特设)

- **P1 顶层异常/中断** → DRIVER_EXCEPTION + NEEDS_ATTENTION;续跑幂等采信双条件(档案 R-V 式复核全过 ∧ 台账有 FIRING_START/leg_start 且无作废记录),缺台账之档案不得采信(W9)。
- **P2 评测发额度**:每发台账制 2 次(FIRING_START 计数),-b 封闭枚举照 R2 D4-P2 逐字;发车前断言失败一律停机不 -b;未过复核之残档一律 RESIDUE_SEALED 不删不改。
- **P3 腿点火额度(台账制 2 次,硬约束落章)**:leg_start 事件计数;第一发未达标(rc≠0 或 nt≠3,997,696)按运维失败停机呈报,重启后允许第二发(_prepare_run_dir 自动归档残留,resume 源恒为王 zip——重点火系同命题续办,非追加重训);**额度耗尽 → OPERATIONAL_FAILURE 全案停机,禁手改台账续命**;npz 导出失败走补导出通道(不记 leg_start 不烧额度);**种子恒 304000,重点火禁换种子**(换种子即换命题)。
- **P4 预检不过** → PREFLIGHT_FAIL 不发车呈报(退出码 3)。
- **P5 甲案停机**:任一必做发额度耗尽 → 全案停机,已有效之发保留,循 D3 链式承继条款在续案承继。
- **P6 链式重冻结**:多条 FREEZE_SHA(携 prev_sha + REFREEZE_REASON 文件),续跑断言 HEAD == 最后一条。
- **P-canary(本案特设)**:金丝雀评测失败系 OPERATIONAL-canary,只记不停腿;补评仅限首个 L1 FIRING_START 时间戳之前;同 ckpt×manager 落档至多一条即终局,重试仅限无档案运维失败且计数入事件;金丝雀缺数之 R 线判"不可判,如实登记"。
- **P-refdiv(本案特设,承合规 B-1/统计 M-2)**:V1 对 v32-ref-launch 失配之处置见 RB.2 带外条款(漂移分诊 → CASE_HALT 或 REF_DIVERGENCE 停机呈报),退出码 8。
- **退出码表**:0 案结/幂等;2 额度耗尽;3 预检;4 不空闲/锁冲突;5 W5 发车前漂移;6 runtime 案中漂移;7 CASE_HALT_G0(位级承接/零侵入失败);8 REF_DIVERGENCE;其余 P1。

## D5 台账与 R 线

台账 `train/runs/infra-b1/gate_ledger.jsonl`(v31/v32 事件词表承继 + 本案新词:G0_NULLINTRUSION / VARPROBE / REF_DIVERGENCE / BC_SD_WAIVER / HOLDOUT_FIRSTBURN / HOLDOUT_EXPOSURE / CANARY_SET / CANARY_EVAL / MS_REPORT / GATE_WOULD_TRIP / OBS_DRIFT / CRITERION_REGISTER / CRITERION_VALIDATE / CASE_HALT_ENV_DRIFT / PRIOR_REFERENCE;exam_ok 同 tag 至多一条,续跑记 exam_adopted)。判决附录写于文末,commit 即公证。

**R 线(可证伪,带点估与带;测量案主判即此记分卡。区间约定/临线/族级条款见 D3。凡结局依赖线,预登记双分支(承统计 B-1,承 v32"F2 未入先验模型"之最大教益的镜像防重犯):支R := RB.4 落"复现/放大";支N := RB.4 落"不复现";RB.4 落"杠杆驱动候选/均值掩蔽候选"时,条件线判"不可判,如实登记"。)**

- **RB.1 世界位级承接(点,非带;承工程 M-3 重构)**:V1 档案对钉死之 v32-ref-launch 逐种子逐字段 biteq 通过(launch 侧);science 侧以 PRIORS 全文 sha 钉死承接(140.9 不设新发,M29 侧确定性推定入残余⑮)。失败 → 循 RB.2 带外分诊,非科学结论。
- **RB.2 参照种子内方差(V1 方差发)**:b1-varprobe-launch 对 v32-ref-launch 逐种子 |Δret| 最大值,**点 0(位级同一),带 [0, 0]**。**带内** → 登记"种子内测量方差 = 0(确定性协议,限本运行时世界、限 H 侧——M29 侧系推定,注记随判词,实测另案,承合规 m-4):配对 Δ 之显著性评估必须走跨种子符号检验,禁以测量噪声名义作 t 检验"——F2 统计登记之'方差未知'缺口就此闭合。**带外(承合规 B-1/统计 M-2 重设)** → **先行漂移分诊**:W-E0/W5/W6/CASE_RUNTIME 全项复验 + 失配模式分析(F2 已登记 3,445 episode 位级一致,首要嫌疑系世界/运行时漂移而非"方差");分诊任一不绿 → 按 CASE_HALT 处置(退出码 7/6 按项);全绿且失配呈非系统性 → REF_DIVERGENCE 事件入账、**停机呈报总设计师裁量**(退出码 8),专项方差案(≥k 次重复)另立——**禁以 n=2 之 1 自由度读数造全项目噪声带,禁"如实发布即续案"**。RB.4 配对基准建于位级同一假设之上,此线不绿则整张记分卡缓判。
- **RB.3 双经理读数差(8000 池王座参照)**:K2 − K1 均差点 **+25**,带 **[+5, +50]**(7000 池已知 +27.9);并列中位/符号/去杠杆。带外任一侧均系"经理格点差随池大幅移位"之登记级发现。
- **RB.4 F2 劣化复现(主判读数一;复合主判,承统计 B-2,禁事后择优)**:x := p8-full32 对 v32-ref-launch 之 32 种子配对均差,点 **−20**,带 **[−45, −2]**(v32 ctrl 实测 −20.74)。**佐证三肢(预登记)**:S1 中位肢 = 配对 Δ 中位 ≤ −5;S2 符号肢 = 负号种子数 ≥ 21/32(精确二项单侧 p ≈ 0.056);S3 去杠杆肢 = 留一去最大杠杆种子之均值与 x 同区。**判决格(五格,预登记)**:
  - **复现**:x ∈ [−45, −2] ∧ 三肢至少两肢成立;
  - **杠杆驱动候选**:x ∈ [−45, −2] ∧ 佐证不足两肢——判词固定为"均值级复现、种子面不支持",效应量禁引均值,损伤名单逐种子列;
  - **不复现**:x > −2 ∧ 中位 > −5 ∧ 负号 ≤ 20;
  - **均值掩蔽候选**:x > −2 而(中位 ≤ −5 ∨ 负号 ≥ 21)——正向单种子杠杆拉平均值之型,判词并列去杠杆均值;
  - **放大**:x < −45 ∧ 三肢至少两肢成立(不足两肢 → 杠杆驱动候选之负向变体),配方级病根权重上调。
  各格皆有效结论,判词按格改述,禁以"成/败"统称。
- **RB.5 F-lock 指纹复现(主判读数二)**:复合签名在 7000 池 C 集王座 D 种子上的命中数,**以 CANARY_SET 实数 n_D 为准(承统计 m-1)**:点 = round(0.7·n_D),带 = **[⌈0.55·n_D⌉, n_D]**(n_D = 7 时即点 5、带 [4, 7]);≤ ⌈0.55·n_D⌉ − 1 → 指纹不复现(与 RB.4 联判)。**下缘依据(承统计 M-3)**:v32 原案系 7/7 全灭且为签名构造样本,带下缘容许种子面近半衰减仍判复现,低于此复现命题失去种子面多数支持;一切命中判读**强制携"签名特异性未标定"限定**(D3)。分诊后件之 F-lock 型占比随判词。8000 池同签名读数并列(CRITERION_VALIDATE:precision 点 **0.7**,带 **[0.4, 1.0]**,**命中 ≥ 3 方可入带判读**,处置见 D3)。
- **RB.6 P8 腿双经理读数差**:均差线(**无条件**,健康王座 +27.9 与损伤腿 +29.1/+32.9 皆在带,故不分支):p8-full32-m29 − p8-full32 均差点 **+30**,带 **[+8, +55]**。MS 线(**双分支**):支R——max 点 **60** 带 **[20, 130]**(F2 灾难 ~80 爆表),超线(>20)种子数点 **6** 带 **[2, 14]**;支N——max 点 **10** 带 **[0, 30]**,超线点 **1** 带 **[0, 4]**(健康腿两侧 Δ 皆小,MS 应近零)。
- **RB.7 留出池转移**:p8-hold8k 对 b1-ref8k-launch 配对均差点 **−18**,带 **[−50, +5]**(留出池首曝,带宽如实放宽);8000 池 king×H depth≥2 种子数点 **7**,带 **[2, 14]**——若 < 2,签名判据在该池"不可考",CRITERION_VALIDATE 判词降级呈报(转化条款)。
- **RB.8 金丝雀仪表(处方②存在性证明;度量公式钉死,承统计 M-5)**:**保持率 := |{s ∈ D_C : 腿×H 金丝雀(+393,216 点)档案该种子 D 窗数 ≥ 1}| / n_D**,D_C = CANARY_SET 之 depth≥2 侧(王座基线 = 钉死之 v32-ref-launch 档案 depth≥2 事实本身)。**双分支**:支R——点 **0.3** 带 **[0, 0.7]**(预测中期已可见下潜链衰减,补 F2 之 −6% vs −34 遥测盲区);带外上(>0.7)而终考仍塌 → "崩塌发生在末段"登记,金丝雀密度另案加密。支N——点 **0.9** 带 **[0.7, 1.0]**;带外下 → "中期可见衰减而终考回收"登记级发现。
- **RB.9 死门 would-trip(口径:g_pg/g_ce,β 折入,见 E3)**:10 点 calib 上梯度比中位点 **40**,带 **[10, 120]**(F2 实测 20-90× 同口径);梯度比门(>30)would-trip 点数点 **7/10**,带 **[3, 10]**;distill_ce 绝对门(>0.2)超标点占比点 **0.8**,带 **[0.3, 1.0]**(**饱和即预期**,该门系重校目标线非区分器,注记随判词);dry-anchor 增量(零点 0.7515)点 **+1.5pp**,带 **[0, +4pp]**,双参考血统注记(0.6305 谱系起点)随定标数据。全部只记不裁,系重校案定标输入。
- **RB.10 经理观测体态漂移(OBS_DRIFT schema 见 E4)**:王座 D 窗侧(**无条件**,参照测量):walkΣ/121 点 **54**,带 **[40, 65]**。腿侧(**双分支**):支R——点 **79**,带 **[65, 95]**(P5 实测 79 vs 54);支N——点 **55**,带 **[40, 70]**(健康腿应呈王座式体态)。8 追加维逐维 P50 差并列(P1 改判限定随行:仪表维仅翻边际窗,体态维系主嫌)。

**残余不确定性**:① P8 仅一腿一新种子——复制数 1→2,仍非总体估计,"任何续训必崩"类外推继续禁用;② 8000 池组成未知,判据可考性依赖其 depth≥2 种子数(RB.7 转化条款设防);③ 金丝雀粒度 ~98k 步,粒间崩塌不可见,且 C 集复用考种子——防窥靠 record-only 纪律与终点检查点唯一条款,非池隔离;④ 死门阈值(0.2 / 30× / +2pp)系 v32 单腿血统标定,本案产出系定标数据,正式挂闸另案;⑤ MS 对"双经理共享盲区损伤"(exhausted 缺旗、7017 型)结构性失明,MS≈0 禁读作无损;⑥ RB.2 若判方差 = 0,结论限本运行时世界与 H 侧,跨机器/引擎重建不保证(CASE_RUNTIME 设防,破防即运维非科学);⑦ 王座参照仍单工人单检查点,"王座式体态"跨工人泛化未知;⑧ horizon 敏感性(D3 声明承 P5,禁调、强制随判词);⑨ W-G0 已保证窗内一切仪表事件至少各触发一次并以张量级判据裁定,残余缩为短程外推全程之 IO/时钟理论扰动;⑩ P8 与 v32 ctrl 共享 BC_SD/KING_SD/M29 治下——复制打破的仅训练种子维度,教师世代与治下维度仍 N=1;⑪ 双经理捆绑靠范本义务与面板纪律而非契约强制,技术上可被绕开——契约级强制须待换届案升版时并案;⑫ 探针池 7000 多案复用之窥考累积继续加深,8000 池即为此设之对冲,其暴露预算(HOLDOUT_EXPOSURE,按发计)同为不可再生资源,首曝 2 对/4 发单列请示条款设防;⑬ **复合签名特异性未标定**——案内无健康腿对照,假阳性率未知,首个实测条件于 RB.4 落"不复现"(转化条款);⑭ **W-H8 系登记面处女断言**,历史未登记之手工 8000 池暴露不可探测;⑮ **M29 侧评测确定性系推定**(V1 仅 H 侧),推定注记强制随 M29 判词,实测另案补测。

---

*(判决附录:案结后由值守记档,commit 即公证。)*

---
