# PREREG B1, bundled evaluation infrastructure: dual-manager bundling confirmed · dead-gate recalibration instruments · P8 seed-replication leg

Status: **frozen** (E0-E8 implementation + three corrections + W-G0 PASS committed together; G0_NULLINTRUSION all tensors equal, identical digest sha on both sides, evidence that the instruments really ran on record). The commit is the notarization.

**D0 approvals (recorded 2026-07-17)**: all three items were approved on 2026-07-17 -- D0-1 gold-evaluation scope "no new rule" approved; D0-2 first exposure of the 8000 pool at 2 pairs/4 runs + counted per run + at most one pair per case from after this case, approved; D0-3 the 2026-07-17 approval also serves as the launch approval of the P8 leg, confirmed. D0-4 operator check: the approval timestamp was re-checked at transcription; BC_SD_WAIVER is recorded with this status line (basis in W7).

**Numbering note**: this is a **measurement and instrumentation case** (same kind as R2); it takes no v-series campaign number, case id B1 (infrastructure 1); although the P8 leg includes a training launch, it is a vehicle for replication measurement, burns no gold seeds and takes part in no title or release verdict. **The 4×2 content case (manager succession / joint training / observation flags etc.) gets its own pre-registration; this case does not touch it.**

**Honest statement on zero drift (correction, compliance M-1 / engineering M-2)**: this case's E items must change train_ppo.py, so **the full impl bundle sha must differ from v32**; the case's first value is the reconciliation baseline under W5/W10. The "zero drift" this case asserts is **zero drift of the environment and evaluation-protocol bundle**, made machine-executable by asserting that the existing runtime_five (the five eval_contract sha values bridge/engine/game_data/assets/protocol; the protocol bundle covers all python/diablogym environment files and eval protocol files) equals the CASE_RUNTIME values settled in the v32 ledger verbatim (W-E0). The phrase "environment-side sub-bundle" is dropped and does not appear again.

## Background and approval chain (recorded as is)

- **Approval**: the scope proposal for the bundled evaluation infrastructure case was approved on 2026-07-17 (the draft wrote 07-18 by mistake; corrected in the D0-4 re-check) -- scope = **prescription 1** of the manager-inquiry reform menu (confirm dual-manager bundled evaluation + a manager-sensitivity metric) + **prescription 2** (dead-gate recalibration + mid-training canary evaluation + manager observation drift monitoring) + **P8** (rerun one ctrl-recipe leg with a new training seed + pre-register the catastrophe criterion for the 8000-series held-out seed pool). The approval date is recorded as is and **was re-checked once by the operator against the original timestamp before freeze** (lesson of the v32 E4 date erratum).
- **Launch-approval note (compliance m-1)**: the v32 path ruling states for the bundled case that "launch requires recorded approval"; this case reads **the approval above (whose scope explicitly includes P8) as the launch approval of the P8 leg**, and this reading is confirmed separately in the freeze report (D0-3), so there is no later dispute over "approved the case or approved the launch".
- **Upstream of the approval chain**: (1) v32 verdict appendix path ruling 1 -- tier 3 triggered the decoupling clause: "the 4×2 bundled evaluation case becomes eligible to start, with its own pre-registration, a critics panel first, and launch requires recorded approval"; (2) the V5 case filing and the reform-menu ranking in FORENSICS-manager-inquiry (1 and 2 are the top two of the menu; "P8 is the prerequisite for every final-level attribution"); (3) the design-input section of FORENSICS-F2 + the prerequisite items registered in the P5/P7 appendices ("P8 and the horizon-sensitivity statement go into the bundled case's pre-registration together").
- **Nature of the case (recorded here)**: this is a **measurement and instrumentation case with zero environment-side changes** -- `OptionsEnv`/`WorkerWindowEnv`/masks/wages/contract fields are not touched (W-E0 assertion, see the honest statement in the numbering note). All implementation is limited to training-driver knobs / evaluation channels / read-only training-side instruments / offline analysis files.
- **Required evidence base**: FORENSICS-F2 (text + appendices P1-P9), FORENSICS-manager-inquiry (V1-V5), the PREREG-v32 verdict appendix (F1/F2/F3; this case takes up F2 directly).

## D0 separate requests (freeze prerequisites; three approval items + one operator check)

1. **Gold-evaluation scope (constitutional level, compliance B-3)**: the prescription 1 confirmation obligation is **explicitly limited to worker exams on the probe pool (7000 series) and the held-out pool (8000 series)**; whether gold-seed pool (9000-9031) exams bundle an M29 reading touches double gold-pool exposure and the "one burn per case" title rule, which is constitutional, so **this case makes no such rule** -- approval of "no new rule" itself was requested (and granted); gold evaluation keeps the current single-H discipline, and bundling it is left to the menu item 4 succession case.
2. **First exposure of the 8000 pool and the exposure rule (compliance M-3 / statistics m-8)**: this case's first exposure is **2 pairs / 4 runs** in total (the K1/K2 reference pair is structurally needed for criterion validation + the L4/L5 leg-exam pair); HOLDOUT_EXPOSURE accounting = **per run**, all 4 runs counted; the rule "at most one bundled pair per later case" **applies from after this case closes**. Approval of the first-exposure budget and the rule was requested (and granted).
3. **Launch approval confirmation**: confirm that the 2026-07-17 approval is the launch approval of the P8 leg (see background).
4. **Operator check (not an approval item)**: re-check of the approval date and timestamp; the discretionary waiver of not regenerating BC_SD (D1-W7) listed separately in the freeze report.

## One campaign, one prescription

Prescription = "**give the evaluation loop a second pair of eyes and an ECG, then ask again: is retraining regression (F2) specific to training seed 303000?**". Three limbs:

1. **Confirm dual-manager bundled evaluation**: from this case on, every worker exam on the probe pool / held-out pool runs H+M29 as a pair, with a new manager-sensitivity metric MS; **the H track keeps the decision default (the R1-W7 contract and the DEFAULT_MANAGER field untouched); the M29 track is an additional scientific reading and never a decision line**.
2. **Dead-gate recalibration instruments + canaries + drift monitoring**: calib/sentinel/dry-anchor all sampled more densely, with dual would-trip readings against pre-registered thresholds, **record only within this case**; mid-training canary evaluation uses the offline checkpoint channel with a pre-registered seed set against peeking; manager observation drift is archived with the canaries.
3. **P8 replication leg**: the v30/v32 ctrl recipe **verbatim**, only the training seed changes; examined on the 7000 probe pool (dual manager) and the 8000 held-out pool (dual manager); the catastrophe criterion is registered first and validated on the 8000 pool afterwards.

Zero gold seeds (9000-9031 untouched throughout), zero title changes, zero releases, zero environment intrusion. **The effective-replication proposition**: all F2 conclusions currently rest on N≈1 (1 checkpoint × 1 recipe × 1 training seed); this case breaks the training-seed dimension to N=2 and leaves standing instruments for later cases. A result in either direction counts as a win (verdict wording in the D3 conversion clause, already limited to N=2).

## E implementation list (instruments and evaluation channels; zero environment-side intrusion; committed with the freeze commit)

- **E0 zero-intrusion general rule**: no E item may touch the training RNG stream, gradients, env stream, mask semantics or contract fields. Every new training-side knob must pass the W-G0 zero-intrusion live run (see D1) before freeze. **Closed enumeration of this case's training-side knobs (three in total, engineering M-1/M-5)**: `--ckpt-every-steps` (exposes every_steps of the existing AtomicRolloutCheckpointCallback, currently hard-coded to 250,000 at the call site), `--sentinel-every`, `--dry-anchor-every` (both callbacks currently use the default 500,000; both are pure reads + IO, and dry-anchor uses its own rng(26) and does not touch the training RNG). `--calib-probes` is an existing CLI flag (comma-separated integer list), no implementation needed. **No new checkpoint export path is written** -- the earlier plan to add `--canary-export-steps` is dropped: the existing callback already provides atomic saves at rollout boundaries, quantum alignment and in-case impl-sha drift assertions, and reinventing it would bring back the "steps recorded, gradient not applied" boundary bug.
- **E1 dual-manager bundled evaluation channel (prescription 1)**: at driver level -- every worker exam is automatically paired (same worker, same seed pool, `--manager-npz` set to H and M29 respectively, following the v30 panel discipline "explicit every time, no default fallback"); the exam_ok event must also carry the paired tag; a new MS_REPORT event (per-seed MS vector **+ the signed Δ_H−Δ_M29 listed alongside** (statistics m-7, so an "M29 side worse" flip is not swallowed by the absolute value; class B of the P3 partition table is the reverse precedent) + max/median/over-line count, constants in D3). **Confirmation clause**: once this case's verdict appendix is recorded, dual-manager bundling becomes a PREREG template obligation (a later probe-pool/held-out-pool worker exam pre-registration without a bundling clause should be blocked by the panel as a blocker); **the obligation explicitly excludes gold evaluation (D0-1)**. **Closed enumeration of exemptions (compliance M-4 / engineering m-4)**: (1) operational quick-screen / sanity runs (L1 type, half pool, H only); (2) zero-score runs such as make-up exports / parity re-checks; (3) offline canary readings (already dual-manager) -- adding a class requires panel review. The contract fields are not upgraded -- the discipline rests on the template and the panel, not on runtime enforcement; the weakness is registered as residual 11. The eval protocol files are not hand-edited.
- **E2 canary channel (prescription 2, redesigned per engineering B-1/M-1)**: `--ckpt-every-steps 98304` (=48×2048) makes the existing callback atomically write a ckpt zip every +98,304k steps within the leg; npz files are exported offline per ckpt by **a separate out-of-case process** with the existing `export_worker_npz.py` (built-in parity self-check, precedent: the v32 make-up export channel); canary evaluation uses eval_assembled (numpy forward pass, the same forward path as the final exam; **never feed ckpt zips directly through a torch forward pass; the definitions differ**). **Canary evaluation seeds = the whole 7000-7031 pool** (eval_assembled `--seeds` only accepts a contiguous range and the eval protocol is not hand-edited; the C set is an offline-extracted **scoring seed set** that constrains only RB.5/RB.8, not the exam surface; measured cost extrapolates to ~70-80 s per run). **Canary point table fixed**: four mid-leg points +98,304k (k=1..4, i.e. 3,596,288 / 3,694,592 / 3,792,896 / 3,891,200) + the end npz (nt 3,997,696); the +491,520 (3,989,504) ckpt produced mechanically **is archived as usual but not part of the canary sequence** (the point table is closed by pre-registration, preventing hindsight picking of an extra point). CANARY_EVAL events are archived. **Three anti-peeking rules (fixed)**: (1) the scoring seed set C is frozen by a CANARY_SET event before the leg launch; (2) every canary reading is record-only and never a basis for any in-leg intervention (no early stopping, no tuning, no checkpoint reselection -- the leg's only end checkpoint = nt 3,997,696); (3) the 8000 held-out pool and the canary channel are disjoint by construction (C ⊂ 7000 pool). **Canary finality (compliance M-5)**: at most one CANARY_EVAL archive per ckpt×manager combination, and it is final; retries only for "operational failure without an archive", with the retry count recorded in an OPERATIONAL-canary event; **make-up deadline = before the timestamp of the first L1 FIRING_START event** (statistics m-3), with no make-up afterwards, preventing hindsight.
- **E3 dead-gate instruments (prescription 2)**: (1) **10 calib probe points**, passed as a verbatim step table through the existing `--calib-probes` (one point every +49,152 steps within the leg, k=1..10; global step table in D2; quantum aligned), with `--calib-record-only` throughout (under leashed_ppo record_only the tripped flag is never armed; verified); (2) dual would-trip readings of the new gates go with each point in a GATE_WOULD_TRIP event: the **absolute gate** distill_ce > 0.2 (F2 lineage baseline target; **saturation note**: v32-ctrl measured 0.974/1.010 at two points, so all 10 points will very likely exceed it -- this gate is a recalibration target, not a discriminator, and the verdict must not narrate it as an anomaly, engineering m-2), the **gradient-ratio gate** g_pg/g_ce > 30, **definition pinned**: ratio := g_pg/g_ce from calib.jsonl, with g_ce = ‖∇(β·distill_ce)‖ (**β=0.015625 folded in**; v32-ctrl measured 44×/52×, the same definition as F2's "20-90×"; reading it as the raw literal CE gradient is off by 64× and not allowed, engineering m-1); (3) sentinel densified to ≥10 points with `--sentinel-every 49152`, keeping the same old-mask yardstick as v28-v30 (v32 residual inherited); (4) dry-anchor densified with `--dry-anchor-every 49152`, **both references pinned (statistics M-4 / engineering m-3 merged)**: the throne-level absolute reference **0.7515** (the throne's entry value; the first v32-ctrl leg point measured the same value) / the lineage reference **0.6305** (v28-leg1 start, recorded in F2-P9 -- the +12pp lineage fact that drift predates v32 is recorded with the calibration data, so later cases do not gate on a misleading line); **zero point of the increment gate = 0.7515** (would-trip line: increment > +2pp; the first run after the callback crosses the boundary, first point about step 3,538,944, not the entry step, recorded as is). **Every gate above is record-only within this case** -- the thresholds are calibration inputs; formal gating is a separate case.
- **E4 manager observation drift monitoring (prescription 2)**: the vehicle is named as the **probe_f2_replay.py pattern** (evaluation archives do not store observations, so the replay probe is the only channel; zero RNG consumption; fidelity self-checked by reconciling archive rows field by field; **the fidelity anchor order is fixed: write the canary evaluation archive first, then replay and reconcile**, engineering m-8). OBS_DRIFT event **schema pinned (statistics M-5)**: the 8 appended dims, each with {mean, std, min, max, P5/P50/P95} + the window-end walkable shape {window-end mean of walkSum/121, south-west band mean} (local-map indices 44-164); **drift reading := per-dim P50 difference, leg side minus throne side**; the set of statistics is closed, with no rewording on the reading day. Per the P1 revised ruling and the P5 correction, the F-lock mechanism lives in the base 295-dim shape, not in the 8 instrument dims, so monitoring covers both. The offline analysis file (a named new file) goes with the verdict appendix.
- **E5 discriminator toolbox v2**: manager_invariant_registry gains the **near-miss criterion** (P4: correction of the missed 7017 type, "same score, variable trajectory"); the composite-signature discriminator script (a named new file, constants in D3) is committed with the freeze; **discriminator semantic-narrowing clause (from P7 ruling 3)**: "manager invariant" only shows "swapping the examiner does not help", not "the loss is unrecoverable"; every verdict that cites it must carry this caveat.
- **E6 launcher hardening written down (after the v32 OPS incident; a named deliverable, engineering m-6)**: deliverables = `train/launch_case.sh` + a launchd plist template (orphaned, hosted directly, PPID=1) + caffeinate assertion + operator procedure for the progress.jsonl heartbeat mtime check (thresholds written down). The repo currently has no committed launcher (v32 was launched by hand); this case promotes the incident patch to a standard driver clause, committed with the freeze commit.
- **E7 test updates**: unit tests for the three knobs (step-set behaviour of ckpt-every/sentinel-every/dry-anchor-every) + MS computation + signature discriminator + calib step-table parsing; **contract_revision untouched** (no environment semantic change; 4 does not become 5).
- **E8 auxiliary script committed (compliance m-8)**: the W-C machine extraction script (depth≥2 extraction per the depth2_count precedent) is committed with the freeze commit -- the extraction result has a CANARY_SET event, and without the extractor in the repo "the archive rows decide" could not be re-verified.

## D1 world identity and W gates

W1 freeze notarization / W2 version / W3 engine REF / W4 diabdat / W5 impl stage sampling / W6 runtime / W8 idle + driver mutual exclusion / W9 first-launch prerequisites / W10 resume reconciliation / W11 identity restated per run / CASE_RUNTIME five case-level sha reconciliation -- all inherited verbatim from the v31/v32 drivers. Specific to this case:

- **W-E0 zero environment drift assertion (redesigned per engineering M-2)**: preflight asserts that this case's runtime_five (the five sha values bridge/engine/game_data/assets/protocol) **equals the CASE_RUNTIME values settled in the v32 ledger (pinned as frozen driver constants) verbatim** -- the meaning is exactly "zero drift of environment + evaluation protocol", with no new machinery; unequal → no freeze, no launch, CASE_HALT_ENV_DRIFT reported. The full impl sha must change because of the E items; the case's first value is the reconciliation baseline under W5/W10.
- **W7 artifact pins** (full-file sha per the frozen driver constants; a mismatch means P4, no launch): king zip/npz (v28-leg1, lineage `976b6c05…f27f8a`), H npz (== DEFAULT_MANAGER_SHA256), M29 npz (`89441388…7b66d6`), KING_SD (the v32 artifact reused + in-case check_teacher_parity 0/1000 re-check). **Explicit discretionary clause for reusing the v32 regenerated BC_SD (compliance M-2)**: the v32 precedent was "full impl bundle changed → BC receipt invalid → regenerate"; this case's full-bundle sha must also change, and the **written basis** for waiving regeneration = the BC generation chain does not pass through the training-driver knobs, and runtime_five has zero drift (W-E0); this waiver is not a default inheritance but a case-level discretion, recorded as a BC_SD_WAIVER event and listed separately in the freeze report (D0-4). The sha is frozen as a case constant at the value settled by v32 BC_REGEN. **PRIORS archive pins**: full-file sha constant assertions for the four archives v32-ref-launch (113.0) / v32-ref-science (140.9) / v32 ctrl-full32 (92.3) / ctrl-m29 (125.2), with a PRIOR_REFERENCE event in the ledger (fingerprint baseline, sealed before use).
- **W-G0 zero intrusion (freeze prerequisite for the E-item knobs, live run; redesigned per compliance B-2 / engineering M-4 / statistics m-4)**: **smoke seed 307000** (correction: the originally pinned 305000 had already been used by v30 bc-leg1 -- the freshness assertion set up by this very clause blocked it on the first run, with a PREFLIGHT_FAIL event on record; 307000 was confirmed virgin by a leg_start scan of all ledgers and a check of every evaluation pool. Not a case-leg seed; 304000 is avoided so there is no back door to "peek at the leg's early telemetry before launch"); a short same-recipe comparison of **2 × 102,400 steps** (50 rollouts, divisible by 2048): all knobs on vs the bare ctrl recipe; **the smoke step table is fixed in the case so that ≥1 calib point, ≥1 ckpt save and ≥1 sentinel and dry-anchor firing really happen in the window** (the same parsing code; "instruments proven zero-intrusion" must not actually mean "instruments never ran"). **The equality criterion is tensor level**: policy state_dict + optimizer state, torch.equal per tensor; telemetry comparison is limited to RNG-related fields (step/loss/ret etc.) field by field -- file byte-level comparison is dropped (SB3 zip member timestamps and pickle serialization are not reproducible, so a file comparison would always give false positives). **Beyond the bit-level criterion above, smoke telemetry must not feed any go/no-go or rework decision**. Unequal → no freeze, no launch, instruments go back for redesign (G0_NULLINTRUSION event, with tensor digest sha for both sides). Limitation: the IO/clock theory of extrapolating the short run to the full run is a residual, registered as residual 9. Smoke cost ~2×9 minutes (scaled from the v32 leg's 45.3 min per 499,712 steps).
- **W-H8 held-out pool virginity assertion**: preflight scans the eval-assembled directory (meta.seeds), all board files and earlier case ledgers, and asserts zero occurrences of 8000-8031; a hit → halt for review, never a silent downgrade. **Evidence grade noted as is (statistics m-5)**: this is a **registered-side virginity** assertion; unregistered historical manual exposure is undetectable, registered as residual 14. **The 8000-pool rule is made here**: the pool is reserved for catastrophe-criterion validation; this case is its first exposure (HOLDOUT_FIRSTBURN event, **2 pairs / 4 runs in this case**, the D0-2 separate request); HOLDOUT_EXPOSURE accumulates **per run**; "at most one bundled pair per later case" applies from after this case closes; the 8000 pool is not the gold pool and carries no title meaning, but its exposure budget does not regenerate either. **The driver preflight also asserts that the training seed ∉ 8000-8031** (the built-in range guard in train_ppo only covers 7000/9000; engineering m-5; train_ppo semantics unchanged, added on the driver side).
- **W-C canary scoring set frozen**: C := every depth≥2 seed in the v32-ref-launch archive (machine-extracted by the E8 script; 7 expected; the archive rows decide, with the actual number recorded as n_D) ∪ healthy controls {7003, 7011} (P5 spike-free seeds, negative controls). Extracted at preflight and fixed by a CANARY_SET event (with n_D and the per-seed list); no such event before the leg launch → no launch. **The proportional clause by which the RB.5/RB.8 points and bands follow n_D is in D5 (statistics m-1).**

## D2 recipes and exam sequence

**Preflight sequence (strictly serial; restructured per engineering M-3 -- the separate "REF_BITEQ continuity step" is dropped, and launch-side bit-level continuity is covered by the V1 variance run doing double duty)**:

instrument implementation commit (all of E1-E8) → all W lines pass (W1/W7/PRIOR_REFERENCE/W-E0/W-H8/304000 and 307000 ledger scans) → W-G0 zero-intrusion live run → FREEZE_SHA/CASE_RUNTIME → **V1 variance run** (also RB.1 bit-level continuity: biteq per seed per field against the pinned v32-ref-launch; a mismatch is handled by the drift triage of the RB.2 out-of-band clause, never downgraded as "the new reading is also plausible") → **K1/K2 two held-out reference runs** → CANARY_SET → **CRITERION_REGISTER** (instantiating the 8000-pool depth≥2 seed list extracted from K1, statistics m-2 -- "register first, open later" also holds for the list itself; the event is a restating record of the D3 criterion, not an opening for a new in-case criterion, compliance m-7) → P8 leg launch → offline canary sequence (all before L1) → five leg exams L1-L5 → CRITERION_VALIDATE → R-line scorecard → verdict appendix.

**Science-side (140.9) continuity**: downgraded to the PRIORS full-file sha pin (already in W7), with no new run; **M29-side evaluation determinism is presumed**; the presumption note goes with every M29 verdict, and a measurement is left to a separate case (residual 15; aligned with the RB.2 conversion clause, compliance m-4 / statistics M-2).

**Evaluation run list (budget per run = 2 under the ledger; the -b closed enumeration follows R2 D4-P2 verbatim; command form = eval_assembled verbatim, --manager-npz explicit every time)**:

| Run | tag | worker | manager | Seed pool | Purpose |
|---|---|---|---|---|---|
| V1 variance run | `b1-varprobe-launch` | king npz | H | 7000-7031 | within-seed variance of the throne reference, also RB.1 bit-level continuity (**the authorization for one independent re-evaluation is recorded here**: re-run king×H in a fresh process and compare per seed per field with v32-ref-launch; record only, no change of anchor or reference title; **V1 never replaces the paired baseline; the final status of the original v32-ref-launch archive and its RB.4 baseline role are unchanged**, compliance m-2) |
| K1 held-out reference | `b1-ref8k-launch` | king npz | H | 8000-8031 | 8000-pool throne H reference (first exposure) |
| K2 held-out reference | `b1-ref8k-science` | king npz | M29 | 8000-8031 | 8000-pool throne M29 reference |
| L1 quick screen | `p8-s16` | P8 npz | H | 7000-7015 | operational sanity reading (**E1 exemption 1 applies; H only is compliant**) |
| L2 main exam | `p8-full32` | P8 npz | H | 7000-7031 | main F2 reproduction reading |
| L3 bundled | `p8-full32-m29` | P8 npz | M29 | 7000-7031 | MS metric and manager grid |
| L4 held-out | `p8-hold8k` | P8 npz | H | 8000-8031 | criterion validation, H side |
| L5 held-out | `p8-hold8k-m29` | P8 npz | M29 | 8000-8031 | criterion validation, M29 side (manager-sensitive ground truth) |

**P8 leg (the only training launch)**: the v30/v32 ctrl recipe **verbatim** -- `--worker --algo mppo --gamma 1.0 --max-steps 3000 --n-steps 512 --num-envs 4 --lr 3e-4 --ent-coef 0.005 --total-steps 499712 --distill-beta 0.015625 --teacher-sd <BC_SD> --teacher-override <KING_SD> --skip-dry --manager-npz <M29 npz> --resume-from <king zip> --allow-legacy-resume --no-drink-sovereignty --calib-record-only`, with **five enumerated deviations**, each recorded verbatim:

(1) `--seed 304000` (**seed selection rule fixed**: the original lineage seed 303000 + 1000, a constant; no seed search, reselection or "swap it if the run goes badly" in any form; preflight asserts that 304000 appears in no leg_start of any earlier ledger, and a hit → halt for review rather than shifting to the next value);
(2) `--calib-probes 3547136,3596288,3645440,3694592,3743744,3792896,3842048,3891200,3940352,3989504` (k=1..10, +49,152k within the leg, quantum aligned);
(3) `--ckpt-every-steps 98304`; (4) `--sentinel-every 49152`; (5) `--dry-anchor-every 49152`.
(The original `--canary-export-steps` is dropped, see E2.) All five instrument knobs must pass W-G0 before freeze.

nt gate: nt_zip == **3,997,696**; timeout 6h; export with export_worker_npz (built-in parity). **The launcher follows the E6 hardening clause.**

**Canary evaluation (offline checkpoints, record only)**: the four mid-leg ckpts (+98,304k, k=1..4) are each exported with export_worker_npz → one eval_assembled run on **the whole 7000-7031 pool** × {H, M29} each (8 runs in total) + the end npz is the leg exam itself; CANARY_EVAL events carry {ckpt global step, manager, per-seed ret/died/depth/D-window count/τ distribution, **a footnote with the near-miss distance of the τ median from the 25 floor** (statistics m-9, record only; P5-style replay forensics explicitly out of scope), OBS_DRIFT summary}. Canary runs do not use the evaluation run budget; the finality and make-up deadline clauses are in E2.

**Exam-run finality clause (v32 verbatim)**: once an eval archive is written and exam_ok is recorded (with the archive sha16), it is frozen at case level; resumption adopts it after re-checking the expected identity + ledger sha (exam_or_adopt); no .void re-runs; a leftover archive without a ledger entry, or a ledger entry without an archive, halts for review.

**This case has no abandon gate (a measurement case; recorded here)**: P8 is a replication measurement, and every score is a reading -- the lesson of v32 is precisely that the default "leg ≈ reproduction" was falsified, so a low score may not void the leg. There are only three operational gates: rc=0 ∧ nt=3,997,696 ∧ parity; a failure means a relaunch within the P-line budget.

**Compute budget (engineering m-7, measured extrapolation)**: leg ~45 min, smoke tests 2×~9 min, 8 evaluation runs ~12 min, 8 canary runs + 4 exports ~15 min; the whole case on one machine in one day ≈ 1.5-2.5 hours, a 6h timeout is very generous, no budget risk.

**Driver**: `train/run_b1_infra.py`, cloned from the run_v32_sovereign.py skeleton (the constant block inherits every v32 W7 constant + the four PRIORS sha values + the five v32 CASE_RUNTIME sha values + SEED=304000 + step-table constants; the utilities log/sha/require/runtime_five/exam_or_adopt/biteq/by_seed/depth2_count etc. inherited verbatim; stages S0 preflight → S1 smoke → S2 V1 → S3 K1/K2 → S4 CANARY_SET+CRITERION_REGISTER → S5 leg → S6 canaries → S7 leg exams → S8 criterion validation → S9 scorecard; the no-hindsight clause is enforced by stage order).

## D3 criteria, constants and verdict clauses (all pre-registered, frozen for the case)

- **Interval convention and borderline clause (uniform for the case, statistics M-1)**: every band is a **closed interval** [lo, hi], in band := lo ≤ x ≤ hi; RB.4's "above/below the band" means x > hi / x < lo (the old text with contradictory endpoint ownership is dropped). **Borderline** := for continuous quantities |x − boundary| ≤ 0.05×bandwidth, for counts exactly on the boundary or within 1 of it -- a borderline verdict **must carry the note "borderline + line not recalibrated"** (from v32 D3), for every hard boundary (RB.4's −2/−45, RB.5 counts, RB.7's 2, REC's ±20, MS's 20).
- **Family-level reading clause (statistics M-6)**: the intended coverage of this case's bands is ≈ 85% (subjective calibration); the R lines contain about 17 band readings, so **2-3 scattered out-of-band lines are statistically normal**; scattered out-of-band lines (≤3, with different mechanisms) must not be narrated as a systematic anomaly, and only clustered out-of-band lines (≥4 in the same direction or with the same mechanism) escalate to NEEDS_ATTENTION; the verdict appendix must list the in-band/out-of-band counts of the whole table, no cherry-picking. The per-seed MS flags form a family of 32 comparisons, with a family accounting note in MS_REPORT (if RB.2 finds variance = 0, the flags carry no measurement-noise false positives; the note applies).
- **MS manager-sensitivity metric (core deliverable of prescription 1)**: for the same pool and worker, MS(s) := |Δ_H(s) − Δ_M29(s)|, where Δ_m(s) = ret_leg,m(s) − ret_ref,m(s) (each paired with the **same-manager** reference); MS_REPORT lists the signed Δ_H−Δ_M29 alongside (E1); aggregate readings = max / median / over-line count; **MS_FLAG_LINE := 20**. The F2 catastrophe went off the scale at ~80 on this metric while training telemetry noticed nothing, which proves the metric's worth. **MS caveat (mandatory with the verdict)**: M29 and H share the exhausted blind spot and the 7017-type window-choice blind spot (manager inquiry V2/P7), so MS≈0 does not show the worker is undamaged -- MS is a detector of "manager-sensitive damage", not of all damage.
- **F-lock composite signature (monitoring criterion; the F×100 fingerprint is dropped)**: seed s is an "F-lock candidate" iff (i) s is in the depth≥2 seed set of the same-pool king×H reference; (ii) the leg×H archive has 0 D windows for that seed; (iii) the seed's median F-window τ is in the floor band [25, 40]. **(ii)∧(iii) are both required** -- a healthy throne also reaches τ==25 in 71.4% (P5 (2)), so the τ floor alone is not a criterion. Post-hit triage: ∧ discriminator v2 says "trajectory variable" → F-lock type; "invariant (incl. near miss)" → worker-damage candidate, carrying the E5 semantic-narrowing caveat. **Uncalibrated-specificity clause (statistics M-3, mandatory with every signature verdict)**: condition (i) excludes healthy controls by construction, and the case has no known healthy leg to estimate the false-positive rate -- every signature-hit reading carries the "specificity uncalibrated" caveat; **conversion clause**: if RB.4 lands on "not reproduced", the P8 leg itself is the first healthy leg, and its signature hit rate becomes **the first measured false-positive rate**, delivered with the calibration. Registered as residual 13.
- **Specification of the 8000-pool catastrophe-criterion validation (register first, open later; redesigned per statistics B-3)**: manager-sensitive ground truth REC(s) := [Δ_H(s) ≤ −20] ∧ [Δ_M29(s) ≥ Δ_H(s) + 20]. **Pool-level note (statistics m-6)**: when the whole pool shifts down, the first REC limb is nearly always true (RB.7 point estimate −18), so the discriminating burden falls mainly on the M29 recovery limb; the verdict carries this note as is. The CRITERION_VALIDATE event **schema is pinned**: (1) **the full 2×2 confusion table** (signature hit/miss × REC/non-REC; denominator = the 8000-pool king×H depth≥2 list embedded in CRITERION_REGISTER); (2) **precision and recall listed together** (recall is a first-exposure calibration reading with no prior band, recorded as is); (3) the precision verdict **must carry the Clopper-Pearson 95% exact binomial interval**, no bare point estimates; (4) **minimum judgeable hits := 3** -- with fewer than 3 hits, precision is "undecidable (too few samples)", not read against the band, and downgraded and reported like RB.7; 0 hits → "fingerprint not reproduced in the held-out pool", judged together with RB.5, not a reason to rerun or add runs.
- **Statistical discipline (hard constraint, mandatory for every verdict)**: every cited mean **must come with the median + a sign test (win/loss counts and exact binomial p) + a deleveraged mean (recomputed without the highest-leverage seed, naming that seed)** -- from F2 headline correction 2 (the lesson that +13.9 was driven by the single seed 7014 at +190.7); the winner's-curse warning on group means applies; lists drawn after the fact may not be used as population effect sizes; **the damage surface is partitioned independently per manager grid point** (P3 lesson), and the P8 leg's worst list must be given from both the H and M29 views. **The main verdict is composite, see RB.4 (statistics B-2); the mean alone never decides.**
- **Horizon-sensitivity statement (mandatory, from P5 (4))**: every signature/criterion/catastrophe-list condition holds under the evaluation-protocol horizon (max-steps 3000 and the eval_assembled game-length definition); F-lock is at heart a sub-threshold suppression, and its zero-crossing speed is sensitive to the time budget (precedent: sov_7029 was only 8 windows from crossing zero), so "D windows = 0" is a statement within the horizon; **the horizon may not change within this case**, and catastrophe verdicts must carry the "horizon caveat" note; the τ near-miss footnote of CANARY_EVAL (D2) gives it a quantitative footnote; a horizon sweep is a separate case.
- **H-track default clause**: the R1-W7 contract and DEFAULT_MANAGER untouched; no M29 reading (incl. MS, held-out pool, canaries) **ever enters any decision line**; manager succession (menu item 4) must go through the anchor bridge in a separate case, and this case's data are only prerequisite material for it.
- **Non-retroactivity clause (fixed)**: dual-manager bundling, the MS metric, the new dead-gate would-trip readings and the composite signature reopen none of the v22-v32 verdicts -- all earlier verdicts, titles and anchor values (r2-throne 97.7 / r2-script 94.2 / r2-launch 133.9 predecessor note etc.) stay as they are; MS values back-filled for historical archives are only a registry appendix, labelled "back-filled, not a basis for any verdict".
- **Acceptance (three limbs of the closing verdict, no tier competition)**: (1) prescription 1 confirmation takes effect (E1 channel + template-obligation clause recorded; gold-evaluation scope per the D0-1 approval); (2) prescription 2 calibration data delivered (10 calib points / two-gate would-trip / dry-anchor dual reference / OBS_DRIFT / full canary sequence archived, for the formal dead-gate gating case to cite); (3) P8 reproduction verdict (RB.4/RB.5 joint verdict; every direction is a legal conclusion). **Conversion clause (wording limited to N=2, statistics M-7)**: RB.4 lands on "not reproduced" → the F2 case conclusion is narrowed to "training-seed-specific candidate", and dead-gate recalibration thresholds use the measured P8 leg distribution -- **that calibration can only set specificity (no false alarms on healthy legs), not sensitivity (whether catastrophes are caught), and this caveat is fixed with the conversion clause**; RB.4 lands on "reproduced" → the verdict reads "**F-lock reproduced on a second training seed (2/2, same recipe, same teacher, same manager), replication evidence 1→2**", must carry residuals 1, 9 and 10, and no population-level "replicable proposition" may be stated; one prerequisite of option 4 (succession case) on the manager-inquiry menu is met; reported for scheduling. **Chained inheritance (compliance m-5)**: if the whole case halts midway under P5, the prescription 1 confirmation and the calibration data already archived under prescription 2 carry over to a follow-up case under the clause "final scientific readings take precedence over the frozen form".

## D4 P lines (exhaustive; inherited from v31/v32 + specific to this case)

- **P1 top-level exception/interruption** → DRIVER_EXCEPTION + NEEDS_ATTENTION; two conditions for idempotent adoption on resume (the archive passes all R-V-style re-checks ∧ the ledger has FIRING_START/leg_start and no void record); an archive without a ledger entry may not be adopted (W9).
- **P2 evaluation run budget**: 2 per run under the ledger (FIRING_START count); the -b closed enumeration follows R2 D4-P2 verbatim; a failed pre-launch assertion always halts, no -b; leftover archives that fail re-checks are always RESIDUE_SEALED, never deleted or edited.
- **P3 leg launch budget (2 under the ledger, a hard constraint)**: counted by leg_start events; if the first launch falls short (rc≠0 or nt≠3,997,696), it is an operational failure, halt for review, and after a restart a second launch is allowed (_prepare_run_dir archives leftovers automatically; the resume source is always the king zip -- a relaunch continues the same proposition, not additional retraining); **budget exhausted → OPERATIONAL_FAILURE, whole case halts; never hand-edit the ledger to keep it alive**; a failed npz export uses the make-up export channel (no leg_start, no budget used); **the seed is always 304000, and a relaunch may not change it** (changing the seed changes the proposition).
- **P4 preflight failed** → PREFLIGHT_FAIL, no launch, report for review (exit code 3).
- **P5 plan A halt**: budget for any mandatory run exhausted → whole case halts; valid runs are kept and carried into a follow-up case under the D3 chained-inheritance clause.
- **P6 chained re-freeze**: multiple FREEZE_SHA events (with prev_sha + a REFREEZE_REASON file); resumption asserts HEAD == the last one.
- **P-canary (specific to this case)**: a failed canary evaluation is OPERATIONAL-canary, recorded only, and the leg continues; make-ups only before the timestamp of the first L1 FIRING_START; at most one archive per ckpt×manager, and it is final; retries only for operational failures without an archive, with the count recorded in the event; an R line missing canary data reads "undecidable, recorded as is".
- **P-refdiv (specific to this case, compliance B-1 / statistics M-2)**: handling of a V1 mismatch against v32-ref-launch is in the RB.2 out-of-band clause (drift triage → CASE_HALT or REF_DIVERGENCE halt for review), exit code 8.
- **Exit code table**: 0 case closed/idempotent; 2 budget exhausted; 3 preflight; 4 not idle/lock conflict; 5 W5 pre-launch drift; 6 runtime drift during the case; 7 CASE_HALT_G0 (bit-level continuity / zero-intrusion failure); 8 REF_DIVERGENCE; anything else P1.

## D5 ledger and R lines

Ledger `train/runs/infra-b1/gate_ledger.jsonl` (not published; v31/v32 event vocabulary inherited + new words in this case: G0_NULLINTRUSION / VARPROBE / REF_DIVERGENCE / BC_SD_WAIVER / HOLDOUT_FIRSTBURN / HOLDOUT_EXPOSURE / CANARY_SET / CANARY_EVAL / MS_REPORT / GATE_WOULD_TRIP / OBS_DRIFT / CRITERION_REGISTER / CRITERION_VALIDATE / CASE_HALT_ENV_DRIFT / PRIOR_REFERENCE; at most one exam_ok per tag, resumption records exam_adopted). The verdict appendix is written at the end of this document; the commit is the notarization.

**R lines (falsifiable, with point estimates and bands; for a measurement case this scorecard is the main verdict. Interval / borderline / family-level clauses in D3. Every outcome-dependent line pre-registers two branches (statistics B-1; mirroring the biggest lesson of v32, "F2 was not in the prior model", so it is not repeated): branch R := RB.4 lands on "reproduced/amplified"; branch N := RB.4 lands on "not reproduced"; if RB.4 lands on "leverage-driven candidate/mean-masking candidate", the conditional lines read "undecidable, recorded as is".)**

- **RB.1 bit-level world continuity (a point, not a band; restructured per engineering M-3)**: the V1 archive passes biteq per seed per field against the pinned v32-ref-launch (launch side); the science side is carried over by the PRIORS full-file sha pin (no new run for 140.9; M29-side determinism presumed, residual 15). A failure → out-of-band triage per RB.2, not a scientific conclusion.
- **RB.2 within-seed variance of the reference (V1 variance run)**: maximum per-seed |Δret| of b1-varprobe-launch vs v32-ref-launch, **point 0 (bit-identical), band [0, 0]**. **In band** → register "within-seed measurement variance = 0 (deterministic protocol, limited to this runtime world and to the H side -- the M29 side is presumed, the note goes with the verdict, measured in a separate case, compliance m-4): significance of the paired Δ must use the cross-seed sign test; no t test in the name of measurement noise" -- closing the "variance unknown" gap registered in the F2 statistics. **Out of band (redesigned per compliance B-1 / statistics M-2)** → **drift triage first**: full re-check of W-E0/W5/W6/CASE_RUNTIME + analysis of the mismatch pattern (F2 already registered 3,445 bit-consistent episodes, so the prime suspect is world/runtime drift, not "variance"); any triage item not green → handle as CASE_HALT (exit code 7/6 per item); all green and the mismatch non-systematic → a REF_DIVERGENCE event, **halt for a design review** (exit code 8), and a dedicated variance case (≥k repeats) is set up separately -- **a 1-degree-of-freedom reading from n=2 must not become a project-wide noise band, and "publish as is and continue" is not allowed**. The RB.4 paired baseline rests on the bit-identity assumption, so if this line is not green the whole scorecard is deferred.
- **RB.3 dual-manager reading difference (8000-pool throne reference)**: K2 − K1 mean difference point **+25**, band **[+5, +50]** (the 7000 pool is known at +27.9); median/sign/deleveraged listed alongside. Out of band on either side is a registered finding: "the manager grid difference shifts a lot between pools".
- **RB.4 F2 regression reproduction (main reading 1; composite main verdict per statistics B-2, no picking the best afterwards)**: x := the 32-seed paired mean difference of p8-full32 vs v32-ref-launch, point **−20**, band **[−45, −2]** (v32 ctrl measured −20.74). **Three supporting limbs (pre-registered)**: S1 median limb = paired Δ median ≤ −5; S2 sign limb = negative seeds ≥ 21/32 (exact binomial one-sided p ≈ 0.056); S3 deleverage limb = the mean without the highest-leverage seed falls in the same region as x. **Verdict cells (five, pre-registered)**:
  - **reproduced**: x ∈ [−45, −2] ∧ at least two of the three limbs hold;
  - **leverage-driven candidate**: x ∈ [−45, −2] ∧ fewer than two supporting limbs -- the verdict is fixed as "reproduced at the mean level, not supported across seeds"; the effect size may not cite the mean, and the damage list is given seed by seed;
  - **not reproduced**: x > −2 ∧ median > −5 ∧ negatives ≤ 20;
  - **mean-masking candidate**: x > −2 but (median ≤ −5 ∨ negatives ≥ 21) -- the pattern where positive single-seed leverage evens out the mean; the verdict lists the deleveraged mean alongside;
  - **amplified**: x < −45 ∧ at least two of the three limbs hold (fewer than two → the negative variant of the leverage-driven candidate); the weight of a recipe-level root cause goes up.
  Every cell is a valid conclusion; the verdict is worded per cell, never lumped as "pass/fail".
- **RB.5 F-lock fingerprint reproduction (main reading 2)**: hits of the composite signature on the throne D seeds of the C set in the 7000 pool, **per the actual CANARY_SET n_D (statistics m-1)**: point = round(0.7·n_D), band = **[⌈0.55·n_D⌉, n_D]** (with n_D = 7: point 5, band [4, 7]); ≤ ⌈0.55·n_D⌉ − 1 → fingerprint not reproduced (judged together with RB.4). **Basis of the lower edge (statistics M-3)**: the original v32 case was 7/7 wiped out and was the sample the signature was built on; the band's lower edge still counts as reproduced when the seed set decays by nearly half, and below it the reproduction proposition loses majority support across seeds; every hit reading **must carry the "signature specificity uncalibrated" caveat** (D3). The F-lock-type share after triage goes with the verdict. The same signature's reading on the 8000 pool is listed alongside (CRITERION_VALIDATE: precision point **0.7**, band **[0.4, 1.0]**, **read against the band only with ≥ 3 hits**; handling in D3).
- **RB.6 P8 leg dual-manager reading difference**: mean-difference line (**unconditional**: the healthy throne +27.9 and the damaged legs +29.1/+32.9 are all in band, so no branches): p8-full32-m29 − p8-full32 mean difference point **+30**, band **[+8, +55]**. MS line (**two branches**): branch R -- max point **60**, band **[20, 130]** (the F2 catastrophe went off the scale at ~80), over-line (>20) seed count point **6**, band **[2, 14]**; branch N -- max point **10**, band **[0, 30]**, over-line point **1**, band **[0, 4]** (on a healthy leg both Δ are small, so MS should be near zero).
- **RB.7 held-out pool transfer**: paired mean difference of p8-hold8k vs b1-ref8k-launch point **−18**, band **[−50, +5]** (first exposure of the held-out pool; bandwidth widened honestly); 8000-pool king×H depth≥2 seed count point **7**, band **[2, 14]** -- if < 2, the signature criterion is "not testable" in this pool, and the CRITERION_VALIDATE verdict is downgraded and reported (conversion clause).
- **RB.8 canary instrument (existence proof for prescription 2; metric formula pinned, statistics M-5)**: **retention := |{s ∈ D_C : the leg×H canary (+393,216 point) archive has ≥ 1 D window for the seed}| / n_D**, with D_C = the depth≥2 side of CANARY_SET (throne baseline = the depth≥2 fact of the pinned v32-ref-launch archive itself). **Two branches**: branch R -- point **0.3**, band **[0, 0.7]** (predicts that the decay of the dive chain is already visible mid-leg, filling the −6% vs −34 telemetry blind spot of F2); above the band (>0.7) while the final exam still collapses → register "the collapse happens in the last part", and denser canaries become a separate case. Branch N -- point **0.9**, band **[0.7, 1.0]**; below the band → registered finding "decay visible mid-leg but recovered by the final exam".
- **RB.9 dead-gate would-trip (definition: g_pg/g_ce with β folded in, see E3)**: median gradient ratio over the 10 calib points point **40**, band **[10, 120]** (F2 measured 20-90× under the same definition); number of points at which the gradient-ratio gate (>30) would trip point **7/10**, band **[3, 10]**; share of points over the distill_ce absolute gate (>0.2) point **0.8**, band **[0.3, 1.0]** (**saturation is expected**; the gate is a recalibration target, not a discriminator; the note goes with the verdict); dry-anchor increment (zero point 0.7515) point **+1.5pp**, band **[0, +4pp]**, with the dual-reference lineage note (lineage start 0.6305) going with the calibration data. All record only; calibration input for the recalibration case.
- **RB.10 manager observation shape drift (OBS_DRIFT schema in E4)**: throne D-window side (**unconditional**, a reference measurement): walkSum/121 point **54**, band **[40, 65]**. Leg side (**two branches**): branch R -- point **79**, band **[65, 95]** (P5 measured 79 vs 54); branch N -- point **55**, band **[40, 70]** (a healthy leg should show the throne's shape). Per-dim P50 differences of the 8 appended dims listed alongside (the P1 revised-ruling caveat applies: the instrument dims only flip marginal windows; the shape dims are the main suspect).

**Residual uncertainty**: (1) P8 is only one leg with one new seed -- replication goes 1→2, still not a population estimate, and extrapolations like "any continued training must collapse" stay forbidden; (2) the composition of the 8000 pool is unknown, and whether the criterion is testable depends on its number of depth≥2 seeds (guarded by the RB.7 conversion clause); (3) canary granularity is ~98k steps, so a collapse between points is invisible, and the C set reuses exam seeds -- the anti-peeking protection rests on the record-only discipline and the single-end-checkpoint clause, not on pool isolation; (4) the dead-gate thresholds (0.2 / 30× / +2pp) were calibrated on a single v32 lineage leg; this case produces calibration data, and formal gating is a separate case; (5) MS is structurally blind to "damage in a blind spot shared by both managers" (missing exhausted flag, 7017 type), so MS≈0 must not be read as undamaged; (6) if RB.2 finds variance = 0, the conclusion is limited to this runtime world and the H side, with no guarantee across machine/engine rebuilds (guarded by CASE_RUNTIME; a breach is operational, not scientific); (7) the throne reference is still a single worker and a single checkpoint; whether the "throne shape" generalises across workers is unknown; (8) horizon sensitivity (the D3 statement from P5; frozen, mandatory with the verdict); (9) W-G0 already guarantees that every instrument event fires at least once in the window and is judged by a tensor-level criterion, so the residual shrinks to the IO/clock theory of extrapolating the short run to the full run; (10) P8 shares BC_SD/KING_SD/the M29 manager with v32 ctrl -- the replication breaks only the training-seed dimension; the teacher-generation and manager dimensions are still N=1; (11) dual-manager bundling rests on the template obligation and panel discipline, not on contract enforcement, and can technically be bypassed -- contract-level enforcement has to wait for the version upgrade in the succession case; (12) cumulative peeking from reusing the 7000 probe pool across cases keeps deepening; the 8000 pool is the hedge for this, and its exposure budget (HOLDOUT_EXPOSURE, per run) also does not regenerate, guarded by the separate request clause for the first exposure of 2 pairs/4 runs; (13) **composite-signature specificity uncalibrated** -- no healthy-leg control in the case, false-positive rate unknown; the first measurement happens if RB.4 lands on "not reproduced" (conversion clause); (14) **W-H8 is a registered-side virginity assertion**; unregistered historical manual exposure of the 8000 pool is undetectable; (15) **M29-side evaluation determinism is presumed** (V1 is H side only); the presumption note must go with M29 verdicts, and a measurement is left to a separate case.

---

*(Verdict appendix: written by the operator after the case closes; the commit is the notarization.)*

---


---

## Pre-freeze correction record (operator, 2026-07-17, implementation and smoke-test stage; all in the tightening direction)

1. **W-G0 smoke seed 305000→307000**: the 305000 pinned by the panel had actually been used by v30 bc-leg1 (leg_start recorded in v30/gate_ledger.jsonl) -- this case's freshness assertion blocked it on the first run (exit code 3, NEEDS_ATTENTION in the infra-b1 ledger), and following the intent of the assertion the virgin seed 307000 was taken instead (full ledger scan: 101-108k/281-283k/301k/302k/303k/305k/306k occupied, 304000 reserved for the case leg).
2. **BC_SD_WAIVER → BC_RECEIPT_REFRESH**: the original waiver clause was inconsistent -- it waived regeneration but gave no channel to refresh the receipt; after the E0 implementation the impl sha must change, and the train_ppo data_gate refused to start on the v32 receipt (measured rc=2, died before creating directories, no side effects). Changed to **in-case receipt refresh + byte-invariance assertion**: rerun bc_worker.py (fully seeded; the v32 _previous snapshot shows that regeneration only refreshes receipt fields, with demos and policy_sd bytes unchanged), asserting that the BC_SD sha equals the v32 constant f052067a… verbatim, with inequality → CASE_HALT and a report -- stricter than the original waiver; the W7 constants are unchanged.
3. **South-west band cell set pinned during implementation**: the RB.10 "south-west band" follows the engine's Direction::SouthWest={0,+1} and takes the 15-cell strip with dy∈[1,5], |dx|≤1 (the case file has no machine-executable precedent) -- registered as an implementation interpretation, for the panel to confirm at the verdict appendix.

---

## Verdict appendix (VERDICT_PATH 2026-07-17; written by the operator; the commit is the notarization)

**Operations note (recorded first, because it is this case's hidden win)**: the E6 hardened launcher's first live success -- during the run
the parent terminal session was replaced, and the orphaned, directly hosted process was unaffected; all stages S0→S9 completed with zero manual intervention.
The v32 OPS incident pattern is closed. The launcher's own PID-bookkeeping assertion raised one false alarm (it checked the wrong
PID; the launch itself was fine), recorded as a minor, to be fixed in the next version.

**Main verdict (RB.4 × RB.5 joint verdict): "leverage-driven candidate".** Rerunning the ctrl recipe leg with a new training seed (304000):
paired mean difference −5.37 (in band), but the three limbs break it down -- median +0.06, signs 14/32,
deleveraged mean (without 7017 at −149.86) −0.71 -- **broad retraining regression was not reproduced, and most of the magnitude of
F2's −20.7 was the lottery component of training seed 303000**; while the F-lock fingerprint partly reproduced
(D windows gone on the throne's deep-dive seeds, 5 hits out of {7001,7004,7017,7024,7029}, inside band [4,7],
with the borderline note) -- **the mechanism is real; the size of the damage is a seed draw**. 7017 was wiped out under both training seeds
(this leg alone −149.86) and is promoted to "deep-water devil seed": a seed every continued-training leg loses, kept as a standing
control for later cases. The mandatory uncalibrated-signature-specificity caveat goes with the verdict.

**RB.3/RB.7 (dual manager and held-out pool, the first readings of the confirmed prescription 1)**: manager sensitivity reproduced on the virgin
8000 pool -- MS mean 20.8 (in band), sign test p=0.0033, 17/32 seeds over the flag line
(family accounting note applies); the new leg's paired difference against the throne on the held-out pool −7.07 (in band, median −0.08, deleveraged
−2.73) ≈ throne level, depth2 count 5 (band [2,14]). **The dual-manager bundling instrument proved its
worth: the manager-coupling fragility that training telemetry never noticed was caught on the first run.** The MS caveat goes with the verdict: M29 and
H share the exhausted blind spot and the 7017-type blind spot, so MS≈0 does not show the worker is undamaged.

**RB.9 (dead-gate calibration data, prescription 2 delivered)**: median gradient ratio 37.5 (F2's 20-90× reproduced on the new
seed, so the anchor-leash failure is recipe-level, not seed-level); would-trip at 6/10 points; ce_over
saturated as expected (recalibration-target note); **the only out-of-band reading: dry-anchor increment +8.35pp
(band [0,4])** -- the new leg drifts on the dry-layer protocol much more than the two v32 legs (+1-2pp) with no loss of
performance, the top new lead for the dead-gate recalibration case (lineage note 0.6305 goes with the calibration data). Family accounting: 11 readings,
1 out of band, scattered not clustered, no escalation.

**CRITERION_VALIDATE (first exam of the criterion, an honest negative result)**: the pre-registered catastrophe criterion on the held-out pool
had precision 0.0 (TP0/FP3/FN1, CI [0, 0.71], out of band) -- with its current construction the composite signature does not discriminate well when the whole pool
shifts down; the first REC limb is nearly always true, and the whole burden falls on the M29 recovery limb (note on record).
**Redesigning the signature goes on the agenda of the bundled content case; this case's verdict is unaffected (the criterion is an instrument, not a decision line).**

**All three acceptance limbs recorded**: (1) prescription 1 confirmed (E1 channel + template obligation; gold evaluation per D0-1, no new rule);
(2) the full prescription 2 calibration data (10 calib points / two gates / dual reference / OBS_DRIFT / canary sequence), for the formal dead-gate
gating case to cite directly; (3) the P8 reproduction verdict as above. The four mandatory notes (horizon / H-track default /
non-retroactivity / residuals 1-15) carry over with the document.

**Scientific conclusion in one sentence**: retraining is not poison, the seed is a lottery ticket, F-lock is a real mechanism, 7017 is a
devil seed, and the dual-examiner instrument is standard from now on -- **the foundation of the bundled content case (4×2) and of the dead-gate gating case
is laid.**
