# PREREG v33 (course 5 × 4b): course 5 dry-window curriculum × 4b positive examples (three-leg single-variable ladder)

**Status (last published record, 2026-07-22)**: frozen on 2026-07-19 (FREEZE_SHA 60facd9); G0 checks passed, including G0-6 REF_BITEQ (refs 113.0 and 140.9, 32 rows bit-identical); the N12 gate failed at 0.65 (n12 157, recall 0.4667), the single OC was used, and it passed at 0.70 (n12 199, recall 0.5263, top-1 0.9816); a launch-time regression audit (2026-07-22) found 2 blockers and 4 major issues, fixed in 239e8b2 under a chained re-freeze; the driver was left at the launch gate (exit 9, AWAITING_LAUNCH). The published record contains no launch.

**rev2 (revision after the three-seat panel)**. Handled: statistical-discipline seat B-1/B-2 + M-1..5 + 6 minor; house-rules compliance seat B-1 + M-1..4 + m-1..6; engineering-feasibility seat B1-B3 + M1-5 + 6 minor, plus its driver-structure advice. Every blocker/major is in the text; the integrated rulings on the three disagreements between seats are in the panel minutes.

**rev3 (verification correction, 2026-07-19)**: the three seats of the verification panel (document consistency / code landing / adversarial falsifiability) reviewed independently, and an adversarial re-checker reviewed each item with the aim of overturning it: **1 blocker + 4 major + 6 minor, all CONFIRMED, zero false alarms**, all corrected in the text: (1) W-PIN "six G1 LINE_DERIVED event lines" copied the row count of the G1 verdict table without checking the ledger (the ledger has five lines + WOULDTRIP_RULING), so a literal preflight would always block (blocker, D1); (2) RC.6 "v32 R32.3 measured 1.5" was a hallucinated citation; the measurement was 0.0 (D8); (3) the E1 impact surface was mislabelled as "six conditional gates"; it is split into four gates + two recorded values (E1/E7); (4) the virgin-seed rule of the smoke test had two coexisting meanings; three limbs pinned, 307000 removed, 308000 pre-registered (D2-2a); (5) all three G0 items were blind to how the annealing table is actually executed; an assertion of identity of the measured p sequence was added (E1 item 2 / D2-2b); (6)-(11) six minor items (split notes on the 75% definition, budget split into rows, the belt-slot back-derivation definition, split notes on the two E5 (1) definitions, the full open-item table A-G written into the document, the timing of confirming early OC use) in their sections. The correction follows the G1 verification precedent: rewrite the text + keep this note; no silent edits.

**rev4 (implementation-period correction, 2026-07-19)**: implementation of E2-E8 + the v33 driver completed; the cross-checker (246 tests green) raised 3 major + 3 minor, all handled: major-1/2 were mechanical additions (three BC-v2 demos byte assertions; the G0-2a instruments-really-ran check extended to the sentinel/dry-anchor limbs), fixed at once; major-3 was an internal conflict in the plan (D3 command form vs E5 (2) / design decision 12 / RC.14), corrected by derivation from the approval (D3 adds two record-only instrument knobs, the obligation side of approved design decision 5); the three minor items (E5 (3) per-game downgrade, DRY_CURRICULUM_TABLE full precision, implementation definition of the G0-2b rng proof) each got a note. The full list is in section 12 appendix 2; the freeze report lists the items still to be confirmed.

**rev5 (plan A, 2026-07-19)**: the first live BC-v2 collection failed both gates (off-ledger rehearsal: n₁₂ 62/73 against gate 122, recall 0.0 against gate 0.5; gap f came true + the sparring proxy used the wrong yardstick); three options were put up for decision, and **plan A was approved: collect x3 + class-weighted sparring, gate values unchanged** -- open item C is closed with this. Changes: E2 collects 384 games (extension of the DEMO_SEEDS_V2 discipline) + class-balanced weighting; rehearsal re-test 157@0.65 (short by 0.033) → **199@0.70, both gates PASS (recall 0.5263)**. The approval record, the full table of readings, the S-bc1 W7 byte-identity evidence and the smoke-test block record are in section 12 appendix 3.

Status of this text: **verification-passed draft (rev3)** (freeze discipline as in R1/R2/B1/G1: the commit is the notarization, the text holds no self-referencing sha, the freeze sha goes only into the ledger FREEZE_SHA event). **Target state = frozen, waiting for launch**: panel review → E0 baseline capture + E implementation → G0 **six-piece set** live (incl. two in-case refs runs, restoring section 9 of the design memo (not published), see D2) → after FREEZE_SHA is recorded, the case waits; **launch deferred until the training machine was available** (a LAUNCH_ORDER event records the approved launch; without it the driver halts and waits before the first leg_start of the three legs, see D5/W-LAUNCH).

**Numbering note**: this is a **content case** (the follow-up of "foundation of the bundled content case laid" in the B1 verdict appendix) and takes a v-series campaign number. **Numbering timing (compliance m-5)**: the number is assigned after the panel review and **before the first ledger event** (the ledger directory `train/runs/<vN>-content/` exists under its real name from the first event on); the earlier wording "assigned at the freeze commit" is dropped, and the tentative course 5 = v33 has historical meaning only; if the numbers happen to match, that is a coincidence, not inheritance (same note as v32, verbatim). **Naming follows the design memo (not published)**: "4b" in this case follows the F1 usage of the v32 verdict appendix (positive-example injection); the "4b = softening" note in docs/design/DESIGN-gear-and-potion-autonomy.md has been superseded by the design memo (not published) and is no longer cited.

**Honest statement on zero drift (from the B1 correction precedent; effective before freeze)**: this case's E1 must change `worker_env.py`, and that file is **part of the protocol bundle** -- so this case's runtime_five and full impl bundle sha **must differ** from the values settled in the v32/B1 ledgers, and the B1-style W-E0 bit-level assertion **cannot be used; never report it as "sha identical"**. The substitute proof of zero drift on the evaluation side = **the two REF_BITEQ runs** (v32-ref-launch 113.0 full table / ref-science 140.9 full table per seed per field; **a G0-6 freeze prerequisite, not a post-launch item**, compliance M-1 restoring item 6 of the G0 list in the design memo (not published)) **+ G0-1 dual-endpoint bit-level identity** (p≡1.0 ≡ current skip_dry=True; p≡0.0 ≡ skip_dry=False; both baseline files are captured by E0 before implementation, see below). A mismatch means CASE_HALT_G0 (exit code 7); no "the new reading is also plausible" downgrade.

---

## 1. Background and approval chain (recorded as is, numbered design decisions)

- **The approvals**: numbered design decisions approved on 2026-07-17 (13 items of the design memo (not published); recorded on 2026-07-18):
  - **Decision 1 (case and form), approved**: course 5 × 4b bundled in one case, three-leg single-variable ladder, with a degenerate two-leg fallback on record;
  - **Decision 2 (course 5 mechanism), approved with an addendum**: 5A proportional annealing as the main plan; **the conservative table 1.0→0.5 is fixed as the main table by the approval** (301,056 linear + 198,656 flat); the aggressive table 1.0→0.0 stays as the OC; 5B on file;
  - **Decision 3 (4b mechanism), approved**: 4b-1′ = improved teacher collection (0.65 preventive threshold + hysteresis guard) + λ_bc auxiliary demo loss (class-12 subset as the main plan / all classes as the OC); **the anchor follows the king and is not broken**; 4b-1-b/4b-2 on file, 4b-1-c/4b-3 not adopted;
  - **Decision 4 (n₁₂ freeze-prerequisite gate), approved**: a minimum class-12 pair count in the BC-v2 receipt + a separate recall gate are freeze prerequisites, with an OC contingency pre-registered for a shortfall -- **the gate values are calibrated by the archive audit and pinned in this document (D7 table); the OC path is narrowed to a single registered path, see D2-5; the difference from the two contingencies in the original approval is submitted for review (open item C)**;
  - **Decision 5 (course 2 not taken), approved**: β=0.015625 frozen at its original value, anchor formula untouched; only read-only dry/fresh distill_ce instruments are added, and they fall inside the W-G0 proof scope;
  - **Decision 6 (first use of the G1 new lines), approved**: full conjunction recorded (four numeric limbs + sentinel limbs + not voided); the abandon gate is pinned here as applying to this case; the single-vector line / K1 single-archive dependency labels travel with every citation; **the freeze report lists a separate "new-line adoption" item** (where the G1 D7-6 first-citation clause is fulfilled, see D4-1); **the G1 D7-2 obligation "later-case panels treat it as an upgrade review" is written into what this document submits for review: the panel must give a separate review opinion on the two labelled limbs; no routine pass-through** (compliance m-3);
  - **Decision 7 (contract 4→5), approved**: all three legs on rev5, L-base carries disabled keys; the known cost that old checkpoints will be refused for non-legacy continued training is recorded as is;
  - **Decision 8 (reuse of seed 304000 + 7017 as a standing control), confirmed**;
  - **Decision 9 (one 8000-pool pair on the winner; gold-evaluation notice), acknowledged**: includes the advance notice that **"failed succession" with died∈{7,8} is structurally expected**;
  - **Decision 10 (the 4b main verdict on L-full is always judged), approved**: judged win or lose; main line under M29, the H reading listed alongside and not a separate line, decoupled from release. **Tier grammar correction (compliance B-1): the original wording "follows the R32 main verdict verbatim" becomes "follows its form, with the decision-line constants re-registered for this case's context (ctrl=L-cur×M29)" -- the original v32 tier constants were registered against ref-science (140.9/died 3) and cannot be carried over verbatim; the re-registered table is in D4-3 and D7 and is submitted for review (open item B)**;
  - **Decision 11 (composite signature v2 goes back as an auxiliary deliverable), confirmed**: material from six legs, selection-bias note, and the firewall that decisions use the v1 constants;
  - **Decision 12, rewritten, with a ladder clause**: the v32 decoupling clause's obligation to move 4×2 to another case is rewritten -- **the exam obligation for item 4 is taken over by this case (course 5 × 4b); course 2 (the wage-stripping formula) becomes its own case, scheduled after this one, and this case's dry/fresh distill_ce split instrument supplies its calibration data**. Ladder semantics: if this case's L-full 4b main verdict fails, **a new design review decides at that point** (retry with a different mechanism, or honestly declare extinction and release), without prejudgment, by the data. This clause is the mandatory exit syntax of this case's verdict (D4-4);
  - **Decision 13 (zero-launch archive audit as a prerequisite), approved and fulfilled**: AUDIT-content-case-archive.md filed on 2026-07-18, read-only throughout, zero launches.
- **Upstream of the approval chain**: (1) v32 verdict appendix F1 (bare unmasking does not work; 4b is the prerequisite for reviving a12) / F2 (retraining regression, handed to the bundled case and the dead-gate case) / path ruling 1; (2) the B1 verdict appendix (seed lottery, the F-lock mechanism is real, 7017 deep-water devil seed, dual-manager bundling confirmed, signature redesign agenda, "foundation of the bundled content case laid"); (3) the G1 verdict appendix + the confirmation record (the full new-line table is final and applies from the first later-case pre-registration that cites it -- **this case is the first citing case**); (4) FORENSICS-F2 (P1/P5/P7/P8/P9) and the course 5 row of docs/design/ROADMAP-course-plan.md.
- **Three archive-audit corrections (frozen definitions, replacing the wording of the design memo (not published))**: (1) "the teacher mostly presses 10/13 in dry windows" has no source and is withdrawn -- the archives show that the BC-v1 demos are a **pure a9 degenerate single-class set (166,383/166,383; the receipt's class_recalls contain only class 9)**; the teacher's real dry-window behaviour = idle a9 presses (only 0.6% adjacent monsters) + the fuse forcing a10 about once every ~25 beats (and the collection rule removes a10 beats entirely) -- **the design that limits λ_bc to the class-12 subset is locked in by the data** (without the class limit, exactly the idle presses would be amplified); (2) "76% of learning episodes are dry windows at p=0" confused the ff definition; **under the episode definition it is 92.9-93.2%**, and the fresh-window gradient share collapses to about 1/3 -- the p_end=0.5 argument becomes stronger (measured episode share ~87%, fresh-window gradient share kept at ~51%; "half the guard rail" holds exactly in the step-share sense); (3) the dry-window signal surface made precise: **median wage −0.03, 93.6% negative windows, 2-9% fat tail** (clearing remaining monsters 0.3-3.4%, levelup conversions 4-11 windows per worker, death penalty 1/3,413) -- the mechanism gap is real but not zero, and the "identifiable signal" table of D4-2 is pinned from the audit.
- **Nature of the case**: this is a **content case with a campaign number** -- it includes an environment-side change (E1) and training launches, and every decision line follows the first use of the G1 new lines; B1's "a measurement case has no abandon gate" **does not automatically apply to this case**; the applicability of the abandon gate is pinned in D4-1.

## 2. One campaign, one prescription

Prescription = "**make the structural blind spot of the training distribution (dry-level windows) part of the curriculum (course 5), and at the same time feed the extinct drink key a12 real examples and lay an injection path that really reaches the policy gradient (4b), to build, on the new G1 scale, a new leg that can really challenge the throne**". The three legs are a dose ladder of the same prescription: L-base (retraining + autonomy baseline) → L-cur (+course 5) → L-full (+4b package). Attribution relies on single variables between adjacent rungs; **4b package = the conjunction of the demo injection path (λ_bc>0) + the BC generation change (v2 files)**, and the verdict must carry this caveat. **Pre-registered degenerate plan**: if the panel finds the conjunction caveat unacceptable, L-base is cut and the case runs two legs (L-cur/L-full) with the same attribution grammar, and the leg budgets and exam table shrink proportionally with a re-freeze; **the decision to cut a leg is taken at design review; the pre-registration does not trigger it automatically** (open item G).

## 3. E implementation list (committed with the freeze commit; every new training-side knob must pass G0 before freeze)

- **E0 pre-implementation baseline capture (engineering B3; before any implementation, executed outside the driver)**: capture **dual baselines at both endpoints** with clean stashed code outside the case (worker_env.py before the change) via the probe_g0_traj rig: the existing `g0v32_traj_baseline.json` only covers the skip_dry=False endpoint -- **add a skip_dry=True rig baseline file** (the rig is constructed with skip_dry=True explicitly), and re-check that the skip_dry=False replay still passes; **the baselines cover both king + throne** (compliance M-3; g0v32_traj_baseline_throne has the same form; both skip_dry=False baselines are on file per the audit). The baseline files are committed with the freeze commit and their sha values become frozen driver constants; the G0_BASELINE event carries the note captured="before the change" (v32 precedent). **The burden of proving "before the change" for G0-1 is carried by this stage alone; new code may not vouch for new code**.
- **E1 5A predicate parameterisation + annealing scheduler**: `skip_dry` in `worker_env.py:112,127,159` is promoted from a boolean to a skip probability p_skip (bool→float, with the boolean meanings kept as 1.0/0.0); train_ppo adds `--dry-curriculum-schedule`, and a callback pushes p per rollout. **Main table (fixed by the approval)**: the first **301,056** steps (147×2048 rollout quanta) linear 1.0→0.5, then **198,656** steps (97×2048) holding 0.5; 147+97=244 quanta, and 301,056+198,656=499,712 is exactly the leg length (re-checked). **Endpoint interpolation semantics (registered during implementation, noted in the freeze report for review)**: item k of a linear segment = p0+(p1−p0)·k/(n−1) (k=0..n−1), and the last item of quantum 147 reaches exactly 0.5 -- strict decrease occupies 299,008 steps and p=0.5 occupies 200,704 steps; the other reading (0.5 only from the start of the flat segment) is also possible on the plan; this registered semantics is the source of truth, and recording the full DRY_CURRICULUM_TABLE seals it. **Scheduler implementation details (engineering M3, frozen)**: (1) the push goes through SubprocVecEnv `env_method`, via Monitor `__getattr__`, to a new setter on WorkerWindowEnv, **before collection starts in the callback `_on_rollout_start`; the callback order is fixed**; (2) the p table is anchored on the **leg-relative rollout index `(num_timesteps − 3,497,984)/2048`** (the leg always starts at 3,497,984 and P3 relaunches always resume from the king zip; anchoring on global steps is forbidden), and the DRY_CURRICULUM_TABLE event records the **full "index → p" table**; **measured p recorded per rollout + identity assertion (added in verification; closes the gap all three G0 items were blind to)**: the curriculum callback records the p actually pushed per rollout (leg-relative index → p), asserts inside the callback that it ≡ the registry entry and raises on a mismatch (leg rc≠0, halt for review), and at the end of the leg the driver re-checks that the whole recorded sequence ≡ the full DRY_CURRICULUM_TABLE, with a mismatch → CASE_HALT_G0 (7) -- this closes the gap of "the table was recorded but a different one ran"; (3) the p draws use their own dedicated rng stream (following the dry-anchor rng(26) precedent, zero contamination of the training RNG), and **the per-env seeding rule is fixed: at reset(seed) it is derived deterministically from the episode seed with a fixed offset**, which makes the reproduction claim hold; G0-2b re-checks it live. **Enumerated impact surface (engineering M4; split per the verification correction)**: the `skip_dry` impact surface in train_ppo.py has **two kinds** -- **four conditional gates** (mutual-exclusion assertion :445, demos/BC preflight gate :499-505, demos_sha256 capture :1766-1771, dry_cb mount gate :2101-2104) have their predicate rewritten to **"dry-window mechanism on = skip_dry ∨ schedule"** (otherwise the dry-layer anchor sentinel and demo-set checks would silently not run on curriculum legs); **two recorded values** (contract key :382, config receipt :1815) **are not gates, and the skip_dry key keeps the CLI flag's literal value as recorded**, while the mechanism state is carried by E4's new dry_curriculum/bc_aux keys (config receipt gets the same added keys) -- the contract skip_dry key is a cross-case forensic anchor and must not be overwritten by the predicate (the original draft's "rewrite all six conditional gates" would, implemented literally, have recorded skip_dry=True in the contracts of L-cur/L-full that do not carry --skip-dry and conflicted with E4; verification CONFIRMED and corrected); **seventh touch point (reported during implementation, confirmed by the cross-checker)**: the env's initial value at construction = the first item of the curriculum table (env_fn) -- SB3 `_setup_learn` calls `env.reset()` before the first `_on_rollout_start` push, and a misaligned initial value would make the leg's first window choose with p=0.0 and break the G0-1/G0-2a endpoint identities; it is neither a gate nor a recorded value but required wiring for the callback semantics, and the enumeration is extended accordingly; **`_validate_args` adds a mutual-exclusion assertion for `--skip-dry` and `--dry-curriculum-schedule`** (engineering B1); the legacy branch print text (:404 "writes contract_revision 4") changes with it. The evaluation definition does not change at all. The aggressive table 1.0→0.0 is a parallel OC row (recorded, not ruled on).
- **E2 4b-1′ collection (bc-worker-v2)**: bc_worker.py teacher v2 -- a branch before `dispatch("farm",…)`: **hp∈[0.5,0.65)∧belt>0→12** (preventive drinking; constant registered in D7); **plan A revision (approved 2026-07-19, section 12 appendix 3)**: collection games x3 (DEMO_SEEDS_V2 = range(100,484), 384 games, extended programmatically from the v1 seed-discipline rule "consecutive ascending integers"; the v1 collection surface unchanged); sparring training switches to **class-balanced weighted CE (w_c = N/(K·n_c), class set = classes actually present)** -- faithful-proxy argument: the real injection path λ_bc feeds the class-12 subset alone, which is structurally the limit of class weighting, so unweighted sparring is the wrong yardstick (rehearsal recall 0.0→0.4667→0.5263); the receipt gains two keys, collection_episodes / class_weights; **the hysteresis guard of ≤1 preventive drink per window is a settled clause of the main plan** (it prevents a chain-drinking distortion between the 0.5 drain exit and the 0.65 threshold; n₁₂=244 is the faithful point estimate under this design). The autonomy-on collection loop (drink_sovereignty=True by default, checked) **really executes and really enters the pool**, and the assertion that removes overridden beats entirely is unchanged -- on-manifold demos, no counterfactual labels. **Generation flag + separate output directory `runs/bc-worker-v2/`** (the v1 canonical paths `train_ppo.py:500,1694,1768,2102` unchanged, avoiding the `_previous` archive exclusivity); **the collection-ban assertion is conditioned on generation**: the v1 path keeps the ban on (11,12) unchanged, the v2 path bans 11 and allows 12 (the guard is not weakened). **v2 receipt schema isolation (engineering M5)**: the v1 receipt schema and `_BC_REPORT_SCHEMA_VERSION` (=1) **stay unchanged** (`_validate_bc_report` asserts exact key-set equality, so adding a key would break v1 re-validation); the v2 receipt has **its own file name + its own schema identifier + a dedicated validator**, and the v1 validator fails loudly on v2 files (the key-set assertion gives this for free). **The v2 demos schema adds a per-sample masks array** (engineering M1 + verification correction: the λ_bc auxiliary CE must be fed action_masks; **the full 15-dim mask vector cannot be fully reconstructed from obs** (m[12] also depends on the autonomy knob); the belt slot of a12 could be back-derived from the obs belt dim (belt_heals/8.0, env.py:1210/1214), but **a back-derived definition would be a second source of truth and is not allowed** -- capturing `env.action_masks()` live at collection time is the only on-manifold source; the draft sentence "belt>0 cannot be reconstructed from obs" did not match the code and is corrected, with the implementation unchanged). **Definitions of the n₁₂ gate and recall gate pinned (statistics B-1)**: denominator = **labelled a12 states of the teacher-v2 hysteresis simulation under the current split (window-start trigger states)** -- not all in-band trigger states (the 1,975-state definition is dropped: the teacher itself labels only ~5% of them, so with that denominator the gate could never pass by construction); metric = **held-out argmax hits**; fail-closed following the bc_worker.py:163-168 precedent (zero coverage is recorded as 0.0 and does not disappear); **the N12_GATE event schema carries the denominator definition and a per-game breakdown**; the verdict must carry the **"denominator cluster" note** (verified correction: what the audit showed is that under the held-out trigger-**state** definition 74.7% is concentrated in two clusters, games 178/187 (880/596/1,975); the per-game concentration under the labelled-window (window-start) definition was not given by the audit and follows the N12_GATE measured per-game breakdown -- the draft passed a state-definition statistic off as the labelled-state definition, the same kind of mix-up as 76%/93%; recall readings are noisy). The receipt adds a teacher_generation field, and the class 12/13 shares and belt-economy readings go into the receipt; the current ≥300 per-class recall gate at `bc_worker.py:163` will certainly not engage for a12 (audit 244<300), so a separate gate is required.
- **E3 4b-1′ path (auxiliary demo loss)**: the leashed_ppo training loop adds an auxiliary demo CE term **λ_bc (main plan limited to class-12 demo pairs** -- the literal implementation of positive-example injection, not pulling against the KING anchor on the other actions; all classes as the OC alternative); train_ppo adds `--bc-aux-lambda / --bc-aux-demos` + a dedicated v2 validator (mirror assertions written separately per generation; the v1 surface at `train_ppo.py:61,702` unchanged); demo minibatch sampling uses a dedicated rng stream (seeding rule pinned in the same form as E1 item 3); the auxiliary CE forward pass consumes the per-sample masks of the v2 demos; **anchor teacher = KING_SD unchanged, β=0.015625 frozen at its original value, the anchor follows the king and is not broken (decision 3; β/anchor formula untouched is decision 5; both cited per compliance m-2)**. The frozen λ_bc constant goes into D7. Anti-P2 fingerprint obligation: v32's wa[12] rel 0.000 "zero path by construction" is the negative example, closed by the G0-2b live assertion "head-12 gradient non-zero".
- **E4 contract upgrade 4→5, all three legs**: +dry_curriculum, +bc_aux keys; L-base carries dry_curriculum=disabled and bc_aux=disabled -- zero dual versions within the case; L-base's equivalence to v32-sov **is proven by the G0-2a tensor-level identity, not guaranteed by a "recipe verbatim" slogan**. The known cost that old checkpoints will be refused for non-legacy continued training is recorded as is (R8).
- **E5 new instruments (all record-only, all inside the W-G0 zero-intrusion proof)**: (1) the dry/fresh distill_ce split is **pinned as an offline probe** (engineering M2; the verification split note on the two definitions: **there is indeed no window-level dry label (window-open flag) in the buffer**; the instantaneous dry flag obs[:,297] is in the buffer per sample (the current DryAnchorSentinel groups by it), but splitting inside training is still dropped to protect the G0-2a zero-intrusion proof; the offline probe uses the instantaneous-flag definition, and the difference from the window-level definition is noted with RC.14) -- a twin of DryAnchorSentinel that, on a fixed demo-state set, splits by the dry flag `x[:,297]` into dry/fresh groups and computes the teacher-student distill CE, with a dedicated rng and zero contact with the training path; the obligation to supply course 2 calibration data (decision 12 rewrite) is met by this form too; (2) **dry-window behaviour instruments**: dry-state action-distribution entropy, a distribution, fresh-window wages and width (τ̄/depth) -- audit gap i on record, **baseline built from this case's first launch, no prior band**; (3) **mid-leg canary a12/game instrument** (the L-full re-extinction dynamics R11 visible throughout; **rev4 downgrade note**: evaluation archives have no per-game action histogram, so the per-game definition (episodes_with_a12/a12_max) is not available in this case -- the A12_CANARY event records the aggregate definition (a12_total/episodes/a12_per_episode), the registered schema is kept for a later case, and the RC.11 sequence is unaffected, see section 12 appendix 2 item 2). The full B1 instrument set (10 calib points / sentinel / denser dry-anchor / ckpt 98,304 canary point table / MS / OBS_DRIFT / dual would-trip readings) comes along with each leg, read under the G1 D6 downgraded definitions (the dry-anchor band never had normative meaning; the +8.35pp counterexample note applies).
- **E6 decoupling of the dry-anchor probe set**: the instrument probes of all three legs are pinned to **the BC-v1 demos bytes** (**demos_sha256 frozen constant 3bf892d611e41853eca8fce0cb146753af41ad2c3a21b6c581df1041fb1d9363**; guarantee chain per engineering B2/minor-5: what guarantees cross-leg, cross-case comparability of the dual references 0.7515/0.6305 is the identity of the demos **array**, carried by this byte assertion); BC-v2 enters the auxiliary loss only via `--bc-aux-demos`, and the canonical path does not change. **f052067a… is the policy_sd.pt constant (BC_SD_SHA in the B1 ledger); the draft attributed it to "demos bytes" by mistake; corrected** (compliance M-2 + engineering B2).
- **E7 E-type test updates**: tests for masks/curriculum assertions rewritten (p parameterisation semantics, contract rev5 key assertions); **unit test of the two-flag mutual exclusion**; **per-gate unit tests of the four E1 "skip_dry ∨ schedule" gates + unit tests that the contract key/config receipt skip_dry ≡ the CLI flag literal** (counterpart of the verification correction); mirror unit tests of the generation-conditioned bc_worker collection ban (v1 bans (11,12) / v2 bans 11, allows 12, written separately); unit tests for annealing-scheduler parsing, leg-relative index anchoring and the per-rollout p table; unit tests for λ_bc consumption, class-12 subset filtering and **masks consumption (class-12 demo pairs have m[12]=True)**; unit tests for the n₁₂/recall gates (fail-closed); unit test of the mutual exclusion of the v1/v2 receipt validators.
- **E8 launcher**: launches via **`train/launch_case.sh`** under the B1 E6 hardening clauses (orphaned, launchd-hosted PPID=1 + caffeinate assertion + progress.jsonl heartbeat mtime operator procedure; B1's first live success on record; the fix for the minor PID-bookkeeping false alarm comes with this case's commit).

## 4. D1 world identity and W gates

W1 freeze notarization / W2 version / W3 engine REF / W4 diabdat / W5 impl stage sampling / W6 runtime / W8 idle + driver mutual exclusion / W9 first-launch prerequisites / W10 resume reconciliation / W11 identity restated per run / CASE_RUNTIME five case-level sha reconciliation / P6 chained re-freeze -- all inherited verbatim from the B1 driver. Specific to this case:

- **W-PIN input archive pins (G1 form, full-file sha as frozen driver constants, preflight mismatch → PREFLIGHT_FAIL)**: v32-ref-launch (113.0) / v32-ref-science (140.9) / b1-ref8k-launch K1 / b1-ref8k-science K2 / the B1 CANARY_SET event line (C set and n_D inherited and pinned) / **the five G1 LINE_DERIVED event lines + the WOULDTRIP_RULING event line** (five LINE_DERIVED lines RG1.1-width/RG1.2a-mean-worker-leg/RG1.2b-mean-manager-arm/RG1.3-death/RG1.4-floor, recal-g1/gate_ledger.jsonl:10-14 (ledger not published), the only source of truth for the new-line values; the WOULDTRIP_RULING line :20 records the ruling that would-trip is record-only and has no value; with archive sha + line references. The draft's "six event lines" copied the row count of the G1 verdict table without checking the ledger and would always block a literal preflight; corrected as the verification blocker, with an erratum note written in the G1 case file) / the full text of the AUDIT reports / **the E0 dual-baseline files (king+throne × both endpoints)** / the full text of each ledger: v29-v32, reanchor-r2, infra-b1, recal-g1 (ledgers not published). **The "all ledgers" set of the 304000 reconciliation is exactly this closed W-PIN ledger set** (engineering minor-2, no open-ended "all").
- **W7 artifact pins**: king zip/npz (v28-leg1, lineage 976b6c05…f27f8a), H npz (==DEFAULT_MANAGER_SHA256), M29 npz (89441388…7b66d6), KING_SD (in-case check_teacher_parity 0/1000 re-check). **BC-v1 files (engineering B2, fully rewritten)**: regenerated in the case with the rewritten bc_worker.py -- **regeneration is not optional**: `_validate_bc_report` pins implementation_sha256 == the current implementation bundle, so after E1 changes worker_env.py the old receipt must be refused by preflight (train_ppo.py:499-505/1767-1771; same cause as in v32 S2); the assertions are split: **demos.npz byte sha ≡ 3bf892d611…d9363 (np.savez_compressed bytes are deterministic, measured)**; **policy_sd.pt vs the old `_previous` archived file, tensor-level torch.equal** (torch.save bytes are not reproducible -- saving the same state_dict twice gives different sha values, measured; a byte-equality assertion can never pass and is dropped), the new sha becomes the settled value through the BC_REGEN event, and f052067a… is the old settled value kept only as the lineage reference for the torch.equal comparison; refreshing the provenance field is legitimate (v32 `_previous` snapshot precedent); any failed assertion means CASE_HALT and a report. **BC-v2 files**: collected + trained once in the case; the receipt passes the n₁₂/recall gates (freeze prerequisite, see D2-5); the sha follows the value settled in the ledger, with per-generation accounting.
- **Seed 304000 ledger reconciliation assertion (rewritten, panel minor-3)**: B1's original virginity assertion "does not appear in any leg_start" has been superseded by P8's use of the seed, **and is rewritten as: a scan of the closed W-PIN ledger set asserts that the historical occurrences of 304000 are exactly and only the P8 leg_start (infra-b1), with the reuse exemption written down (same seed, carrying the cross-case note of the P8 triangle reconciliation: the L-base vs P8 difference = the autonomy knob)**; the ban on seed search follows the B1 clause verbatim, and a relaunch may not change the seed. All three legs use the same seed.
- **W-H8 held-out pool discipline**: the 8000 pool gets **1 pair/2 runs in this case, on the winner** (the first case under B1's "at most one pair per case" rule); HOLDOUT_EXPOSURE is counted per run; the registered-side virginity caveat carries over from B1 residual 14.
- **W-C canary scoring set**: the C set and n_D are inherited and pinned from the B1 CANARY_SET event (not re-extracted); the scoring seed set only constrains R-line readings, not the exam surface (canary exams cover the whole 7000-7031 pool).
- **W-LAUNCH launch-order gate (specific to this case; gate position moved later per compliance M-1)**: after FREEZE_SHA and **before the first leg_start of the three legs**, the ledger must hold **a LAUNCH_ORDER event recording the approved launch, with its timestamp**; without it the driver halts and waits (exit code 9, AWAITING_LAUNCH, not a failure state). **The two refs runs (R1/R2) are G0-6 freeze prerequisites and run before FREEZE_SHA and the approved launch** -- this is the faithful execution of the approved state of section 9 of the design memo (not published), not a deviation (refs are reference re-checks, not exams of this case's candidates). **Freeze ≠ launch; panel review passed ≠ launch; no clause of this document may be read as a launch authorization.**

## 5. D2 G0 parallel six-piece proof (compliance M-1, restoring section 9 of the design memo (not published); freeze prerequisites; any failure means no freeze, no launch)

1. **G0-1 dual-endpoint bit-level identity**: p≡1.0 ≡ current skip_dry=True, p≡0.0 ≡ skip_dry=False, reconciled bit for bit per seed per beat against **the king+throne dual baselines captured by E0 before implementation** (frozen policies, 7000-7015; baseline files notarized + sha as frozen constants); the wage identity W≡R−bonus is asserted per window as part of it. **The burden of proof for the distribution endpoints is carried by this item alone** (split from the zero-intrusion proof, per the panel blocker handling). **Bridge-attestation downgrade clause (registered, compliance M-3)**: if any anchor-bridge attestation fails → gold evaluation only reports numbers without comparing to the board, and the title verdict is suspended and reported (v32-style downgrade); D4-1's "title transfer inherited as in v32" requires this item's dual-baseline replay to pass first; old bridge attestations are not inherited automatically.
2. **G0-2a zero-intrusion pair (tensor level)**: the full set of new knobs on (dry_curriculum + bc_aux + dry/fresh offline probe + dry-window behaviour instruments + a12 canary instrument), schedule pinned at p≡1.0, λ_bc=0, vs **the bare-recipe arm = v32-sov command form verbatim incl. `--skip-dry`** (engineering B1, dependency pinned), 2×102,400 steps; policy state_dict + optimizer state, torch.equal per tensor; the smoke seed is a virgin seed, with **the three limbs of the rule pinned (verification correction: the draft's scan semantics had two readings -- :55 used all-event semantics, the B1 precedent used leg_start semantics, and they give opposite answers for the candidate 307000)**: (1) scan surface = **all-event occurrences** in the closed W-PIN ledger set (same semantics as the 304000 clause) **+ the B1 evaluation-pool check limb unchanged (range guard for 7000/8000/9000)**; (2) **under this rule 307000 is no longer virgin** (infra-b1:3 G0_NULLINTRUSION on record), and this case's smoke seed is **pre-registered = 308000** (scanned under rule (1) on 2026-07-19: zero occurrences in all events of all ledgers and in all docs); (3) every G0-2b short run uses the same rule and the same seed 308000 (the with/without runs for rng non-contamination compare on the same seed by definition); not a case-leg seed; the smoke step table is fixed so that calib/ckpt/sentinel/dry-anchor each really fire ≥1 time in the window. **This item is the guarantee that L-base ≡ the v32-sov recipe (E4).**
3. **G0-2b functional smoke test (no equality assertion)**: a p<1 short run asserts that both branches, dry windows entering and leaving the learning distribution, **really happen ≥1 time each** ("proven zero-intrusion" must not actually mean "never ran"); proof of zero contamination by the dedicated rng streams (the episode seed sequence and the training RNG state trajectory of the with/without runs of p draws and demo minibatches equal point by point, including a re-check of the E1 item 3 per-env seeding rule); **a λ_bc>0 short run asserts that the auxiliary CE is really consumed for ≥1 batch and the head-12 gradient is non-zero** (following the calib-probe autograd.grad precedent; anti-P2 zero-path-by-construction fingerprint); **all class-12 demo pairs assert m[12]=True** (engineering M1); **assertion of identity of the measured p sequence (added in verification)**: the p<1 short run records the p value actually pushed per rollout and asserts ≡ the corresponding prefix of the DRY_CURRICULUM_TABLE registry (including leg-relative index anchoring and the `_on_rollout_start` callback order) -- G0-1 only locks the endpoints, G0-2a pins the constant 1.0 table, and this item's original enumeration was orthogonal to p-value correctness, so all three were blind to "wrong anchoring wiring / whole table shifted / constant 0.5" (the adversarial seat constructed the evidence: taking the table by global index clamps to the end of the table from the first rollout, giving a constant 0.5, and all three items stay green); this assertion + the E1 item 2 per-rollout assertion on the real legs close the gap together. **Implementation definition of the rng non-contamination proof (rev4 note)**: a full-trajectory proof that "the training RNG state trajectory is equal point by point" is beyond the visible run outputs -- implementation = identity of the two runs' episode seed sequences (progress.jsonl) + identity of the p draw streams (dry_curriculum.jsonl of both runs, verbatim) + the unit-test family of the derivation functions (zero contamination / disjoint seed domains) + the G0-2a torch.equal backstop on the λ_bc=0 side; the discretion to approximately close the gap goes into the G0_SMOKE limitations slot, and the freeze report lists it to be confirmed (section 12 appendix 2 item 3).
4. **G0-ghost (v12 lesion regression, reusing the v32 probe)**: with belt=0, 12 is still illegal; the `_drain` fallback is not disabled by the curriculum/auxiliary loss; the death-beat termination ladder is unchanged; no drinking after death.
5. **G0-demo pool and per-generation accounting**: BC-v1 regeneration assertion (W7 split form: demos bytes ≡ 3bf892d6… / policy torch.equal against the archived file); the v1 path collection ban on (11,12) unchanged, the v2 path bans 11 and allows 12; the train_ppo loading side asserts per generation (canonical v1 validator unchanged, v2 aux validator separate, v1/v2 schema isolation per E2); all class-12 BC-v2 samples are **real executed beats** in the autonomy-on environment (no counterfactual labels; the assertion removing overridden beats entirely is unchanged); the demo-pool seed-discipline assertion is unchanged; **passing the n₁₂ gate and recall gate (N12_GATE event, with denominator definition and per-game breakdown) is a freeze prerequisite** -- on a shortfall, per D7, **the single registered OC (preventive threshold raised 0.65→0.70) resamples once** (the automatic class-weighting contingency is removed and needs a separate approval -- open item C); still short → no freeze, report for review; never launch with a gate failure (against a v32-F1-style predictable "not used" tier).
6. **G0-6 two in-case refs runs (REF_BITEQ, restoring item 6 of section 9 of the design memo (not published))**: R1/R2 run with the command form of the D3 table, ≡113.0 / ≡140.9 full table per seed per field; **passing is a FREEZE_SHA prerequisite** -- the frozen, waiting-for-launch state carries no unproven drift risk, closing the loop with the preamble's "honest statement on zero drift, effective before freeze"; a mismatch goes to P-refdiv, no downgrade.

## 6. D3 recipes and exam sequence

**Three legs (same seed 304000, launched in series, launch budget 2 each under the ledger). The full per-leg CLI additions are frozen (engineering B1 -- the shared form itself does not produce p≡1.0, so L-base must carry `--skip-dry` explicitly)**:

| Leg | Per-leg additions (authoritative, frozen) | Only variable vs the rung below | Role |
|---|---|---|---|
| **L-base** | `--skip-dry` | (difference from B1-P8 = the autonomy knob, cross-case note) | retraining + autonomy baseline |
| **L-cur** | `--dry-curriculum-schedule <main table>` (1.0→0.5@301,056 linear; 0.5@198,656 flat) | dry_curriculum | course 5 effect alone |
| **L-full** | `--dry-curriculum-schedule <main table> --bc-aux-lambda 0.015625 --bc-aux-demos runs/bc-worker-v2/demos.npz` | 4b package (conjunction: v2 files + λ_bc>0) | **prescribed leg, the leg on which 4b is always judged** |

- Shared command form (v32-sov verbatim + B1 instrument knobs): `--worker --algo mppo --gamma 1.0 --max-steps 3000 --n-steps 512 --num-envs 4 --lr 3e-4 --ent-coef 0.005 --seed 304000 --total-steps 499712 --distill-beta 0.015625 --teacher-sd <BC_SD(v1)> --teacher-override <KING_SD> --manager-npz <M29 npz> --resume-from <king zip> --allow-legacy-resume --calib-probes 3547136,3596288,3645440,3694592,3743744,3792896,3842048,3891200,3940352,3989504 --calib-record-only --ckpt-every-steps 98304 --sentinel-every 49152 --dry-anchor-every 49152 --distill-ce-probe-every 49152 --drywin-metrics-every 49152` (autonomy on by default; the two-flag mutual exclusion is asserted by `_validate_args`; **the last two flags were added by the rev4 correction** -- the cross-checker caught an internal conflict between the original command form and E5 (2) "baseline built from this case's first launch" / the decision 12 obligation to supply course 2 calibration data / RC.14; the dry/fresh distill_ce instrument is approved design decision 5, and leg-side data is its obligation, so by derivation from the approval the "add record-only knobs" option was taken, with zero intrusion guaranteed by the G0-2a live run with all knobs on; listed in the freeze report to be confirmed, see section 12 appendix 2 item 1). nt gate: each leg nt_zip == **3,997,696**; timeout 6h per leg; export with export_worker_npz (built-in parity); **the launcher is always `train/launch_case.sh`**.
- **Canaries (offline checkpoints, record only)**: four mid-leg points +98,304k (3,596,288/3,694,592/3,792,896/3,891,200) × {H, M29} × 3 legs ≤24 runs, whole pool 7000-7031, outside the budget; the mechanically produced 3,989,504 ckpt is archived as usual but not part of the sequence; B1's three anti-peeking clauses (CANARY_SET frozen / record only, no in-leg intervention / a single end checkpoint) inherited verbatim; **the make-up deadline is tightened to before the FIRING_START of the leg's first scoring run (s16)** (compliance M-4, restoring B1's meaning of "before the first scoring run"; the draft's full32 definition is dropped), asserted in the driver; **every canary reading is descriptive; the decision surface = the final exam archives and the release line** (panel M3).
- **Scoring evaluation runs (budget 2 per run; eval_assembled command form verbatim; --manager-npz explicit every time)**:

| Run | tag | worker | manager | Pool | Purpose | Timing |
|---|---|---|---|---|---|---|
| R1 | `<vN>-ref-launch` | king npz | H | 7000-7031 | REF_BITEQ ≡ 113.0 full table | **G0-6 freeze prerequisite, before FREEZE_SHA** |
| R2 | `<vN>-ref-science` | king npz | M29 | 7000-7031 | REF_BITEQ ≡ 140.9 full table | same |
| S1-S3 | `<vN>-{base,cur,full}-s16` | each leg's npz | H | 7000-7015 | quick screen (B1 E1 exemption 1, H only, compliant) | after the launch order |
| F1-F3 | `<vN>-{base,cur,full}-full32` | each leg's npz | H | 7000-7031 | main exam (release definition) | after the launch order |
| M1-M3 | `<vN>-{base,cur,full}-full32-m29` | each leg's npz | M29 | 7000-7031 | dual-manager bundling obligation (E1 confirmed template) | after the launch order |
| H1-H2 | `<vN>-win-hold8k{,-m29}` | winner npz | H / M29 | 8000-8031 | the only bundled pair, on the winner | after the launch order |

- **Preflight and stage order (strictly serial; the driver `train/run_<vN>_content.py` enforces the stages against hindsight)**: **number assigned (after panel review, before the first event) → E0 pre-implementation dual-baseline capture (outside the driver, G0_BASELINE transcription)** → E implementation commit → all W lines pass (W-PIN/W7/304000 reconciliation/W-H8/W-C) → BC-v1 regeneration split assertion → BC-v2 collection + training + N12_GATE → G0 six-piece set (G0-1 … G0-ghost … G0-6 two refs runs) → FREEZE_SHA/CASE_RUNTIME → **[W-LAUNCH launch-order gate, exit code 9 standby]** → three legs in series (L-base→L-cur→L-full; each leg: launch → offline canary sequence → s16 → full32 → full32-m29; canary make-up deadline = before that leg's **s16** FIRING_START) → winner ruling → H1/H2 → R-line scorecard → verdict appendix. The exam-run finality clause (exam_ok/exam_adopted, no .void re-runs) follows v32 verbatim.
- **Compute budget**: 3×~45 min legs + smoke tests 2×~9 min + 13 scoring runs ~20 min + **12 canary ckpt exports ~45 min + ≤24 evaluation runs ~30 min (engineering minor-3, added to the budget)** + 3 end-of-leg npz exports ~12 min (verification correction: the draft's "12 exports" double-counted the canary row and left the end-of-leg exports unassigned; the total band is unchanged after splitting) + BC pipeline ~30 min; the whole case on one machine **≈6-8 hours**, so a 6h timeout per leg is ample.

## 7. D4 criteria and verdict clauses (all pre-registered, frozen for the case)

**Interval conventions / borderline clause / hard statistical-discipline constraints (the mean always comes with median + sign test + deleveraged mean, and the 7017 leave-one-out reading is mandatory alongside; dual-manager view of the damage surface; the MS≈0 caveat) / horizon statement (max-steps 3000, frozen, mandatory with the verdict) / H-track default and non-retroactivity clauses -- inherited from B1 D3. The family-level reading clause is restated with this case's numbers (statistics minor-3): this card reads 14 bands (RC.2/3/4/8×2/9×3/10/12/13×2/15×2); ≤3 scattered out-of-band lines is normal (similar to B1's ratio of ≤3 out of about 17 bands); no cherry-picking. Note on point estimates (statistics minor-6): every R-line point estimate without a stated source is discretionary, not an estimate; those with a prior basis are noted one by one**. Training reward is blind to this failure (−6% vs −34) and is never a criterion; every reading uses the final evaluation archives.

1. **Release criterion = first use of the G1 new lines, full conjunction recorded**: the winner's full32 (H) paired mean difference vs `<vN>-ref-launch` ≥ **+17.32** (worker-leg family, **the "single-vector line" label is mandatory**) ∧ wins ≥ **22/32** ∧ died ≤ **8** (**the "K1 single-archive dependency" label is mandatory**) ∧ floor (85/92)×R ∧ sentinel limbs untouched (level change/cap/override, G1 L1 untouched list) ∧ not voided. **Per-leg eligibility written out (compliance m-4)**: eligibility = died≤8 (same-value replacement of G1 L2-0 (a)) ∧ sentinel limbs untouched ∧ not voided; the "eligible legs" of the D4-5 winner clause are exactly these. **Multiple-comparison listing obligation (statistics M-3, G1 L1a form)**: the freeze report and verdict appendix must list -- the per-candidate single-limb margin α as registered in G1; with 3 arms deciding, **effective α ≈ 1−0.9749³ ≈ 0.073** (width-limb margin 0.0251); joint α derived for 3 arms ≈ **0.013** (G1 joint 0.0045 basis); a residual note alone is not enough, the listing goes into the table. **The abandon gate (75/112.4)×R is pinned here as applying to this case** (a content case with a campaign number; B1's "a measurement case has no abandon gate" does not automatically apply); the abandon gate and the eligibility gate only block release and the title context, **not the criterion 2/3 scientific main verdicts** (judged as soon as the archives are written). **First-use fulfilment**: the freeze report lists a separate "new-line adoption" item (G1 D7-6); the panel gives separate upgrade-review opinions on the two labelled limbs, single-vector line / K1 (decision 6); the verdict appendix back-fills the operating characteristics of the first live use of the new lines; **advance notice of the gold-evaluation line asymmetry (decision 9)**: died≤8 does not extend to the gold-evaluation P lines (gold evaluation keeps deaths ≤6, G1 L2-0), so a winner with died∈{7,8} passing the release line while gold evaluation structurally falls back a tier ("failed succession") is an expected case. If released, the gold pool is burned once (campaign-level notice rule, decision 9 acknowledged); the title-transfer clauses follow the v32 D3 anchor bridge and the written transfer rules, **require the G0-1 dual-baseline replay to pass first, with the downgrade clause in D2-1**. Eligibility-gate note: if the winner is disqualified only by cap_rate, the disqualification stands + the "candidate gate-semantics drift" note + τ̄ difference vs ref and the per-window distribution (R5).
2. **Course 5 main verdict (re-established, statistics B-2/M-2)**: **the only decision quantity = F-lock composite signature v1 hit count; the pure course 5 decision leg = L-cur×H** (consistent with the ladder attribution grammar: course 5 effect = L-cur − L-base). **Anchor = measured L-base hit count in the case** (prior point 5 from P8 -- P8 is two variable levels away (autonomy + same seed origin but different autonomy), so it is demoted to a listed prior and is not the anchor). **Decision lines (pre-registered, with baseline branches)**: measured L-base ≥5 → L-cur ≤ L-base−2 = direction success; = L-base−1 decrease within the noise band (descriptive); ≥ L-base = no improvement. L-base = 4 → success line ≤2, with the "low baseline reduces discriminating power" note. **L-base ≤3 → the hit-count main verdict reads "floor effect, undecidable"**; the course 5 main verdict gives no pass/fail and moves to the dry-window behaviour instruments (E5 (2)), D-window retention and τ distribution, recorded as is for a follow-up case. **The L-full hit count must be listed alongside but is a combined course 5 + 4b reading and never enters the course 5 decision; if the L-cur and L-full hit counts disagree, list both as is, never aggregate**. **7017 structural slot note (statistics minor-5)**: the baseline hits include the 7017 deep-water devil seed slot (every continued-training leg loses it), so the effective movable slots = baseline count minus 7017 if listed; the note is mandatory with the verdict. D-window retention (RB.8 formula verbatim, C set from B1 CANARY_SET), τ/window distribution, OBS_DRIFT window-end walkSum/121 and the south-west band (RB.10 schema verbatim) **are all demoted to mandatory descriptive readings listed alongside, aligned with RC.10; the wording "close to the throne baseline" is dropped** (statistics M-2, removing reading freedom). **Firewall: F-lock decides with the current v1 construction constants; v2 goes back as an auxiliary delivered instrument and never enters this case's decision lines**; the "signature specificity uncalibrated" and horizon caveats are mandatory. **Identifiable signal inside dry windows, pinned (audit tables)**: death penalty 1/3,413, clearing remaining monsters a 0.3-3.4% tail event, the −0.002 per beat wall-facing penalty as the dense body, median wage −0.03 / 93.6% negative / 2-9% fat tail -- "learned shape vs random shape" can be told apart by the dry-window behaviour instruments before the final exam, record only.
3. **4b main verdict (a12 behaviour) = L-full is always judged, win or lose; control = L-cur (same autonomy, same curriculum, no demo path). Tier table re-registered (compliance B-1: follows the v32 form, with constants re-registered for this case's context; exhaustiveness guaranteed by the complement tier; decision order tier 1 → tier 2 → tier 3)**:
   - **Main line**: L-full×M29 a12/game ≥ **0.1** (A12_USE_LINE, the registered v32 constant; discrete meaning ≥4 real drinks in 32 games);
   - **Tier 1 (injection converted into survival)**: a12/game ≥0.1 ∧ L-full×M29 died ≤ L-cur×M29 died ∧ paired mean difference (L-full−L-cur, under M29) ≥ **−2** (the ±2 noise band follows the v32 form; the triangle-reconciliation note that the same seed only guarantees initial equivalence applies); low usage 0<a12/game<0.1 that meets the survival/mean limbs falls into tier 2 by the tier order, and the verdict must note "low usage and improved survival appear together; attribution unproven" (v32 verbatim); **counterpart of the retraining-regression safeguard**: the L-base×M29 reading is mandatory alongside, and if L-cur and L-base survive equally, the causal "conversion" caveat is tightened;
   - **Tier 2 (examples given but not used)**: a12/game < 0.1 ∧ paired mean difference (vs L-cur, M29) ≥ −2 ∧ died ≤ L-cur×M29 died + 1 (one-life noise band, v31 precedent) -- **this tier is the v32 F1 explicit expectation and the primary risk case this case admits under R11/residual 4; the draft's three-tier table had no place for it, a grammar break, now restored**;
   - **Tier 3 (complement tier, guarantees exhaustiveness)**: every other case, with sub-cases (a) used but not converted (a12≥0.1 but the survival/mean limbs are not met) and (b) not used and worse;
   - The verdict is worded per sub-case, never by the tier name alone; the difference in the set of dead seeds is listed seed by seed; the binomial note (the died tier is a directional reading, not evidence of significance) applies; the reading under H is mandatory alongside, with the "structurally tight" caveat (ref depth2 7 vs 15), not a separate line; decoupled from the release criterion. **Control crosses the line (statistics M-1, pre-registered)**: the L-cur a12/game reading is mandatory alongside; if L-cur a12/game ≥0.1 → the verdict must carry "curriculum-only revival candidate (compound survival path, same family as v32 residual 2); 4b package attribution downgraded to 'gain unproven'"; the tier is still decided, but the attribution sentence is reworded accordingly. The canary a12/game sequence (R11 re-extinction dynamics) is always descriptive; this criterion is the final-exam backstop.
4. **Attribution grammar (pinned)**: course 5 effect = L-cur − L-base; 4b effect = L-full − L-cur; the verdict must carry the caveat "4b package = the conjunction λ_bc>0 + v2 files" and the R7 teacher-generation mixing note (with the v25 warm lineage note); the triangle reconciliation follows the v32 R32 split definition (the note "the same seed only guarantees initial equivalence; 'the only variable' is a configuration-level statement, not trajectory pairing", verbatim); **RC.3 and RC.4 share L-cur, so their errors are negatively correlated by construction (their sum ≡ L-full−L-base); they must not be stacked as independent evidence** (statistics minor-4). **Exit syntax of the decision 12 ladder clause**: the 4b main verdict lands in tier 2 or tier 3 → the verdict is fixed as "referred to a new design review (retry with a different mechanism, or honestly declare extinction and release)", no prejudgment, no automatic transfer to another case.
5. **Winner clause**: winner = the eligible leg (defined in criterion 1) with the highest full32 (H) mean; pre-registered tie-break order: difference ≤0.05 → fewer deaths; still tied → L-full (the prescribed leg first); if the highest mean is blocked by eligibility → next in line (substitution recorded). The quantitative listing of the type-I error inflation of the max-of-3 order statistic is in criterion 1 (α table); the v29 residual #6 family note goes with the verdict. **Release (winner) and the 4b scientific main verdict (L-full always judged) are decoupled.**
6. **Dry-anchor increment: descriptive, not a criterion limb** (the G1 D6 direction reversal is on record; the +8.35pp healthy-leg counterexample note is mandatory); the dual references 0.7515/0.6305 are recorded as is (the probe set uses the same BC-v1 demos bytes for all three legs; guarantee chain in E6), collecting healthy/damaged-leg discrimination samples for a future gating case (material for the "≥2 healthy legs" prerequisite of the G1 re-discussion, which this case happens to produce).
7. **Auxiliary deliverable: composite signature v2 redesign** -- recalibrated on material from six legs: this case's three legs + P8 + the two v32 legs (draft: "Δ after pool-median adjustment + M29 recovery-magnitude limb"); **the three legs' material is a product of max-of-3 selection, so any specificity calibration must carry the selection-bias note**; an instrument, never in a decision line (the dual of the firewall in criterion 2).

## 8. D5 P lines and exit codes

- **P1 top-level exception/interruption** → DRIVER_EXCEPTION + NEEDS_ATTENTION; the two conditions for idempotent adoption on resume follow B1.
- **P2 evaluation run budget**: 2 per run under the ledger; the -b closed enumeration follows R2 D4-P2 verbatim; leftover archives RESIDUE_SEALED.
- **P3 leg launch budget**: 2 launches per leg under the ledger (counted by leg_start, 6 for the three legs); if the first launch falls short (rc≠0 or nt≠3,997,696), it is an operational failure, halt for review, and a restart may use the second launch (_prepare_run_dir archives leftovers; the resume source is always the king zip); **budget exhausted → OPERATIONAL_FAILURE, whole case halts; never hand-edit the ledger to keep it alive; a relaunch may not change the seed**. A failed npz export uses the make-up export channel (no budget used).
- **P4 preflight failed** → PREFLIGHT_FAIL (exit code 3).
- **P5 plan A halt**: budget for a mandatory run exhausted → whole case halts; scientific readings already archived are carried into a follow-up case under the B1 chained-inheritance clause.
- **P6 chained re-freeze**: multiple FREEZE_SHA events (prev_sha + REFREEZE_REASON).
- **P-canary**: B1 verbatim (a failure is recorded only and the leg continues; make-up deadline = before that leg's s16 FIRING_START; at most one archive per ckpt×manager).
- **P-refdiv**: REF_BITEQ (G0-6) mismatch → drift triage (full re-check of CASE_RUNTIME/W5/W6) → CASE_HALT_G0 (7) or REF_DIVERGENCE halt for review (8), no downgrade; **the mismatch happens before freeze, so the frozen state carries no unproven drift risk**.
- **P-N12**: BC-v2 gate not passed → **the single registered OC (threshold 0.65→0.70) resamples once**; still not passed → no freeze, report for review (pending a decision once the decision 4 contingency is exhausted; class-weighted sparring was approved as plan A (2026-07-19, section 12 appendix 3), which settles the original "nothing may stand in for it while pending" clause); not a failure state, recorded as NEEDS_ATTENTION. **Clause on the timing of early OC use (added in verification)**: when the first gate fails and the single OC is used, the OC_CONSUMED event must carry the note **"convergence rule pending confirmation (open item C)"**; if launch review 2(f) rejects the convergence rule, the frozen case **moves automatically to a P6 re-freeze pending a decision** -- the rework path is written in advance, so early use is not a fait accompli.
- **Exit code table**: 0 case closed/idempotent; 2 budget exhausted; 3 preflight; 4 not idle/lock conflict; 5 W5 pre-launch drift; 6 runtime drift during the case; 7 CASE_HALT_G0; 8 REF_DIVERGENCE; **9 AWAITING_LAUNCH (FREEZE_SHA recorded, LAUNCH_ORDER not recorded: standby state, not a failure, no NEEDS_ATTENTION)**; anything else P1.

## 9. D6 exposure accounting

| Pool | Exposure in this case | Accounting |
|---|---|---|
| 7000 probe pool | **+11 scoring runs** (refs 2 [before freeze, G0-6] + s16 3 + full32 × dual manager 6) + ≤24 canary runs (outside the budget; cumulative peeking recorded as is, C ⊂ 7000 pool) | PROBE definition per B1 residual 12 |
| 8000 held-out pool | **+2 runs (1 pair, on the winner)** -- first case under B1's "at most one pair per case" rule | HOLDOUT_EXPOSURE counted per run |
| 9000 gold pool | 0; **burned once only if the winner passes the G1 release line** (decision 9 acknowledged; advance notice of the died∈{7,8} failed-succession asymmetry) | GOLDEN_AUTHORIZED carries the notice source |

## 10. D7 honest-boundary table (per constant; submitted with the freeze report for line-by-line panel review; full table goes into the verdict appendix)

| Constant/definition | Value | Source and ownership | Were the consequences foreseeable when chosen / would the principle reverse if the data reversed |
|---|---|---|---|
| Main annealing table | 1.0→0.5@301,056 + 0.5@198,656 | **fixed by the approval (decision 2 addendum)**; audit Q4 evidence (fresh-window gradient share ~51% at p=0.5) | the conservative direction can be extended but not retreated; if canaries look healthy, a later case lowers p_end further |
| p=0 definition | episode share 93% (not 76%) | **audit correction 2**; the frozen text uses the correct definition | the real number is more extreme, strengthening the main-table argument |
| λ_bc | 0.015625 | **registered discretionary constant** (same magnitude as the distillation anchor β, not overpowering the existing anchor); a new hyperparameter with no calibration precedent | consequences not foreseeable (emergent); G0-2b only proves the path is non-zero; the magnitude effect is carried by L-full−L-cur |
| λ_bc limited to class 12 | main plan; all classes as the OC | **locked in by the audit**: BC-v1 is a pure single-class set of idle a9 presses, so no class limit would amplify the idle presses | strongly supported by the data; the OC row is kept for the panel |
| Preventive threshold | hp∈[0.5,0.65)∧belt>0 | draft value from the design memo (not published); audit: the in-band distribution is smooth along belt, no sign of existing clustering distortion | new constant; the knock-on effects of teacher v2 are an emergent unknown (gap f) |
| Hysteresis guard | ≤1 preventive drink per window, **a settled clause of the main plan** | the design memo (not published); **n₁₂=244** is the faithful point estimate under this design (the draft's typo n₂ corrected) | ownership: design discretion, preventing the 0.5/0.65 chain distortion (R12) |
| n₁₂ gate | **≥122** (= half of the audit point estimate 244) | **registered discretion**; audit Q3 (244 < 300, the current per-class recall gate does not engage) | **single registered OC: raise the threshold 0.65→0.70 and resample once**; class-weighted sparring approved (plan A, section 12 appendix 3); rehearsal: old spec 62@0.65/73@0.70 both fail → plan A 157@0.65 (n₁₂ passes, recall short by 0.033) → **199@0.70 both gates PASS**, and the official S-bc2 is expected in the same order; if still short, no freeze, report for review |
| Recall gate | held-out class-12 recall **≥0.5**; **denominator = labelled a12 states of the teacher-v2 hysteresis simulation (window-start trigger states); metric = argmax hits; fail-closed** | **registered discretion** (pinned by statistics B-1); the audit shows the denominator is non-zero and measurable | a first-time gate without historical calibration; **the verdict must carry the "denominator cluster" note (state definition: 74.7% concentrated in games 178/187; the labelled-window definition follows the N12_GATE per-game breakdown, verification correction)**; N12_GATE carries the per-game breakdown |
| Course 5 main-verdict anchor | **measured L-base hit count in the case** (prior point 5 from P8 listed alongside); success line ≤anchor−2; baseline branches 4→≤2 / ≤3→undecidable | **registered discretion (statistics B-2 changed the anchor)**; P8 is two variable levels away and is not the anchor | all branches pre-registered, no look-then-decide; 7017 structural slot note applies; **changing the anchor changes how the decision 2 draft value is pinned; submitted for review (open item A)** |
| 4b tier decision lines | mean-difference limb ≥−2 (vs L-cur×M29 pairing) / tier 1 died not worse / tier 2 died ≤ctrl+1 | **registered discretion; v32-form constants re-registered** (the original v32 constants were registered against ref-science and cannot be carried over verbatim) | first registration against a relative reference, uncalibrated; the borderline clause applies; **submitted for review (open item B)** |
| A12_USE_LINE | 0.1 (≥4 real drinks in 32 games) | registered v32 constant, verbatim | not recalibrated under the autonomy protocol; the borderline note clause applies |
| F-lock decision constants | current v1 construction | **firewall**: v2 goes back as an instrument; B1 CRITERION_VALIDATE precision 0.0 on record | the uncalibrated-specificity caveat is mandatory |
| Multiple-comparison listing | effective α for 3 arms ≈0.073 (width-limb margin 0.0251); joint α derived for 3 arms ≈0.013 | **G1 L1a form obligation (statistics M-3)** | a listing, not a decision line; back-filled in the verdict appendix |
| Seed | 304000 (same seed for all three legs) | P8 reuse; **the assertion is rewritten as "in the closed W-PIN ledger set the historical occurrence is exactly the P8 leg_start" reconciliation + written exemption** | no seed search; bonus of cross-case triangle reconciliation |
| BC-v1 file assertions | demos bytes ≡ 3bf892d6…d9363; policy torch.equal against the archived file | **engineering B2, measured live** (torch.save bytes not reproducible; np.savez bytes deterministic); f052067a… is the old settled policy value | split assertions are machine-executable; BC_REGEN records the new policy sha |
| The four G1 new-line constants | +17.32 / 22/32 / ≤8 / (85/92)×R | **cited values** (LINE_DERIVED line references); the single-vector line / K1 single-archive dependency labels travel with them; panel upgrade review | first use is the first exam; operating characteristics back-filled in the verdict appendix |
| Canary point table | +98,304k×4; make-up deadline = before that leg's s16 FIRING_START | B1 closed point table + restored deadline clause (compliance M-4) | closed by pre-registration, no picking the best |
| Manager-generation confound g | demo collection manager v22-H; training manager v29-mfresh | **audit gap g recorded as is** | a confound on record, not fixed in this case; the verdict carries it as is |
| Audit gaps a-i | per-window wages unavailable (a) / per-beat pairs (b) / training-side dry-window reasons (c) / non-stationary ff counts (d) / drains distribution (e) / divergence after teacher-v2 triggers (f) / manager generation gap (g) / window_econ cross-generation (h) / dry-window instrument baselines missing (i) | **section 5 of the AUDIT filed in full** | if gap a needs measured window-level wages, the replay probe must be extended and rerun -- a new launch needing separate approval, not included in this case |
| Dry-window signal surface | median wage −0.03 / 93.6% negative / 2-9% fat tail / death penalty 1/3,413 | **pinned by audit correction 3** (three proxies: window_econ_v23 + G0 corr −0.87 + reward-decomposition band) | window_econ is a cross-generation archive; the generation-gap caveat travels with it (gap h) |
| Teacher dry-window behaviour | pure idle a9 presses + fuse a10 (removed from the demo set); "mostly presses 10/13" withdrawn | **audit correction 1**; wording frozen to the current generation | generation drift on record (the a10 class died out between generations) |
| The two s_dry definitions | measured band [13,25] (audit §1.2); extrapolation grid points 16/20/25, band [16,25], midpoint 20 (audit §4) | **the two definitions noted separately (integrated ruling adopting statistics minor-2 + compliance m-6)** | gradient-share estimate band ±5.5pp (45.2–56.3%); the draft's ±11pp mistook the full width for the half width, corrected |

## 11. D8 ledger and R-line scorecard

Ledger `train/runs/<vN>-content/gate_ledger.jsonl` (number assigned first, see the numbering note; B1/v32 vocabulary inherited + new words in this case: G0_BASELINE / DRY_CURRICULUM_TABLE / BC_REGEN / BC_V2_RECEIPT / N12_GATE / G0_ENDPOINT / G0_SMOKE / DEMO_LEDGER / A12_CANARY / DRYWIN_METRICS / **LAUNCH_ORDER** / NEWLINE_ADOPT / OPERATIONAL_FAILURE).

**R lines (falsifiable, with point estimates and bands; interval/borderline clauses per B1 D3; family-level clause restated for this card's 14 bands; point estimates without a stated source are discretionary, not estimates; marked [decision] / [record only])**:

- **RC.1 [freeze prerequisite (G0-6)] two REF_BITEQ runs**: ≡113.0 / ≡140.9 full table (a point, not a band); a mismatch goes to P-refdiv.
- **RC.2 [record only] L-base full32 (H) paired mean difference**: point **−12**, band **[−45, +5]** (priors: v32-sov −33.8@303000, P8 −5.37@304000, sov−ctrl same-seed −13.07 noise band).
- **RC.3 [record only → attribution] course 5 effect (L-cur − L-base, same-seed pairing)**: point **+4**, band **[−12, +25]**; below the band → course 5 harm candidate (a reverse of the v26 operation showing up); the verdict is worded per cell, never lumped as "pass/fail"; **not stacked with RC.4 as independent evidence (D4-4)**.
- **RC.4 [record only → attribution] 4b effect (L-full − L-cur)**: point **+2**, band **[−12, +18]**; the conjunction caveat is mandatory.
- **RC.5 [decision · course 5 main verdict] F-lock v1 signature hit count**: the measured L-base value is the anchor (prior point 5 from P8, 7017 structural slot note); **decision leg = L-cur: ≤anchor−2 direction success / anchor−1 noise band / ≥anchor no improvement; anchor=4 → success line ≤2; anchor≤3 → floor effect, undecidable**; the L-full hit count listed alongside (combined reading, not in the decision); uncalibrated-specificity + horizon caveats apply.
- **RC.6 [decision · 4b main verdict] L-full×M29 a12/game**: point **0.8** (discretionary; v32 R32.3 pre-registered point 1.5, **measured 0.0** (F1: autonomy never exercised, recorded in the R32_MAIN ledger) -- this case is the first non-zero expectation after injection, **with no measured precedent**; the draft's "measured 1.5" was a hallucinated citation, corrected in verification and recorded with the same kind of error as "the teacher presses 10/13"), band **[0, 8]**; main decision line 0.1 (discrete: ≥4 in 32 games); **L-cur a12/game mandatory alongside; ≥0.1 triggers the control-crosses-the-line branch (D4-3)**; the H side listed alongside, not a separate line; tier grammar per the re-registered D4-3 table.
- **RC.7 [decision · release] the winner against the four G1 numeric limbs**: see D4-1 (the point is the line value; full conjunction recorded; α listing obligation with the document).
- **RC.8 [record only] died per leg (H side)**: point **2**, band **[0, 8]**; M29 side point **5**, band **[0, 10]** (v32 ctrl-m29 died 8 / same source as R32.9); the per-seed died vector is mandatory with the verdict (R1).
- **RC.9 [record only] dual-manager reading difference (m29 − full32 per leg)**: point **+30**, band **[+8, +55]** (inherited from B1 RB.6). **Two MS branches pre-registered (statistics M-5; the branch is triggered mechanically by where the measured RC.2 lands, no choosing a band afterwards)**: healthy branch (RC.2 ≥ −20): MS max band **[5, 45]**, over-line count **[0, 8]**; damaged branch (RC.2 < −20): MS max band **[45, 130]**, over-line count **[8, 17]**; the MS caveat applies.
- **RC.10 [record only] canary D-window retention (+393,216 point, leg×H, C set)**: **two branches triggered as in RC.9**: healthy **[0.5, 1.0]** / damaged **[0.0, 0.5)**; always descriptive.
- **RC.11 [record only] canary a12/game sequence (L-full)**: expected shape = non-zero after injection, with a risk of decay in the middle/late part (mirror of R11 v14/v15); no prior band, recorded point by point as is.
- **RC.12 [record only] dry-anchor increment (zero point 0.7515)**: point **+4pp**, band **[0, +10pp]** (the healthy P8 +8.35 has rewritten the prior; G1 D6 reading: it never had normative meaning).
- **RC.13 [freeze prerequisite] BC-v2 receipt n₁₂**: point **244**, band **[122, 700]**; gate ≥122; held-out recall point **0.8** (discretionary), band **[0.5, 1.0]**, gate ≥0.5 (denominator/metric/fail-closed in E2/D7; the cluster note goes with the verdict).
- **RC.14 [record only] dry-window behaviour instruments (dry-state action entropy / a distribution / fresh-window wages and width τ̄/depth)**: **baseline built first in this case, no prior band, recorded as is** (the starting point for closing audit gap i); the dry/fresh distill_ce split (offline probe) likewise; it is the delivery of course 2 calibration data.
- **RC.15 [record only] 8000-pool winner pair**: paired mean difference vs K1 point **−10**, band **[−50, +10]**; depth2 count point **5**, band **[2, 14]**.

## 12. Residual uncertainty

(1) The three legs are the product of max-of-3 selection -- **the type-I error of the release line is listed quantitatively (effective α for 3 arms ≈0.073 / joint ≈0.013, D4-1)**, and the selection-bias note goes with the signature v2 calibration; (2) λ_bc is a new hyperparameter without precedent; G0-2b only proves the path exists, not that the magnitude is right; (3) n₁₂=244 is a faithful first-trigger estimate under the hysteresis design; trajectory divergence after a teacher-v2 trigger is unavailable in principle (gap f), so the real BC-v2 collection can fall below the point estimate -- guarded by the gate and the single registered OC; (4) the a12 injection support can still be driven extinct again by negative PPO advantages (R11, mirror of v14/v15); canary instruments only observe and never rescue, and the final-exam criteria are the backstop (tier 2 now has a place, D4-3); (5) the annealing table is a static extrapolation; with p<1 the worker takes over dry windows, which changes the sequence of windows the manager meets (gap d); (6) the two s_dry definitions: measured band [13,25] (§1.2), extrapolation grid 16/20/25 (§4), borrowed across managers (training v29-mfresh vs evaluation H), **fresh-window gradient share estimate band at p=0.5 ±5.5pp (45.2–56.3%)**; (7) the teacher generation change is an F2 regression candidate on record, and the continued-training + teacher-change family carries the v25 warm self-destruction precedent -- the ladder legs absorb it, but it cannot be split further within the single L-full leg; (8) the manager-generation confound g is on record and not fixed; (9) contract rev5 means old checkpoints will be refused for non-legacy continued training (a conservative known cost); (10) first use of the G1 new lines: the first-exam operating characteristics of the single-vector line / K1 single-archive dependency are unknown, with a back-fill obligation on record; (11) 7017, the standing deep-water devil-seed control, is expected to be lost entirely (−150-level leverage); the deleveraged reading is mandatory alongside, and the criteria may not be changed for it; (12) cumulative peeking at the 7000 pool deepens by +11 runs; the 8000-pool pair is the hedge; (13) the dry-window behaviour instruments have no baseline; within this case they can only show internal consistency, not a norm; (14) M29-side evaluation determinism is still presumed (B1 residual 15 not settled), and the presumption note goes with every M29 verdict; (15) horizon sensitivity (max-steps 3000) conditions every signature and criterion; it is frozen, and any change requires re-citation and re-review; (16) (new) the 4b tier decision lines and the course 5 main-verdict anchor are first registrations against a relative reference / in-case measurement, without cross-case calibration; the borderline clause and noise bands are discretionary; (17) (new) the L-base hit-count anchor is a single measured point whose own draw noise is uncalibrated; the baseline branch clause is its hedge.

## 12 appendix. Full table of open items A-G (written in during verification, so every citation has a target)

The targets of the six "open item X" citations in the text are as follows (caught by the adversarial verification seat: the draft had citations without an enumerated target, items D/E/F never appeared in the document, and the mapping to launch clause 2 (a)-(g) was misaligned); this table is the only resolution source:

| Item | Content | Ownership and timing | Mapping to launch clause 2 |
|---|---|---|---|
| A | Course 5 main-verdict anchor changed: the measured L-base hit count in the case is the anchor + baseline branches (changes how the decision 2 draft value is pinned; statistics B-2) | decided at launch review | 2(e) |
| B | Re-registered table of the 4b tier decision-line constants (D4-3/D7; the original v32 constants were registered against ref-science and cannot be carried over; compliance B-1) | decided at launch review | 2(d) |
| C | Confirmation of the N12 OC convergence: the automatic class-weighting contingency removed, single registered OC = raise the preventive threshold 0.65→0.70 and resample once (statistics M-4) | **closed (plan A approved 2026-07-19, section 12 appendix 3)**: class-weighted sparring approved and recorded, collection x3 approved with it, the OC kept as the registered fallback; the official S-bc2 using the OC is the order expected under the approval, and launch review 2(f) becomes a review of how the approval was executed | 2(f) |
| D | Verdict form of the control-crosses-the-line branch: L-cur a12/game ≥0.1 → 4b attribution downgrade grammar (statistics M-1, already pre-registered in D4-3) | pinned by pre-registration; presented at launch review with the tier table | with 2(d), no separate sub-item |
| E | Conclusion of the upgrade review of the two G1 labelled limbs (single-vector line / K1 single-archive dependency) (the G1 D7-2 obligation of decision 6) | **fulfilled (2026-07-19)**: an independent reviewer's separate opinion is filed as docs/forensics/AUDIT-content-case-G1-dual-label-review.md -- both limbs **hold, with conditions**; conditions = the verbatim text of G1 verification notes 1 and 2 + deleverage_q95 +15.99 + the g1_results.json sha pinned into the freeze report and the NEWLINE_ADOPT event (that review's 8+7 mandatory caveats and back-fill obligations take effect with the verdict); the second erratum note in the G1 case file written at the same time | merged into 2(a); the full review accompanies it |
| F | Three items for review: new-line adoption (G1 D7-6 first use) / measured n₁₂ gate and pass result / reconfirmation of the died∈{7,8} failed succession | reviewed at launch review | 2(a)/2(b)/2(c) |
| G | Decision on the degenerate two-leg plan: if the panel finds the conjunction caveat unacceptable, cut L-base and run two legs, shrinking leg budgets and the exam table proportionally with a re-freeze (pre-registered in section 2) | triggered by a condition, decided at design review; the pre-registration does not trigger it automatically (the rev3 panel did not trigger this branch) | no mapping (a pre-freeze branch, not a launch-review item) |

Note: launch clause 2(g) (recording as is that the two refs runs came before the approved launch) is a recording obligation of the operator and has no capital-letter item.

## 12 appendix 2. Implementation-period plan corrections and items to confirm (rev4; handling record of the cross-checker's 3 major + 3 minor)

| Item | Content | Handling | Freeze report |
|---|---|---|---|
| 1 | The D3 shared command form adds `--distill-ce-probe-every 49152 --drywin-metrics-every 49152` (major-3: an internal conflict between the original command form and E5 (2) / the decision 12 course 2 data-supply obligation / RC.14; the obligation side of approved design decision 5; by derivation from the approval the "add" option was taken, with zero intrusion guaranteed by G0-2a) | plan correction + driver leg command form synced | **to be confirmed** |
| 2 | E5 (3) per-game definition (episodes_with_a12/a12_max) downgraded: evaluation archives have no per-game action histogram, A12_CANARY records the aggregate definition, and the registered schema is kept for a later case (minor) | plan note | listed separately for review |
| 3 | Implementation definition of the G0-2b rng non-contamination proof: episode seed-sequence identity + p stream identity + unit-test family + G0-2a backstop; the approximate gap closure goes into limitations (minor) | plan note + driver adds the episode seed-sequence assertion | **to be confirmed** |
| 4 | Three BC-v2 demos byte assertions added (major-1: the v2 validator's measured byte assertion mirroring the policy side / the driver identity chain's .get fallback changed to a hard failure / before the L-full launch, sha ≡ the N12_GATE settled value) | code addition | recorded as is |
| 5 | G0-2a evidence that the instruments really ran extended to the sentinel/dry-anchor limbs (major-2: all four checked; "proven zero-intrusion must not mean never ran") | code addition | recorded as is |
| 6 | DRY_CURRICULUM_TABLE recorded at full precision (minor: round(p,10) was not identical to the re-check source below 1e-10; switched to a lossless json float round trip) | code addition | recorded as is |

## 12 appendix 3. Plan A (2026-07-19, handling the double N12 gate failure)

**Facts**: the first live BC-v2 collection failed both gates (entirely in an off-ledger rehearsal; the main ledger is clean and the official OC was not used). Two root causes, both pre-registered risks that came true: (1) **gap f confirmed** -- the audit point estimate 244 was a hysteresis simulation on old trajectories of a teacher that did not drink; once the real teacher drinks, health recovers, the heavy trigger games 178/187 disappear, and labels thin out to 1-3 per game; (2) the drink class is 0.04% of 160,000 demo pairs, so unweighted sparring cannot learn class 12 by construction -- while the real injection path λ_bc feeds the class-12 subset alone ≡ the limit of class weighting, so the gate's proxy used the wrong yardstick.

**Options and decision**: three options were put up (A: collect x3 + class weighting, gate values unchanged; B: a new review of the gate lines; C: postpone), and **plan A: collect x3 + class weighting (recommended)** was approved on 2026-07-19. Open item C is closed with this.

**Full table of rehearsal readings (off-ledger)**:

| Spec | Threshold | n₁₂ (gate ≥122) | recall_12 (gate ≥0.5) | top1 | Result |
|---|---|---|---|---|---|
| old (128 games, unweighted) | 0.65 | 62 | 0.0 | 1.000 | FAIL |
| old | 0.70 | 73 | 0.0 | 1.000 | FAIL |
| plan A (384 games, class-weighted) | 0.65 | 157 | 0.4667 | 0.9884 | FAIL (short by 0.033) |
| plan A | 0.70 | **199** | **0.5263** | 0.9816 | **PASS** |

**Expected official order (S-bc2)**: the 0.65 main setting FAILs → OC_CONSUMED (with the note) → 0.70 resample PASSes; collection and training are byte-for-byte deterministic, so the rehearsal is purely a preview with zero judgement freedom (all gate lines frozen and unchanged).

**Same-day supporting evidence**: (1) all three S-bc1 rehearsal assertions PASS -- after the E1 code change the v1 demos byte sha ≡ the frozen constant verbatim and the policy reproduces f052067a… bit for bit, **so the W7 claim "regeneration must be identical" holds live**; (2) the smoke bare arm's preflight refusing the old v1 receipt (implementation bundle identity mismatch) is the W7 "regeneration is not optional" clause coming true, not damage; recorded.

## 13. Launch clauses

1. Once this document passes panel review, **the G0 six-piece set is all green (including the two G0-6 refs runs)** and FREEZE_SHA is recorded, the case enters the **frozen, waiting for launch** state; the campaign number is assigned after panel review (numbering note).
2. **Launch deferred until the training machine was available; the only valid launch order is an explicit approval**, recorded by the operator verbatim as a LAUNCH_ORDER event (with its text and timestamp) in the ledger; the freeze report lists **seven items** for review: (a) **the "new-line adoption" item** (G1 D7-6 first use, carrying the single-vector line / K1 labels and the full upgrade review (docs/forensics/AUDIT-content-case-G1-dual-label-review.md, both limbs hold with conditions); pinning its two conditions -- the verbatim text of G1 verification notes 1 and 2, deleverage_q95 +15.99 and the g1_results.json sha -- into this item and the NEWLINE_ADOPT event is a reporting obligation); (b) the measured n₁₂ gate value and pass result (including whether the OC was used); (c) reconfirmation of the died∈{7,8} failed-succession asymmetry (decision 9 already acknowledged, restated at launch); (d) **the re-registered table of 4b tier decision-line constants** (D4-3/D7, compliance B-1); (e) **the changed L-base anchor and baseline branches of the course 5 main verdict** (statistics B-2); (f) **confirmation of the N12 OC convergence (class weighting removed)** (statistics M-4; if the single OC was already used before freeze, it is "early execution pending confirmation", and a rejection → P6 re-freeze, see the D5 P-N12 timing clause); (g) recording as is that the two refs runs came before the approved launch (item 6 of the G0 list in the design memo (not published), a freeze prerequisite).
3. Without LAUNCH_ORDER, the driver halts and waits before the first leg_start of the three legs (exit code 9); **no panel conclusion, operator judgement or clause of this document constitutes a launch authorization**.
4. Every launch goes through `train/launch_case.sh` (E8); the verdict appendix is written by the operator after the case closes, and the commit is the notarization; the exit syntax of the decision 12 ladder clause (D4-4) is the mandatory form of the verdict.

## Appendix: driver structure (engineering advice, normative)

`train/run_<vN>_content.py`: skeleton = run_b1_infra.py style (frozen W-PIN/W7 constant block at the top, `stage_done` idempotence, `pre()` preflight, separate `--smoke` entry) + the `exam_or_adopt`/`biteq`/leg loop/winner ruling sections of run_v32_sovereign.py. E0 baseline capture is done **outside** the driver, before the freeze commit; the driver only checks the sha constants + the G0_BASELINE transcription. LEGS table = the D3 per-leg additions column, verbatim. Two BC stages (S-bc1 v1 regeneration split assertion + BC_REGEN; S-bc2 v2 + N12_GATE with the OC use slot). The launch-order gate = a separate stage between FREEZE_SHA and the first leg_start: `if not stage_done(events,"LAUNCH_ORDER"): sys.exit(9)` (no NEEDS_ATTENTION). The canary loop is copied from B1, with the make-up deadline asserted in the driver on each leg's s16 FIRING_START. The exit code table is centralised as constants.

---

*(This document involves zero implementation and zero launches; E0 capture, E implementation, the live G0 runs and the freeze commit all come after panel review; the launch comes after the freeze and after the approval.)*

---
