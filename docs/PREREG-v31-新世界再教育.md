# PREREG-v31:新世界再教育(v3 赛季揭幕战;终稿)

状态:**终稿**(批评者面板 wf_8e3a60e2:一票 BLOCK + 两票 AMEND,4
blocker / 12 major / 15 minor 全部落地;BLOCK 之两项冻结先决——
RESUME_SMOKE 与 G-R31——已实弹通过并入台账)。冻结纪律循 R1/R2:
commit 即公证,正文零自指 sha。战役号 v31 依 ROADMAP 时代切换注记在此
授予;**课③④⑤之 v2 预排号(v31/v32/v33)全部作废,另行编号**(ROADMAP
同步注记随冻结 commit)。

## 案由与授权

R2 判决落账(4618ee2)起课程战役解锁。v3 世界与 v2 训练世界实质不同:
现任经理谱系全部在旧世界受教。**换届命题**:在 v3 世界重新教育经理,
能否击败现任组装体?双臂 = "v3 从零"对"v2 存量续训"——同时回答"旧知识
在新世界是资产还是包袱"(v25 裸微调 / v28 久系则缚的经理侧续篇)。

**立项裁量入册(面板 major)**:本案不在 ROADMAP 课表 ②-⑤ 之列,亦非
R2 后续节两候选(课⑤ / A 复赛权),系课①(v29 经理再教育)在 v3 世界之
重开考。路线图级核准对本案之适用系驾驶席推理:v3 时代一切课程战役以
"现任组装体在新世界是否仍配位"为先决,故换届命题构成课程序列之先行闸
——该推理如实入册,施工级授权仍以本预注册面板过审 + 冻结 commit 为准。

**授权链勘正(面板 major,逐字全文、禁省略号截头)**:总设计师
2026-07-12 夜原话全文:『把这两个PR趁今天抓紧报了吧 谢谢您 然后我们开始
训练吧 嘿嘿 我已经迫不及待地看到 重构后的代码的样子了』。注记:其中
"然后我们开始训练吧"之应答已在 R2 案由被消费为金种子重测之知情知会;
本案再引其为训练重启授权,系同一话语之第二次消费,推理链在此如实入册
(循 R2 转录纪律)。

**金种子知会出处(R1 条款承继,面板 major)**:本案若发射将烧金种子
9000-9031 一次(v31-golden)。知会出处 = 战役级金评纪律先例(v24-v30,
无逐案二次签批,该裁量已在 R2 案由入册);冻结前如未获总设计师对本案之
明示应答,GOLDEN_AUTHORIZED 事件强制携注记『知会出处 = 战役级纪律,无
本案专项应答』,如实入册。

**A 复赛权**:不消耗——A 系"经理熵配平复赛"(法庭原文),本案双臂同
ent 0.02,臂间零熵变量;其存续与"v3 新锚落地后方可动用"顺延条款不受
本案影响。

## 班底(全 sha 驱动器冻结常量,预检断言)

- 工人(不动):v28-worker-leg1 npz(976b6c05…)。**仪器口径修正**:
  v31 全案评测 worker 一律 **npz 表示**(v29 系 zip/npz 混用;npz≡zip
  位级等价在 v3 未重证,统一表示以杀仪器不对称——系口径裁量而非等价
  证明,入残余#3)。
- 现任组装体(被挑战者):v22-H × v28-leg1(v3 金池锚 133.9 =
  r2-launch)。**v3-launch-anchor 之消费定位**:金池档案与探针配对线
  异池,依 v29 逐种子同池配对纪律不能充当配对基线——此系另烧
  v31-ref-launch 之全部理由;133.9 之消费限于跨池观察、D3-0 断路器
  输入与名分对表,不构成锚之降格。
- 存量臂初始化:M29-fresh 检查点(9d5820bf…,nt=160000);其 npz 归档件
  89441388…。
- **R2 锚消费(全式,面板 major 补全)**:四档锚在全文 sha 断言之外,
  预检补做当前运行时可比性复验——freeze_eval_identity(逐档以其原阵容:
  launch=W×默认H / science=W×M29 / throne=v24-leg7×默认H / script=
  script×默认H)→ expected_eval_identity(tag, 9000-9031) →
  read_eval_archive → verify_eval_identity;任一不过 → 停机呈报
  "R2 锚与当前运行时不可比,须另立重锚案"(科学读数终局,禁止重烧)。
- **现任身份钉死(面板 major)**:参照发经理 == r2-launch 之经理,由
  freeze_eval_identity 默认经理路径之 DEFAULT_MANAGER_SHA256 内建断言 +
  r2-launch 全式复验共同保证,双 sha 随 preflight_ok 入册。

## D2 配方与预算

**施工条目(面板 BLOCK ①,随冻结 commit 入库)**:train_ppo.py 增设
经理侧 options resume 口——① 放行门 `(worker ∨ options) ∧ algo==mppo`;
② options 路检查点走通用闸(CRC/关键成员/步数/权重有限),不作
distill_beta 断言(该标记系工人 Leashed 专属);③ 加载类保真
`MaskablePPO.load`(存什么类续什么类,不涉教师/β,无 G-KL-B 义务),
v24 封条断言(lr/ent/gamma/n_steps/batch/target_kl/seed 重设)原封;
④ `--teacher-override` 收紧为 worker 侧专用。施工限训练侧,eval 协议束
零接触。**冻结先决(已履行,台账在案)**:RESUME_SMOKE(rc=0,
160000→160256 逐步连续)+ G-R31(零训练加载导出 == 归档 npz,6/6 数组
逐位相等,parity 0/1000;驱动器每次发车重证)。

**探针池参照(开场两发,7000-7031,闸门用途零金种子)**:
- `v31-ref-launch` = W-npz × 默认 H——发射配对之同池参照,其均值 R 系
  裁决线现场推导基准;`v31-ref-science` = W-npz × M29-npz——前朝谱系
  v3 探针读数,cont 臂净变之配对基准。refs 事件记两发全套深度仪表
  (died/depth2/DIVE/奖金/哨兵三率)。
- **D3-0 参照健全闸(面板 BLOCK ③;运维护栏,非科学裁决)**:
  v31-ref-launch 须 died ≤ 8 ∧ R ∈ [100, 170](端点系面板注册值,以
  金池锚 133.9 为中枢之宽带;133.9 在此仅作断路器输入,与"跨池只记
  不裁"并行不悖)。越界 → CASE_HALT_REF_CORRIDOR 停机呈报,不训臂、
  不烧金种子、禁以任何在册档案替充基准;复测须另案重冻结。本闸系 v29
  G-A0 仪器闸在换世界后的等价替身。
- **参照终局条款(面板 blocker/major 合并)**:两参照一经有效落档即
  案级冻结(sha 随 ref_archive 事件入台账);续跑对其不适用"目标档案
  不存在"预检,按台账 sha 采信首考,禁 .void 重烧;档案损毁/复验不过 →
  REF_INVALID 全案停机,经勘误 commit 后方可全案重置(CASE_RESET,全案
  至多一次,旧臂档案一并作废,禁新参照对旧臂混对)。参照发仅崩溃/无效
  档可自动重考一次(exam_retry 语义),分数高低不构成重考理由;两次皆
  败 → 停机,禁以 r2-launch 跨池替充。参照两发必须先于任何臂训练收档,
  裁决线绝对数以 refs 事件落账值为准,案中禁重算。
- **案级运行时对账(面板 major;R2 W10 之案内版)**:preflight 落
  CASE_RUNTIME(bridge/engine/game_data/assets/python_protocol 五 sha);
  此后每次 exam 发车前断言当次快照与之逐字相等,不等 → 停机呈报
  CASE_HALT_RUNTIME_DRIFT,不按崩溃重考、不落 .void。

**双臂串行**(共用:`--options --algo mppo --gamma 1.0 --max-steps 3000
--n-steps 64 --num-envs 4 --worker-npz <W-npz>`):
- **v31-mfresh**:v29-fresh 配方逐字(lr 3e-4,ent 0.02,seed 22),
  `--total-steps 160000`;nt 闸 == 160000。
- **v31-mcont**:`--resume-from <M29 zip> --allow-legacy-resume`,
  lr 3e-4,ent 0.02,seed 26,**`--total-steps 160000`(增量语义,面板
  BLOCK ②勘误:train_ppo 之 --total-steps 系"新增样本数",resume 腿
  reset_num_timesteps=False,SB3 以 += 续训;v24 审计注释在案)**——在
  160000 存量上再教育 160000;**nt 闸 == 320000**(=1250×256 账面注记;
  新增段 160000=625×256 满足整除闸)。驱动器 ARMS 拆双字段:cli_steps
  (入命令行)与 nt_target(达标闸)。
- **R31.2 判词口径 + 双口径义务(面板 major)**:两臂对比 = "v3 从零
  路径 vs v2 存量续训路径"整体对比(init/步数账/seed 捆绑于路径定义),
  禁单因素归因;任何引用两臂对比之判词/附录,必须**同时并列**"cont 总
  步数 2×(320k 累计)"与"新世界步数相等(各 160k v3 步)"两种口径,
  禁单口径引用;cont 败 → 判词强制携"超参未做续训适配(lr 无日程、
  优化器状态承继、熵沿 fresh 配方),溃败系路径级结果,不得读作旧知识
  必为包袱(v25 裸微调先例)"限定;cont 胜 → 禁归因于总步数/初始化/
  种子任一单因素。r31_2 系单次配对描述量,禁作显著性解读。
- 时钟:臂时预计 10-30 分(R2 后评测提速实测),timeout 仍 4h/臂
  (余量纪律);评测 30 分。

## D3 裁决梯(裁决线由 R 现场推导;**正典 = 分数 75/112.4 与 85/92**,
0.6673/0.9239 系展示舍入,不作计算口径;判词文本印分数原文)

0. **参照健全闸**(见 D2;先于一切臂训练)。
1. **提前放弃闸**:双臂 s16 均 < (75/112.4)×R → "训练失败,换届命题
   未考"。
2. **资格逐臂**(v29 逐字):死 ≤6 ∧ 非作废 ∧ 哨兵闸(descend ≤2.04% /
   cap <5% / override <3%,≥8% 作废;双归因条款原封);胜者 = 过资格臂
   中满 32 均值最高(±0.05 → 死少 → 取 mfresh);递补与无胜者档原封。
3. **复现地板**:胜者 < (85/92)×R → "重训未复现参考水平,命题未考"。
4. **发射线**:配对 vs v31-ref-launch(seed 键 join + 集合断言):均差
   ≥+4 ∧ 赢 ≥18/32 ∧ 死 ≤6 ∧ 哨兵;双归因先裁后烧原封。
5. **不发射档穷尽**(v29 逐字):点估增益 / 探针级 [+2,+4) / 连任 <+2;
   赢 ≥14 附宽度注记。
6. **金牌(若发射,单臂一次,值守手启)**:组装体 = 胜者 npz × W-npz,
   金池,tag v31-golden。**P 线对王座在位锚值 97.7(r2-throne,R2 重测
   值;重测不改名分,王座头衔仍系在位组装体)与脚本参照 94.2
   (r2-script)对表,数值由档案现场读取**:死>6 → 回退;金 ≥ 王座+4 ∧
   死≤4 → P31-登基;**∈(王座, 王座+4)** ∧ 死≤4 → 点估增益王座不动
   (开区间,循 v29 口径,面板勘误);>王座 ∧ 死5-6 → 持平(安全性);
   ∈[脚本, 王座] → 持平;<脚本 → 回退。**金池开牌史注记**:金牌实开
   v22/v23/v24 三次(v29/v30 零接触);R2 定锚五发系测量暴露非开牌;
   本案若发射为金牌第 4 次实开、金池累计第 9 次暴露,固定池偏置随判词。
   **名分流转条款(面板 blocker;两闸两名分,COURT wf_5fa772de 口径
   成文化)**:王座与现任组装体系两套独立名分,分别由金评 P 线与发射
   配对线裁决,判词分行宣示,禁止合并叙事。(a) **换届判据**:胜者过
   D3-4 发射线 ∧ 金评非回退档(金 ≥ 94.2 ∧ 金评死 ≤6)→ 现任组装体
   换届:新现任 = 胜者经理 × v28-worker-leg1;v3 发射锚随届流转 :=
   v31-golden 档案(锚随王走);r2-launch 133.9 降格为前任历史读数,
   不再充当后续配对/对表基准,由判决附录注记废止其 v3-launch-anchor
   身份。(b) 金评回退档(<94.2 或 死>6)→ 换届未遂:现任与发射锚不动,
   判词记"发射后金评回退,换届未遂",发射事实照常入册。(c) **P 线只动
   王座**:P31-登基仅裁王座名分;换届成立而王座不动之组合合法,判词
   强制注记"现任组装体与王座分属不同组装体(v28 以来既成格局)"并分列
   两者金池读数。(d) 未发射 → 两套名分皆不动,判词禁用"连任"以外之
   名分动词。
7. **深度副判 + 现场基线条款(面板 major)**:三档(≥12 已学 / ≤7 未
   解锁 / 带外)边界系 v2 承继数字;refs 事件记 ref-launch 实测 depth2
   为**现场基线**,判词强制并引;若现场基线 ≥12,"已学"档强制改述
   "达 v2 承继线,但现场基线已在线上,无鉴别力";任何档位单独不作
   Mark 论据(v29 D3-7 尾注纪律)。
8. **科学观察(只记不裁,面板勘误)**:**恒算 cont 臂**满 32 对
   v31-ref-science 之逐种子配对差("存量续训相对其前身之净变",不随
   胜者切换);胜者非 cont 时另附 winner 对照作跨谱系旁证;放弃闸路径
   下该观察如实缺席,判词注记。
9. **多重比较台账(只记不裁,承 v30 D3-7)**:同池 18/32 线第 5 次
   挑战者开奖(11→16→17→v30 未及发射检→本案);P(赢≥18|p=.5)≈43%
   注记随判词;本案基线系现场推导之 R 而非历史档案,一并注记。
10. **线效力未重标定限定(面板 major)**:本案全部裁决线系 v2 操作
    特性之形式承继,v3 分数尺度与方差结构下的一类/二类错误率未重标定;
    临线判决(均差距线 ≤1.0 或赢数距线 ≤1)判词强制携"临线 + 线未重标"
    注记;死亡闸/作废线对深潜行为在 v3 系统性偏紧列为已知偏置,凡以其
    裁决之判词须并引 depth 分布与逐种子 died;线之重标定另案,本案内
    禁调线。

## R 线(可证伪预测;点值系驾驶席冻结前登记)

| # | 预测 | 带,点 |
|---|---|---|
| R31.1 | v31-ref-launch 均值 R | ∈[100,145],点 125 |
| R31.2 | 配对(cont−fresh,满 32 同种子) | ∈[−15,+15],点 0(双向开) |
| R31.3 | 胜者对 R 配对均差 | ∈[−10,+15],点 +2 |
| R31.4 | 胜者 depth≥2 种子数 | ∈[8,28],点 16(v3 深度上移背景) |
| R31.5 | 金牌(若发射) | ∈[95,140],点 115 |

## 残余不确定性

1. 胜者系 max-of-2 顺序统计量,发射线一类错误率放大(v29 残余#6 承继)。
2. 两臂 init/步数账/seed 三者捆绑于路径定义,归因限于整体对比。
3. 裁决线由 R 现场推导:R 自身系一次 32 种子抽样,比例线随其方差平移
   ——如实注册,不设重烧(D3-0 走廊防的是崩塌,不防抽样噪声)。
4. npz≡zip 位级等价在 v3 未重证,统一 npz 系口径裁量而非证明。
5. 探针池同池挑战开奖之族错误率累计,披露见 D3-9。
6. cont 臂 --allow-legacy-resume 系一次性迁移口注册用例,旧检查点在
   v3 引擎下之数值语义未经位级证明,以 nt 闸/封条断言/G-R31 兜底。
7. cont 超参未做续训适配(lr 无日程、优化器状态承继)。

## D4 P 线(v29 运维护栏全套承继 + 本案增补)

顶层异常 → DRIVER_EXCEPTION + NEEDS_ATTENTION;训练 4h/评测 30 分超时
(killpg 全组 + ProcessLookupError 竞态护挡);exam 拒覆写 + .void 轮转 +
时间戳日志;nt_zip 唯一计步源(逐臂对 nt_target 精确断言);preflight
(R2 四锚全式复验 / 工件 sha / RESUME_SMOKE 在册断言 / G-R31 重证 /
目标档案不存在(参照两发除外,走终局采信)/ ARMS 目录无残留 /
PROTOCOL_VERSION==3 自拒闸);CASE_RUNTIME 每考对账;驱动器
exclusive_lock;训练失败/未达标 → 停机呈报,本版不追加重训(v25 条款)。
**退出码对照(面板 minor,预检通道统一:驱动器 require 一律
OperationalFailure)**:0 = 判决落账(含不发射档)/ GOLDEN_AUTHORIZED
待手启;2 = 运维失败(预检不过 / 参照健全闸 / 训练未达标 / 考试连败 /
运行时漂移 / 台账不可解析);其余非零/异常死亡 = P1(DRIVER_EXCEPTION)。

## D5 台账与判词纪律

`train/runs/v31/gate_ledger.jsonl`。满 32 事件携深度仪表;arm_done 含
steps_status 诊断字段(nt_zip 唯一计步源不变——克隆差异如实申报);
连败路径记 STOP 事件 + attention(v29 事件词表对齐)。**判词纪律(面板
major)**:GOLDEN_AUTHORIZED 与一切 VERDICT_PATH 事件强制携两臂 full32
均值、r31_2 与 R 三数。判决附录写于本文末。
